python -c "import sys; from pathlib import Path; import subprocess; param = sys.argv[1] if len(sys.argv) > 1 else ''; workspace_root = Path.cwd(); script = workspace_root / r'agile_bot/bots/story_bot/src/story_bot_cli.py'; cmd = [sys.executable, str(script), 'shape', 'initialize_project', '--confirm=true']; cmd.extend(['--project_area=' + param] if param else []); subprocess.run(cmd)" ${1:}