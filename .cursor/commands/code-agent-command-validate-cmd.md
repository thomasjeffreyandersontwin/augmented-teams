### Command: `/code-agent-command-validate`

**Purpose:** Validate files for a command. Delegates to main command with explicit validate action.

**Usage:**
* `/code-agent-command-validate [feature-name] [command-name]` — Validate command files (AI determines parameters if not provided)

**Steps:**
1. **Code** Execute the validate action in `/code-agent-command`

