apiVersion: apps/v1
kind: ContainerApp
metadata:
  name: mcp-proxy
  namespace: mcp-proxy
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: mcp-proxy
        feature: mcp-proxy
    spec:
      containers:
      - name: mcp-proxy
        image: augmentedteams-hfdzfjhghshucaf6.azurecr.io/mcp-proxy:latest
        ports:
        - containerPort: 8000
        env:
        # Service Configuration
        - name: SERVICE_PORT
          value: "8000"
        - name: SERVICE_HOST
          value: "0.0.0.0"
        - name: ENVIRONMENT
          value: "production"
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "0.5Gi"
            cpu: "0.25"
          limits:
            memory: "0.5Gi"
            cpu: "0.25"
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
  service:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
