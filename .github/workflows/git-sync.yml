name: Test Workflow

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "✅ Actions are working!"
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Print Working Directory (sanity test)
        run: pwd

      - name: List Files
        run: ls -R

      - name: (Placeholder) Run Git Sync Script
        run: echo "Would run src/integration/git/git_sync.py here"
