name: Deploy Complete System

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  deploy-all-features:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"
    - name: Install dependencies
      run: |
        pip install docker requests pyyaml fastapi uvicorn pydantic
    - name: Install Docker
      uses: docker/setup-buildx-action@v3
    - name: Login to Azure Container Registry
      uses: azure/docker-login@v1
      with:
        login-server: augmentedteams-hfdzfjhghshucaf6.azurecr.io
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
    - name: Deploy all features
      env:
        ACR_PASSWORD: ${{ secrets.ACR_PASSWORD }}
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: |
        echo "🚀 Deploying all features..."
        echo ""
        
        # Find all features at features/* level
        FEATURES=""
        for dir in features/*; do
          DIRNAME=$(basename "$dir")
          if [ "$DIRNAME" = "containerization" ] || [ "$DIRNAME" = "_template" ]; then
            continue
          fi
          if [ -f "$dir/config/provision-service.py" ]; then
            FEATURES="$FEATURES $DIRNAME"
          fi
        done
        FEATURES=$(echo "$FEATURES" | xargs)
        
        echo "📦 Features found: $FEATURES"
        echo ""
        
        for FEATURE in $FEATURES; do
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🚀 Deploying $FEATURE..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          python "features/$FEATURE/config/provision-service.py" AZURE --always
          echo "✅ $FEATURE deployed"
          echo ""
        done
        
        echo "✅ ALL FEATURES DEPLOYED!"
