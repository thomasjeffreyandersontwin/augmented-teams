{
  "behaviorName": "discovery",
  "instructions": [
    "Discovery diagram filenames MUST include increment identifier:**",
    "",
    "The render-discovery synchronizer automatically modifies output filenames:",
    "- Single increment: Appends sanitized increment name or number",
    "  Example: 'story-map-discovery.drawio' → 'story-map-discovery-Foundation-Create-Mob.drawio'",
    "- Multiple increments: Appends priority range",
    "  Example: 'story-map-discovery.drawio' → 'story-map-discovery-Increments-1-2.drawio'",
    "",
    "Verify the returned 'output_path' confirms the increment identifier was added."
  ]
}
