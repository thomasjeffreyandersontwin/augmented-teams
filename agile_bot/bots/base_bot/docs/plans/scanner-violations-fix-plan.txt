================================================================================
INTEGRATED INCREMENTAL PLAN TO FIX ALL SCANNER VALIDATION VIOLATIONS
================================================================================

Generated: 2025-12-19
Total Violations: 1,189 (590 Errors, 497 Warnings, 102 Info)
Priority: Fix errors first, then warnings, ensuring fixes don't introduce new violations

================================================================================
DOMAIN MODEL (CRC Format) - Resource-Oriented
================================================================================

Scanner Orchestrator
    Selects scanner helpers by rule: ScannerRegistry, Rule, Scanner
    Returns scan: Scan
   Performs scan on scope: Scan,Scope, Rule, Scanner

Scanner
    Performs scan for one rule: Scan,Scope,Rule
    Associated with rule: Rule
    Checks file naming: File,FileNamingChecker
    Checks class naming: Block,ClassNamingChecker
    Checks method naming: Block,MethodNamingChecker
    Analyzes code structure: Block,CodeStructureAnalyzer
    Examines AST for violations: Block,CodeStructureAnalyzer
    Identifies code patterns: Block,PatternCollection,CodeStructureAnalyzer

Scan
    Has violations: Violation
    Created for scope: Scope
   undergoes a scan: Scope,Rule, Scanner 

Scope
    Has files: File
    Has blocks: Block

File
    Has lines: Line
    Has blocks: Block,BlockExtractor
    Parses safely: 
    Checks if test file: 
    Parses Python file: 
    Checks file naming: FileNamingChecker 

Line
    Belongs to file: File
    Has number: 
    Has content: 
    Extracts from AST node: 
    Extracts from position: 

Block
    Has content: 
    Has subblocks: Block
    Has similarity: Block,SimilarityCalculator
    Has violations: Violation,ViolationReporter
    References file: File
    Normalizes content: 
    Analyzes structure: CodeStructureAnalyzer
    Calculates complexity: ComplexityMetrics
    Checks class naming: ClassNamingChecker
    Checks method naming: MethodNamingChecker

Walk
    Has lines: Line
    Has similarity: Block 

Violation
    References rule: Rule
    References block: Block
    References scan: Scan
    Creates from rule and context: Rule

Rule
    Defines validation: 
    Validates content: Scan,Scope
    May be violated : Scan, Violations

ScannerRegistry
    Finds scanner by rule: Rule
    Loads scanner class: 
    Registers helper: Scanner

PatternCollection
    Contains pattern: 
    Matches text: 

ComplexityMetrics
    Calculates cognitive complexity: 
    Calculates cyclomatic complexity: 
    Calculates max nesting depth: 
    Calculates LCOM: 
    Detects function responsibilities: 
    Detects class responsibilities: 

BlockExtractor
    Extracts blocks from file: File,Block
    Identifies code blocks: File,Block

SimilarityCalculator
    Calculates block similarity: Block
    Groups similar blocks: Block
    Compares block content: Block

GivenWhenThenHelpersScanner : Scanner
    Scans helper functions: Block,PatternCollection
    Validates GWT structure: Block,PatternCollection

ImportPlacementScanner : Scanner
    Scans import placement: Block
    Validates import order: Block

IntentionRevealingNamesScanner : Scanner
    Scans variable names: Block,PatternCollection
    Scans function names: Block,PatternCollection
    Validates naming clarity: Block,PatternCollection

RealImplementationsScanner : Scanner
    Scans implementation details: Block
    Validates real implementations: Block

SpecificationMatchScanner : Scanner
    Scans test specifications: Block
    Validates spec matching: Block

VerbNounScanner : Scanner
    Scans verb noun format: Block,PatternCollection
    Validates naming format: Block,PatternCollection

BadCommentsScanner : Scanner
    Scans comments: Block
    Detects bad comments: Block

ExcessiveGuardsScanner : Scanner
    Scans guard clauses: Block
    Detects excessive guards: Block

FunctionSizeScanner : Scanner
    Scans function size: Block,ComplexityMetrics
    Detects large functions: Block,ComplexityMetrics

ClassBasedOrganizationScanner : Scanner
    Scans file organization: File,Block
    Coordinates naming checks: File,Block

FileNamingChecker
    Checks file name matches sub epic: File
    Validates file naming conventions: File

ClassNamingChecker
    Checks class name matches story: Block
    Validates class naming conventions: Block

MethodNamingChecker
    Checks method name matches scenario: Block
    Validates method naming conventions: Block

CodeStructureAnalyzer
    Analyzes code structure: Block
    Examines AST for violations: Block
    Identifies code patterns: Block,PatternCollection

ViolationReporter
    Creates violation from block: Violation,Block
    Formats violation message: Violation
    Reports duplicate blocks: Violation,Block

ScannerRegistry
    Finds scanner helper by rule: Rule
    Loads scanner helper class: 
    Registers scanner helper:

================================================================================
PHASE 1: CRITICAL ERROR FIXES (590 Violations)
================================================================================

INCREMENT 1.1: Quick Fixes (5 Violations)
------------------------------------------
Goal: Fix simple, isolated errors that don't require major refactoring

1. Import Placement (1 violation)
   File: src/scanners/story_scanner.py:9
   Action: Move import statement to top of file
   
   Validation Command (after fix):
   python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/story_scanner.py
   
   Expected: Import placement violation should be fixed, no new violations

2. Resource-Oriented Design (1 violation)
   File: src/scanners/scanner_loader.py:6
   Action: Refactor ScannerLoader to be a property/method of a domain object
          (e.g., ScannerRegistry class or make it part of Rule class)
   
   Validation Command (after fix):
   python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/scanner_loader.py
   
   Expected: Resource-oriented violation should be fixed, no new violations

3. Test Quality (3 violations)
   Files: Check report for specific test quality issues
   Action: Fix test structure/naming as needed
   
   Validation Command (after fix):
   python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior tests --action validate agile_bot/bots/base_bot/test
   
   Expected: Test quality violations should be fixed

Final Validation Checkpoint (after all 1.1 fixes):
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/story_scanner.py src=agile_bot/bots/base_bot/src/scanners/scanner_loader.py


INCREMENT 1.2: Swallowed Exceptions (74 Violations)
----------------------------------------------------
Goal: Fix all empty except blocks

Strategy:
1. Create helper function: _log_and_continue(error, context) for non-critical errors
2. Replace all "except: pass" with proper logging
3. For syntax errors in file parsing: log with file context
4. For critical errors: re-raise with context

Files to Fix (74 total):
- All scanner files with "except: pass" blocks
- Pattern: Replace "except (SyntaxError, UnicodeDecodeError): pass" 
  with "except (SyntaxError, UnicodeDecodeError) as e: logger.debug(...)"

Implementation Pattern:
  # Before:
  except (SyntaxError, UnicodeDecodeError):
      pass

  # After:
  except (SyntaxError, UnicodeDecodeError) as e:
      logger.debug(f'Skipping file {file_path} due to {type(e).__name__}: {e}')

Validation Commands (after each batch of 10 files):
  Batch 1 (files 1-10):
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/file1.py src=agile_bot/bots/base_bot/src/scanners/file2.py ... src=agile_bot/bots/base_bot/src/scanners/file10.py
  
  Batch 2 (files 11-20):
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/file11.py src=agile_bot/bots/base_bot/src/scanners/file12.py ... src=agile_bot/bots/base_bot/src/scanners/file20.py
  
  (Continue for all batches)
  
  Expected: Swallowed exception violations should be fixed, no new violations introduced


INCREMENT 1.3: Duplication - File-by-File (11 Violations)
----------------------------------------------------------
Goal: Extract duplicate code blocks into helper functions

Files and Fixes:

1. bad_comments_scanner.py:71-95
   Extract: _create_commented_code_violation(rule_obj, file_path, line_num)

2. duplication_scanner.py:1005-1028
   Extract: _check_helper_pattern_match(func_name, helper_patterns)

3. excessive_guards_scanner.py:191-235 (4 locations)
   Extract: _check_optional_pattern(var_name, optional_patterns)
   Consolidate the 4 duplicate blocks

4. given_when_then_helpers_scanner.py:94, 113, 268
   - Remove duplicate _get_defined_helper_functions (keep one)
   - Remove duplicate _get_helper_calls_in_file (keep one)
   - Extract: _end_current_block(current_block_start, line_num, current_block_lines)

5. intention_revealing_names_scanner.py:136-253
   Extract: _create_generic_name_violation(rule_obj, file_path, node, name, name_type)

6. primitive_vs_object_scanner.py:105-126
   Extract: _create_primitive_violation(rule_obj, file_path, func_node, arg, type_name, message)

7. specification_match_scanner.py:70-131
   Extract: _create_violation_with_line_number(rule_obj, file_path, node, message)

8. story_map.py:139, 150
   - Remove duplicate steps() method
   - Remove duplicate default_test_method() method

Validation Commands (after each file fix):
  After fixing bad_comments_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py
  
  After fixing duplication_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/duplication_scanner.py
  
  After fixing excessive_guards_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py
  
  After fixing given_when_then_helpers_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py
  
  After fixing intention_revealing_names_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/intention_revealing_names_scanner.py
  
  After fixing primitive_vs_object_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py
  
  After fixing specification_match_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py
  
  After fixing story_map.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/story_map.py
  
  Expected: Duplication violations should be fixed, no new violations introduced


INCREMENT 1.4: Duplication - Cross-File (500 Violations)
----------------------------------------------------------
Goal: Address cross-file duplication patterns

Strategy:
1. Create shared utilities module: src/scanners/scanner_utilities.py
   - Common violation creation helpers
   - Common AST traversal patterns
   - Common pattern matching utilities

2. Extract Common Patterns:
   - Violation creation: create_violation(rule_obj, file_path, line_number, message, severity)
   - File parsing: parse_file_safely(file_path) -> Optional[ast.AST]
   - Pattern matching: matches_patterns(text, patterns) -> bool

3. Refactor Scanners to Use Shared Utilities:
   - Update imports (all at top)
   - Replace duplicate violation creation
   - Replace duplicate file parsing
   - Replace duplicate pattern matching

4. Create Collection Classes for Pattern Delegation (29 violations):
   - PatternCollection class for pattern collections
   - Refactor scanners to use PatternCollection.contains(pattern) instead of iterating

Validation Commands (after each step):
  After creating scanner_utilities.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/scanner_utilities.py
  
  After creating pattern_collections.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/pattern_collections.py
  
  After refactoring each batch of 20 scanners:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/scanner1.py src=agile_bot/bots/base_bot/src/scanners/scanner2.py ... src=agile_bot/bots/base_bot/src/scanners/scanner20.py
  
  Expected: Cross-file duplication violations should be reduced, no new violations introduced


================================================================================
PHASE 2: WARNING FIXES (497 Violations)
================================================================================

INCREMENT 2.1: Excessive Guards (3 Violations)
------------------------------------------------
Files:
- code_scanner.py:75 - Remove None check
- cover_all_paths_scanner.py:44 - Remove truthiness check
- violation.py:82 - Remove None check

Action: Remove guards, let code fail fast

Validation Commands (after each fix):
  After fixing code_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/code_scanner.py
  
  After fixing cover_all_paths_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py
  
  After fixing violation.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/violation.py
  
  Expected: Excessive guard violations should be fixed, no new violations introduced


INCREMENT 2.2: Delegation to Lowest Level (29 Violations)
----------------------------------------------------------
Strategy: Create PatternCollection classes

1. Create base: src/scanners/pattern_collections.py
   class PatternCollection:
       def __init__(self, patterns: List[str]):
           self._patterns = patterns
       
       def contains(self, text: str) -> bool:
           return any(pattern in text.lower() for pattern in self._patterns)

2. Refactor each scanner to use PatternCollection:
   - Replace "for pattern in PATTERNS:" with "if pattern_collection.contains(text):"
   - Create PatternCollection instances as class attributes

Files: 29 scanners with delegation violations

Validation Commands (after each batch of 5 scanners):
  Batch 1 (scanners 1-5):
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/scanner1.py src=agile_bot/bots/base_bot/src/scanners/scanner2.py ... src=agile_bot/bots/base_bot/src/scanners/scanner5.py
  
  (Continue for all 29 scanners in batches of 5)
  
  Expected: Delegation violations should be fixed, no new violations introduced


INCREMENT 2.3: Function Size (51 Violations)
--------------------------------------------
Strategy: Extract helper functions

Approach:
1. Functions >20 lines: Extract logic to helper functions
2. Deep nesting (>4 levels): Extract nested blocks
3. High cognitive complexity (>15): Extract complex logic

Priority Files:
- duplication_scanner.py (largest class, likely has large functions)
- class_based_organization_scanner.py
- code_scanner.py
- complexity_metrics.py
- Others per report

Validation Commands (after each file):
  After fixing duplication_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/duplication_scanner.py
  
  After fixing class_based_organization_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py
  
  After fixing code_scanner.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/code_scanner.py
  
  After fixing complexity_metrics.py:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/complexity_metrics.py
  
  (Continue for all files with function size violations)
  
  Expected: Function size violations should be fixed, no new violations introduced




INCREMENT 2.5: Control Flow Simplification (167 Violations)
-------------------------------------------------------------
Strategy: Use guard clauses and extract nested logic

Approach:
1. Convert nested ifs to guard clauses
2. Extract deeply nested blocks to helper functions
3. Reduce nesting depth to <=4

Validation Commands (after each batch of 20 files):
  # After fixing control flow in first 20 files:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/[list of 20 changed files]
  
  # Or validate all scanners, excluding unchanged ones:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate agile_bot/bots/base_bot/src/scanners --exclude unchanged_file1 unchanged_file2
  
  Expected: Control flow violations should decrease, no new violations


INCREMENT 2.6: Bad Comments (118 Violations)
---------------------------------------------
Strategy: Remove commented code and misleading comments

Approach:
1. Delete commented-out code (it's in git)
2. Remove misleading/outdated comments
3. Remove redundant comments that state the obvious

Validation Commands (after each batch of 20 files):
  # After removing bad comments from first 20 files:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/[list of 20 changed files]
  
  Expected: Bad comments violations should decrease, no new violations


INCREMENT 2.7: Meaningful Context (97 Violations)
-------------------------------------------------
Strategy: Replace magic numbers and numbered variables

Approach:
1. Magic numbers -> Named constants
2. Numbered variables (name1, name2) -> Descriptive names
3. Generic names -> Context-specific names

Validation Commands (after each batch of 20 files):
  # After fixing meaningful context in first 20 files:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/[list of 20 changed files]
  
  Expected: Meaningful context violations should decrease, no new violations


INCREMENT 2.8: Remaining Warnings
----------------------------------
- Vertical Density (59)
- Clear Parameters (33)
- Natural English (14)
- Refactor Completely (6)
- Open-Closed Principle (4)
- Encapsulation (3)
- Calculation Timing (1)
- Domain Language (1)


================================================================================
PHASE 3: VALIDATION AND TESTING
================================================================================

INCREMENT 3.1: Run Full Validation
-----------------------------------
python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate agile_bot/bots/base_bot/src/scanners


INCREMENT 3.2: Run Test Scanners
---------------------------------
python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior tests --action validate agile_bot/bots/base_bot/test


INCREMENT 3.3: Run Unit Tests
------------------------------
pytest agile_bot/bots/base_bot/test -v


INCREMENT 3.4: Final Validation
--------------------------------
- Verify no new violations introduced
- Verify all errors fixed
- Verify warning count reduced
- Verify tests pass


================================================================================
IMPLEMENTATION GUIDELINES
================================================================================

CODE QUALITY RULES (While Fixing):
----------------------------------
1. Imports at Top: All imports at module top
2. No Useless Comments: Don't add obvious comments
3. Resource-Oriented: Use OO design, avoid manager/doer patterns
4. Meaningful Names: No numbered variables, no magic numbers
5. Small Functions: <=20 lines, extract helpers
6. Small Classes: <=300 lines, extract related classes
7. No Swallowed Exceptions: Log or re-raise
8. Guard Clauses: Avoid excessive guards
9. Delegation: Use collection classes for pattern matching


VALIDATION AFTER EACH INCREMENT:
---------------------------------
CRITICAL: Run validation IMMEDIATELY after each file or batch of files is changed.

Validation Command Pattern:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=<file1> src=<file2> ...
  
  OR for multiple files:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate src=agile_bot/bots/base_bot/src/scanners/file1.py src=agile_bot/bots/base_bot/src/scanners/file2.py
  
  OR exclude unchanged files:
  python agile_bot/bots/story_bot/src/story_bot_cli.py --behavior code --action validate agile_bot/bots/base_bot/src/scanners --exclude unchanged_pattern1 unchanged_pattern2

Validation Steps:
1. Run validation on changed files using src= parameter
2. Check validation report for violations in changed files
3. Verify violations decreased (or fixed)
4. Verify NO new violations introduced
5. Run relevant unit tests for changed files
6. Fix any regressions before proceeding to next increment

Test Command Pattern:
  pytest agile_bot/bots/base_bot/test/test_[specific_test].py -v
  
  OR run all scanner tests:
  pytest agile_bot/bots/base_bot/test -k scanner -v


TESTING STRATEGY:
----------------
1. Unit tests for new helper functions
2. Integration tests for refactored scanners
3. Validation tests to ensure scanners still work
4. Regression tests to ensure no new violations


================================================================================
SUCCESS CRITERIA
================================================================================

- Zero error violations
- Warning violations reduced by >=80%
- All tests passing
- No new violations introduced
- Code follows all validation rules


================================================================================
NOTES
================================================================================

This plan addresses all violations incrementally, with validation checkpoints
to prevent regressions. Each increment is small enough to validate independently
while building toward the complete solution.

The plan prioritizes:
1. Errors over warnings
2. Simple fixes over complex refactoring
3. Shared utilities to reduce duplication
4. Incremental validation to catch issues early

