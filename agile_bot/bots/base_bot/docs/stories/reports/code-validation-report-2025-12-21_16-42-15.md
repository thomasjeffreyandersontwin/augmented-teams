# Validation Report - Code

**Generated:** 2025-12-21 17:21:03
**Project:** base_bot
**Behavior:** code
**Action:** validate

## Summary

Validated story map and domain model and 228 code file(s) against **32 validation rules**.

## Content Validated

- **Rendered Outputs:**
  - `story-graph.json`
- **Code Files Scanned:**
  - `src\actions\action.py`
  - `src\actions\action_context.py`
  - `src\actions\action_factory.py`
  - `src\actions\action_scope.py`
  - `src\actions\action_state_manager.py`
  - `src\actions\actions.py`
  - `src\actions\activity_tracker.py`
  - `src\actions\build\build_action.py`
  - `src\actions\build\build_scope.py`
  - `src\actions\build\knowledge.py`
  - `src\actions\build\knowledge_graph_spec.py`
  - `src\actions\build\knowledge_graph_template.py`
  - `src\actions\clarify\clarify_action.py`
  - `src\actions\clarify\evidence.py`
  - `src\actions\clarify\key_questions.py`
  - `src\actions\clarify\recommended_activities.py`
  - `src\actions\clarify\required_context.py`
  - `src\actions\clarify\requirements_clarifications.py`
  - `src\actions\content.py`
  - `src\actions\context_data_injector.py`
  - `src\actions\guardrails.py`
  - `src\actions\help_action.py`
  - `src\actions\instructions.py`
  - `src\actions\render\evidence.py`
  - `src\actions\render\render_action.py`
  - `src\actions\render\render_config_loader.py`
  - `src\actions\render\render_instruction_formatter.py`
  - `src\actions\render\render_scope.py`
  - `src\actions\render\render_spec.py`
  - `src\actions\render\synchronizer.py`
  - `src\actions\render\template.py`
  - `src\actions\rules\rule.py`
  - `src\actions\rules\rule_filter.py`
  - `src\actions\rules\rule_loader.py`
  - `src\actions\rules\rules.py`
  - `src\actions\rules\rules_action.py`
  - `src\actions\scoping_parameter.py`
  - `src\actions\strategy\assumptions.py`
  - `src\actions\strategy\json_persistent.py`
  - `src\actions\strategy\recommended_activities.py`
  - `src\actions\strategy\strategy.py`
  - `src\actions\strategy\strategy_action.py`
  - `src\actions\strategy\strategy_criteria.py`
  - `src\actions\strategy\strategy_criterias.py`
  - `src\actions\strategy\strategy_decision.py`
  - `src\actions\validate\background_validation_handler.py`
  - `src\actions\validate\file_discovery.py`
  - `src\actions\validate\file_link_builder.py`
  - `src\actions\validate\knowledge_graph.py`
  - `src\actions\validate\path_resolver.py`
  - `src\actions\validate\scanner_status_formatter.py`
  - `src\actions\validate\scanners\useless_comments_scanner.py`
  - `src\actions\validate\story_graph.py`
  - `src\actions\validate\validate_action.py`
  - `src\actions\validate\validation_executor.py`
  - `src\actions\validate\validation_report_builder.py`
  - `src\actions\validate\validation_report_formatter.py`
  - `src\actions\validate\validation_report_writer.py`
  - `src\actions\validate\validation_scanner_status_builder.py`
  - `src\actions\validate\validation_scope.py`
  - `src\actions\validate\validation_stats.py`
  - `src\actions\validate\validation_violations_builder.py`
  - `src\actions\validate\violation_formatter.py`
  - `src\actions\workflow_status_builder.py`
  - `src\base_bot_cli.py`
  - `src\bot\behavior.py`
  - `src\bot\behaviors.py`
  - `src\bot\bot.py`
  - `src\bot\bot_paths.py`
  - `src\bot\merged_instructions.py`
  - `src\bot\reminders.py`
  - `src\bot\trigger_words.py`
  - `src\bot\workspace.py`
  - `src\cli\base_bot_cli.py`
  - `src\cli\behavior_matcher.py`
  - `src\cli\bot_matcher.py`
  - `src\cli\cli_command_router.py`
  - `src\cli\cli_context_builder.py`
  - `src\cli\cli_executor.py`
  - `src\cli\cli_generator.py`
  - `src\cli\cli_help_generator.py`
  - `src\cli\cli_help_renderer.py`
  - `src\cli\cli_parameter_parser.py`
  - `src\cli\cli_parser_generator.py`
  - `src\cli\cli_script_generator.py`
  - `src\cli\cursor_command_generator.py`
  - `src\cli\cursor_help_renderer.py`
  - `src\cli\description_extractor.py`
  - `src\cli\help_context.py`
  - `src\cli\help_renderer.py`
  - `src\cli\parameter_info_builder.py`
  - `src\cli\trigger_domain.py`
  - `src\cli\trigger_router.py`
  - `src\cli\trigger_router_entry.py`
  - `src\cli\unified_help_generator.py`
  - `src\mcp\behavior_tool_generator.py`
  - `src\mcp\bot_tool_generator.py`
  - `src\mcp\mcp_code_generator.py`
  - `src\mcp\mcp_config_generator.py`
  - `src\mcp\mcp_server.py`
  - `src\mcp\mcp_server_generator.py`
  - `src\mcp\mcp_tool_registrar.py`
  - `src\mcp\server_deployer.py`
  - `src\mcp\server_restart.py`
  - `src\scanners\abstraction_levels_scanner.py`
  - `src\scanners\ac_consolidation_scanner.py`
  - `src\scanners\active_language_scanner.py`
  - `src\scanners\arrange_act_assert_scanner.py`
  - `src\scanners\ascii_only_scanner.py`
  - `src\scanners\background_common_setup_scanner.py`
  - `src\scanners\bad_comments_scanner.py`
  - `src\scanners\behavioral_ac_scanner.py`
  - `src\scanners\business_readable_test_names_scanner.py`
  - `src\scanners\calculation_timing_code_scanner.py`
  - `src\scanners\calculation_timing_scanner.py`
  - `src\scanners\class_based_organization_scanner.py`
  - `src\scanners\class_size_scanner.py`
  - `src\scanners\clear_parameters_scanner.py`
  - `src\scanners\code_representation_code_scanner.py`
  - `src\scanners\code_representation_scanner.py`
  - `src\scanners\code_scanner.py`
  - `src\scanners\communication_verb_scanner.py`
  - `src\scanners\complete_refactoring_scanner.py`
  - `src\scanners\complexity_metrics.py`
  - `src\scanners\consistent_indentation_scanner.py`
  - `src\scanners\consistent_naming_scanner.py`
  - `src\scanners\consistent_vocabulary_scanner.py`
  - `src\scanners\cover_all_paths_scanner.py`
  - `src\scanners\delegation_code_scanner.py`
  - `src\scanners\delegation_scanner.py`
  - `src\scanners\dependency_chaining_code_scanner.py`
  - `src\scanners\dependency_chaining_scanner.py`
  - `src\scanners\descriptive_function_names_scanner.py`
  - `src\scanners\domain_concept_node.py`
  - `src\scanners\domain_grouping_code_scanner.py`
  - `src\scanners\domain_grouping_scanner.py`
  - `src\scanners\domain_language_code_scanner.py`
  - `src\scanners\domain_language_scanner.py`
  - `src\scanners\duplication_scanner.py`
  - `src\scanners\encapsulation_scanner.py`
  - `src\scanners\enumerate_ac_permutations_scanner.py`
  - `src\scanners\enumerate_stories_scanner.py`
  - `src\scanners\error_handling_isolation_scanner.py`
  - `src\scanners\exact_variable_names_scanner.py`
  - `src\scanners\exception_classification_scanner.py`
  - `src\scanners\exception_handling_scanner.py`
  - `src\scanners\excessive_guards_scanner.py`
  - `src\scanners\exhaustive_decomposition_scanner.py`
  - `src\scanners\explicit_dependencies_scanner.py`
  - `src\scanners\fixture_placement_scanner.py`
  - `src\scanners\function_size_scanner.py`
  - `src\scanners\generic_capability_scanner.py`
  - `src\scanners\given_precondition_scanner.py`
  - `src\scanners\given_state_not_actions_scanner.py`
  - `src\scanners\given_when_then_helpers_scanner.py`
  - `src\scanners\implementation_details_scanner.py`
  - `src\scanners\import_placement_scanner.py`
  - `src\scanners\increment_folder_structure_scanner.py`
  - `src\scanners\intention_revealing_names_scanner.py`
  - `src\scanners\invest_principles_scanner.py`
  - `src\scanners\meaningful_context_scanner.py`
  - `src\scanners\minimize_mutable_state_scanner.py`
  - `src\scanners\mock_boundaries_scanner.py`
  - `src\scanners\natural_english_code_scanner.py`
  - `src\scanners\natural_english_scanner.py`
  - `src\scanners\no_fallbacks_scanner.py`
  - `src\scanners\no_guard_clauses_scanner.py`
  - `src\scanners\noun_redundancy_scanner.py`
  - `src\scanners\observable_behavior_scanner.py`
  - `src\scanners\one_concept_per_test_scanner.py`
  - `src\scanners\open_closed_principle_scanner.py`
  - `src\scanners\parameterized_tests_scanner.py`
  - `src\scanners\plain_english_scenarios_scanner.py`
  - `src\scanners\prefer_object_model_over_config_scanner.py`
  - `src\scanners\present_ac_consolidation_scanner.py`
  - `src\scanners\primitive_vs_object_scanner.py`
  - `src\scanners\property_encapsulation_code_scanner.py`
  - `src\scanners\property_encapsulation_scanner.py`
  - `src\scanners\real_implementations_scanner.py`
  - `src\scanners\resource_oriented_code_scanner.py`
  - `src\scanners\resource_oriented_design_scanner.py`
  - `src\scanners\resources\block.py`
  - `src\scanners\resources\block_extractor.py`
  - `src\scanners\resources\file.py`
  - `src\scanners\resources\line.py`
  - `src\scanners\resources\scan.py`
  - `src\scanners\resources\scope.py`
  - `src\scanners\resources\violation.py`
  - `src\scanners\scanner.py`
  - `src\scanners\scanner_loader.py`
  - `src\scanners\scanner_orchestrator.py`
  - `src\scanners\scanner_registry.py`
  - `src\scanners\scenario_outline_scanner.py`
  - `src\scanners\scenario_specific_given_scanner.py`
  - `src\scanners\scenarios_cover_all_cases_scanner.py`
  - `src\scanners\scenarios_on_story_docs_scanner.py`
  - `src\scanners\separate_concerns_scanner.py`
  - `src\scanners\simplify_control_flow_scanner.py`
  - `src\scanners\single_responsibility_scanner.py`
  - `src\scanners\specification_match_scanner.py`
  - `src\scanners\specificity_scanner.py`
  - `src\scanners\spine_optional_scanner.py`
  - `src\scanners\story_enumeration_scanner.py`
  - `src\scanners\story_filename_scanner.py`
  - `src\scanners\story_graph_match_scanner.py`
  - `src\scanners\story_map.py`
  - `src\scanners\story_scanner.py`
  - `src\scanners\story_sizing_scanner.py`
  - `src\scanners\swallowed_exceptions_scanner.py`
  - `src\scanners\technical_abstraction_code_scanner.py`
  - `src\scanners\technical_abstraction_scanner.py`
  - `src\scanners\technical_language_scanner.py`
  - `src\scanners\test_boundary_behavior_scanner.py`
  - `src\scanners\test_file_naming_scanner.py`
  - `src\scanners\test_quality_scanner.py`
  - `src\scanners\test_scanner.py`
  - `src\scanners\third_party_isolation_scanner.py`
  - `src\scanners\type_safety_scanner.py`
  - `src\scanners\ubiquitous_language_scanner.py`
  - `src\scanners\unnecessary_parameter_passing_scanner.py`
  - `src\scanners\useless_comments_scanner.py`
  - `src\scanners\verb_noun_scanner.py`
  - `src\scanners\vertical_density_scanner.py`
  - `src\scanners\vertical_slice_scanner.py`
  - `src\scanners\violation.py`
  - `src\story_graph\domain.py`
  - `src\story_graph\nodes.py`
  - `src\utils.py`
  - **Total:** 228 src file(s)

## Scanner Execution Status

### 🟨 Overall Status: NEEDS ATTENTION

| Status | Count | Description |
|--------|-------|-------------|
| 🟩 Executed Successfully | 30 | Scanners ran without errors |
| 🟩 Clean Rules | 27 | No violations found |
| 🟨 Rules with Warnings | 1 | Found 1 warning violation(s) |
| 🟥 Rules with Errors | 2 | Found 1703 error violation(s) |
| [i] No Scanner | 2 | Rule has no scanner configured |

**Total Rules:** 32
- **Rules with Scanners:** 30
  - 🟩 **Executed Successfully:** 30
- [i] **Rules without Scanners:** 2

### 🟩 Successfully Executed Scanners

- 🟥 **[Eliminate Duplication](#eliminate-duplication)** - 1701 violation(s) (EXECUTION_SUCCESS) - [View Details](#eliminate-duplication-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.duplication_scanner.DuplicationScanner`
- 🟥 **[Use Resource Oriented Design](#use-resource-oriented-design)** - 2 violation(s) (EXECUTION_SUCCESS) - [View Details](#use-resource-oriented-design-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.resource_oriented_code_scanner.ResourceOrientedCodeScanner`
- 🟨 **[Use Clear Function Parameters](#use-clear-function-parameters)** - 1 violation(s) (EXECUTION_SUCCESS) - [View Details](#use-clear-function-parameters-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.clear_parameters_scanner.ClearParametersScanner`
- 🟩 **[Avoid Excessive Guards](#avoid-excessive-guards)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.excessive_guards_scanner.ExcessiveGuardsScanner`
- 🟩 **[Avoid Unnecessary Parameter Passing](#avoid-unnecessary-parameter-passing)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.scanners.unnecessary_parameter_passing_scanner.UnnecessaryParameterPassingScanner`
- 🟩 **[Chain Dependencies Properly](#chain-dependencies-properly)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.dependency_chaining_code_scanner.DependencyChainingCodeScanner`
- 🟩 **[Classify Exceptions By Caller Needs](#classify-exceptions-by-caller-needs)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_classification_scanner.ExceptionClassificationScanner`
- 🟩 **[Delegate To Lowest Level](#delegate-to-lowest-level)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.delegation_code_scanner.DelegationCodeScanner`
- 🟩 **[Enforce Encapsulation](#enforce-encapsulation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.encapsulation_scanner.EncapsulationScanner`
- 🟩 **[Favor Code Representation](#favor-code-representation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.code_representation_code_scanner.CodeRepresentationCodeScanner`
- 🟩 **[Group By Domain](#group-by-domain)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_grouping_code_scanner.DomainGroupingCodeScanner`
- 🟩 **[Hide Business Logic Behind Properties](#hide-business-logic-behind-properties)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
- 🟩 **[Hide Calculation Timing](#hide-calculation-timing)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
- 🟩 **[Keep Classes Small With Single Responsibility](#keep-classes-small-with-single-responsibility)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.class_size_scanner.ClassSizeScanner`
- 🟩 **[Keep Functions Single Responsibility](#keep-functions-single-responsibility)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.single_responsibility_scanner.SingleResponsibilityScanner`
- 🟩 **[Keep Functions Small Focused](#keep-functions-small-focused)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.function_size_scanner.FunctionSizeScanner`
- 🟩 **[Maintain Vertical Density](#maintain-vertical-density)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.vertical_density_scanner.VerticalDensityScanner`
- 🟩 **[Never Swallow Exceptions](#never-swallow-exceptions)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.swallowed_exceptions_scanner.SwallowedExceptionsScanner`
- 🟩 **[Place Imports At Top](#place-imports-at-top)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.import_placement_scanner.ImportPlacementScanner`
- 🟩 **[Prefer Object Model Over Config](#prefer-object-model-over-config)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.scanners.prefer_object_model_over_config_scanner.PreferObjectModelOverConfigScanner`
- 🟩 **[Provide Meaningful Context](#provide-meaningful-context)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.meaningful_context_scanner.MeaningfulContextScanner`
- 🟩 **[Refactor Completely Not Partially](#refactor-completely-not-partially)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.complete_refactoring_scanner.CompleteRefactoringScanner`
- 🟩 **[Simplify Control Flow](#simplify-control-flow)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.simplify_control_flow_scanner.SimplifyControlFlowScanner`
- 🟩 **[Stop Writing Useless Comments](#stop-writing-useless-comments)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.scanners.useless_comments_scanner.UselessCommentsScanner`
- 🟩 **[Use Consistent Indentation](#use-consistent-indentation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.consistent_indentation_scanner.ConsistentIndentationScanner`
- 🟩 **[Use Consistent Naming](#use-consistent-naming)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.consistent_naming_scanner.ConsistentNamingScanner`
- 🟩 **[Use Domain Language](#use-domain-language)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_language_code_scanner.DomainLanguageCodeScanner`
- 🟩 **[Use Exceptions Properly](#use-exceptions-properly)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_handling_scanner.ExceptionHandlingScanner`
- 🟩 **[Use Explicit Dependencies](#use-explicit-dependencies)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.explicit_dependencies_scanner.ExplicitDependenciesScanner`
- 🟩 **[Use Natural English](#use-natural-english)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.natural_english_code_scanner.NaturalEnglishCodeScanner`

### <span style="color: gray;">[i] Rules Without Scanners</span>

- <span style="color: gray;">[i]</span> **[Detect Legacy Unused Code](#detect-legacy-unused-code)** - No scanner configured
- <span style="color: gray;">[i]</span> **[Refactor Tests With Production Code](#refactor-tests-with-production-code)** - No scanner configured

## Validation Rules Checked

### 🟥 Rule: <span id="eliminate-duplication">Eliminate Duplication</span> - 1701 ERROR(S) - [View Details](#eliminate-duplication-violations)
**Description:** CRITICAL: Every piece of knowledge should have a single, authoritative representation (DRY principle). Extract repeated logic into reusable functions and use abstraction to capture common patterns.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.duplication_scanner.DuplicationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟥 Rule: <span id="use-resource-oriented-design">Use Resource Oriented Design</span> - 2 ERROR(S) - [View Details](#use-resource-oriented-design-violations)
**Description:** CRITICAL: Code must use resource-oriented, object-oriented design. Use object-oriented classes (singular or collection) with responsibilities that encapsulate logic over manager/doer/loader patterns. Maximize encapsulation through collaborator relationships.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.resource_oriented_code_scanner.ResourceOrientedCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="use-clear-function-parameters">Use Clear Function Parameters</span> - 1 WARNING(S) - [View Details](#use-clear-function-parameters-violations)
**Description:** CRITICAL: Function signatures must be simple and intention-revealing. Prefer 0-2 parameters. NEVER pass Dict[str, Any] or List[str] for complex data - create typed objects instead. Examples: parameters dict → ParametersObject, files dict → FilesCollection, exclude list → ExcludePatterns.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.clear_parameters_scanner.ClearParametersScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="avoid-excessive-guards">Avoid Excessive Guards</span> - CLEAN (0 violations)
**Description:** Excessive guard clauses add to cyclomatic complexity and make code harder to read. Centralize error handling in one place rather than scattering defensive checks throughout the code. Let code fail fast with clear errors rather than silently handling missing components.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.excessive_guards_scanner.ExcessiveGuardsScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="avoid-unnecessary-parameter-passing">Avoid Unnecessary Parameter Passing</span> - CLEAN (0 violations)
**Description:** Don't pass parameters to internal methods when the value is already accessible through instance variables. Access instance properties directly instead of passing them around unnecessarily.
**Scanner:** `agile_bot.bots.base_bot.src.scanners.unnecessary_parameter_passing_scanner.UnnecessaryParameterPassingScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="chain-dependencies-properly">Chain Dependencies Properly</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must chain dependencies properly with constructor injection. Map dependencies in a chain: highest-level object → collaborator → sub-collaborator. Inject collaborators at construction time so methods can use them without passing them as parameters. Access sub-collaborators through their owning objects.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.dependency_chaining_code_scanner.DependencyChainingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="classify-exceptions-by-caller-needs">Classify Exceptions By Caller Needs</span> - CLEAN (0 violations)
**Description:** Design exceptions based on how callers will handle them. Create exception types based on caller's needs, use special case objects for predictable failures, and wrap third-party exceptions at boundaries.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_classification_scanner.ExceptionClassificationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="delegate-to-lowest-level">Delegate To Lowest Level</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must delegate responsibilities to the lowest-level object that can handle them. If a collection class can do something, delegate to it rather than implementing it in the parent.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.delegation_code_scanner.DelegationCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="enforce-encapsulation">Enforce Encapsulation</span> - CLEAN (0 violations)
**Description:** CRITICAL: Hide implementation details and expose minimal interface. Make fields private by default, expose behavior not data. NEVER pass raw dicts/lists that expose internal structure - use typed objects that encapsulate the data. Follow Law of Demeter (principle of least knowledge).
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.encapsulation_scanner.EncapsulationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="favor-code-representation">Favor Code Representation</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code should represent domain concepts directly. Domain models should match code. If code doesn't match domain concepts, refactor the code rather than creating abstract domain models.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.code_representation_code_scanner.CodeRepresentationCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="group-by-domain">Group By Domain</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must be organized by domain area and relationships, not by technical layers, object types, or architectural concerns.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_grouping_code_scanner.DomainGroupingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="hide-business-logic-behind-properties">Hide Business Logic Behind Properties</span> - CLEAN (0 violations)
**Description:** CRITICAL: Hide business logic behind properties. Properties hide logic that occurs—it may be computed on-demand, cached, pre-computed, or loaded from storage. The caller shouldn't know or care when the values are calculated / determined.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="hide-calculation-timing">Hide Calculation Timing</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must hide calculations. Properties hide logic that occurs—it may be computed on-demand, cached, pre-computed, or loaded from storage. The caller shouldn't know or care when the values are calculated / determined.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="keep-classes-small-with-single-responsibility">Keep Classes Small With Single Responsibility</span> - CLEAN (0 violations)
**Description:** CRITICAL: Classes should be small (under 200-300 lines) with a single responsibility. Keep classes cohesive (methods/data interdependent), eliminate dead code, and favor many small focused classes over few large ones.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.class_size_scanner.ClassSizeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="keep-functions-single-responsibility">Keep Functions Single Responsibility</span> - CLEAN (0 violations)
**Description:** CRITICAL: Functions should do one thing and do it well, with no hidden side effects. Each function must have a single, well-defined responsibility.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.single_responsibility_scanner.SingleResponsibilityScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="keep-functions-small-focused">Keep Functions Small Focused</span> - CLEAN (0 violations)
**Description:** Functions should be small enough to understand at a glance. Keep functions under 20 lines when possible and extract complex logic into named helper functions.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.function_size_scanner.FunctionSizeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="maintain-vertical-density">Maintain Vertical Density</span> - CLEAN (0 violations)
**Description:** Related code should be visually close. Group related concepts together, declare variables close to usage, and keep files under 500 lines when possible.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.vertical_density_scanner.VerticalDensityScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="never-swallow-exceptions">Never Swallow Exceptions</span> - CLEAN (0 violations)
**Description:** CRITICAL: Never swallow exceptions silently. Empty catch blocks hide failures and make debugging impossible. Always log, handle, or rethrow exceptions with context.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.swallowed_exceptions_scanner.SwallowedExceptionsScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="place-imports-at-top">Place Imports At Top</span> - CLEAN (0 violations)
**Description:** Place all import statements at the top of the file, after module docstrings and comments, but before any executable code. This improves readability and makes dependencies clear.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.import_placement_scanner.ImportPlacementScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="prefer-object-model-over-config">Prefer Object Model Over Config</span> - CLEAN (0 violations)
**Description:** Use existing object model to access information instead of directly accessing configuration files
**Scanner:** `agile_bot.bots.base_bot.src.scanners.prefer_object_model_over_config_scanner.PreferObjectModelOverConfigScanner`
**Execution Status:** EXECUTION_SUCCESS

*... and 12 more rules*

## Violations Found

**Total Violations:** 1704
- **File-by-File Violations:** 1
- **Cross-File Violations:** 1703

### File-by-File Violations (Pass 1)

These violations were detected by scanning each file individually.

#### <span id="use-clear-function-parameters-violations">Use Clear Function Parameters: 1 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\validate\validation_executor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_executor.py:85): Function "_process_scanner_status" has 6 parameters - consider using existing domain objects with properties instead of passing primitives. Extend domain objects (Behaviors, Behavior, Actions, RenderSpec, etc.) with properties that encapsulate the needed data rather than creating new parameter objects.

### Cross-File Violations (Pass 2)

These violations were detected by analyzing all files together to find patterns that span multiple files.

#### <span id="eliminate-duplication-violations">Eliminate Duplication: 1701 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\action_scope.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/action_scope.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (action_scope.py:_collect_from_story_names (lines 63-68)):
    ```python
    story_names_list = scope_config['story_names']
    if isinstance(story_names_list, list):
        story_names.update(story_names_list)
    elif isinstance(story_names_list, str):
        story_names.add(story_names_list)
    ```

  Location 2 (scenarios_on_story_docs_scanner.py:_get_story_names_from_scope (lines 32-37)):
    ```python
    story_names_list = scope_config['story_names']
    if isinstance(story_names_list, list):
        story_names.update(story_names_list)
    elif isinstance(story_names_list, str):
        story_names.add(story_names_list)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-50)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-51)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-52)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-53)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-54)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-55)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-56)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-57)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 54-58)):
    ```python
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-32)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 55-59)):
    ```python
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions.add('')
    instructions.add('When analyzing code, focus on finding violations and cite...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-50)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-51)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-52)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-53)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-54)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-55)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-56)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-57)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 54-58)):
    ```python
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-33)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 55-59)):
    ```python
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions.add('')
    instructions.add('When analyzing code, focus on finding violations and cite...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-50)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-51)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-52)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-53)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-54)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-55)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-56)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-57)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 54-58)):
    ```python
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 30-34)):
    ```python
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 55-59)):
    ```python
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions.add('')
    instructions.add('When analyzing code, focus on finding violations and cite...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 44-49)):
    ```python
    instructions.add('')
    instructions.add(rules_digest)
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 45-50)):
    ```python
    instructions.add(rules_digest)
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-51)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-52)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instruction...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-53)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    inst...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-54)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-55)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-56)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-57)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-58)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-33)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 54-59)):
    ```python
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 44-49)):
    ```python
    instructions.add('')
    instructions.add(rules_digest)
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 45-50)):
    ```python
    instructions.add(rules_digest)
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-51)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-52)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instruction...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-53)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    inst...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-54)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-55)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-56)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-57)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-58)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 29-34)):
    ```python
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 54-59)):
    ```python
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 44-50)):
    ```python
    instructions.add('')
    instructions.add(rules_digest)
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principle...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 45-51)):
    ```python
    instructions.add(rules_digest)
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-52)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-53)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instruction...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-54)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    inst...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-55)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-56)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-57)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-58)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 28-34)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    instructions.add_display('---')
    instructions.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-59)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:get_command_order (lines 54-59)):
    ```python
    cmd_name = cmd_file.stem
    behavior_name = cmd_name.replace(f'{bot_name}-', '').replace('-', '_')
    if behavior_name in ['', 'continue', 'help', 'get_working_dir', 'set_working_dir'] or cmd_name == bot_name:
        return (0, cmd_name)
    order = self._get_behavior_order(bot_directory, behavior_name)
    return (...
    ```

  Location 2 (cli_help_generator.py:get_command_order (lines 106-111)):
    ```python
    cmd_name = cmd_file.stem
    behavior_name = cmd_name.replace(f'{self.bot_name}-', '').replace('-', '_')
    if behavior_name in ['', 'continue', 'help', 'get_working_dir', 'set_working_dir'] or cmd_name == self.bot_name:
        return (0, cmd_name)
    order = self._get_behavior_order_from_config(behavior_name)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 378-388)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-84)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-234)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 378-388)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-84)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-234)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 378-388)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-84)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-234)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-39)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', ...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 376-388)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-39)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', ...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-84)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-40)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 376-388)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-40)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-84)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\bot_paths.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/bot_paths.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_paths.py:_load_documentation_path (lines 23-29)):
    ```python
    config = read_json_file(bot_config_path)
    docs_path = config.get('docs_path', 'docs/stories')
    return Path(docs_path)
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 128-134)):
    ```python
    behavior_data = read_json_file(behavior_file)
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\bot_matcher.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/bot_matcher.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_matcher.py:_load_patterns_from_file (lines 44-49)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    data = json.loads(content)
    return data.get('patterns', [])
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 128-134)):
    ```python
    behavior_data = read_json_file(behavior_file)
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\bot_matcher.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/bot_matcher.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_matcher.py:_load_patterns_from_file (lines 44-49)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    data = json.loads(content)
    return data.get('patterns', [])
    ```

  Location 2 (server_deployer.py:_load_trigger_words (lines 110-115)):
    ```python
    behavior_data = json.loads(behavior_file.read_text(encoding='utf-8'))
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\bot_matcher.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/bot_matcher.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_matcher.py:_load_patterns_from_file (lines 44-49)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    data = json.loads(content)
    return data.get('patterns', [])
    ```

  Location 2 (file_link_builder.py:get_file_uri (lines 58-65)):
    ```python
    resolved_path = self._resolve_file_path(location)
    file_str = self._normalize_path_string(str(resolved_path))
    return self._build_vscode_uri(file_str, line_number)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_generator.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_generator.py:generate_cli_code (lines 27-31)):
    ```python
    cli_powershell_path = self._script_generator.generate_powershell_script()
    behaviors = self._get_behaviors_from_config()
    cursor_commands = self._command_generator.generate_cursor_commands(cli_python_path, behaviors)
    registry_path = self._command_generator.update_bot_registry(cli_python_path)
    return {...
    ```

  Location 2 (scanner_status_formatter.py:build_executed_rule_info (lines 85-89)):
    ```python
    violations = scanner_status.get('violations_found', 0)
    exec_status = scanner_status.get('execution_status', 'SUCCESS')
    scanner_results = rule_dict.get('scanner_results', {})
    has_errors, has_warnings = self.check_violation_severities(scanner_results)
    return {'rule': rule_file, 'violations': violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_generator.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_generator.py:_discover_behaviors_from_folders (lines 37-45)):
    ```python
    behaviors_dir = self.workspace_root / self.bot_location / 'behaviors'
    if not behaviors_dir.exists():
        return []
    behaviors = []
    for item in sorted(behaviors_dir.iterdir()):
        if item.is_dir() and (not item.name.startswith('_')) and (not item.name.startswith('.')):
            if (item / 'behavior.js...
    ```

  Location 2 (server_deployer.py:_discover_behaviors_from_folders (lines 53-61)):
    ```python
    behaviors_dir = self.bot_directory / 'behaviors'
    if not behaviors_dir.exists():
        return []
    behaviors = []
    for item in sorted(behaviors_dir.iterdir()):
        if item.is_dir() and (not item.name.startswith('_')) and (not item.name.startswith('.')):
            if (item / 'behavior.json').exists():
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-50)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-51)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-52)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-53)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-54)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-55)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-56)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-57)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 54-58)):
    ```python
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 69-73)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 55-59)):
    ```python
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you need. Only read full rule files when you need the remaining 20%.')
    instructions.add('')
    instructions.add('When analyzing code, focus on finding violations and cite...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-71)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-52)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-71)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-53)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instruction...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-71)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-54)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    inst...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-71)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-55)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-71)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-56)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-71)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-57)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-71)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-58)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-71)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-59)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 66-72)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-52)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 66-72)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-53)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instruction...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 66-72)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-54)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    inst...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 66-72)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-55)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 66-72)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-56)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 66-72)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-57)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 66-72)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-58)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 66-72)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 53-59)):
    ```python
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule names when making decisions')
    instructions.add('')
    instructions.add('The digest gives you 80% of what you nee...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-72)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 46-53)):
    ```python
    instructions.add('')
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-72)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 47-54)):
    ```python
    instructions.add('CRITICAL: The rules digest above contains everything you need to get started.')
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instruction...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-72)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 48-55)):
    ```python
    instructions.add('')
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    inst...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-72)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 49-56)):
    ```python
    instructions.add('WORKFLOW:')
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Us...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-72)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 50-57)):
    ```python
    instructions.add('1. Read the rules digest above (descriptions + key principles)')
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-72)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 51-58)):
    ```python
    instructions.add("2. Apply rules to the user's request")
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_generator.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_generator.py:_build_help_instructions (lines 65-72)):
    ```python
    help_inst.add_display('')
    help_inst.add_display('---')
    help_inst.add_display('')
    self._add_all_command_help_to_display(command_files, help_inst)
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    ```

  Location 2 (rules_action.py:_add_rules_context (lines 52-59)):
    ```python
    instructions.add('3. IF you need clarity on a specific rule (examples, edge cases, detailed patterns):')
    instructions.add('   - Use read_file tool to read that specific rule file')
    instructions.add('   - The full rule has detailed examples and detection patterns')
    instructions.add('4. Cite rule name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 378-388)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-84)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 33-41)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-234)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\unified_help_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/unified_help_generator.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (unified_help_generator.py:_render_behavior (lines 44-54)):
    ```python
    behavior_name = behavior.name
    behavior_description = self.description_extractor.get_behavior_description(f'{self.bot_name}-{behavior_name}')
    actions = self._get_behavior_actions(behavior)
    additional_options = self._get_additional_options(behavior_name)
    context = BehaviorHelpContext(bot_name=self.bot...
    ```

  Location 2 (build_action.py:inject_rules (lines 76-82)):
    ```python
    rules_obj = validate_action.rules
    rules_text = rules_obj.formatted_rules()
    rules_data = validate_action.inject_behavior_specific_and_bot_rules()
    all_rules = rules_data.get('validation_rules', [])
    existing_instructions = instructions.get('base_instructions', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\bot_tool_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/bot_tool_generator.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_tool_generator.py:_get_current_behavior (lines 25-30)):
    ```python
    if bot.behaviors.first:
        bot.behaviors.navigate_to(bot.behaviors.first.name)
        current_behavior = bot.behaviors.current
    else:
        raise ValueError('No behaviors available')
    ```

  Location 2 (mcp_tool_registrar.py:register_bot_tool (lines 38-43)):
    ```python
    if self.bot.behaviors.first:
        self.bot.behaviors.navigate_to(self.bot.behaviors.first.name)
        current_behavior = self.bot.behaviors.current
    else:
        raise ValueError('No behaviors available')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_config_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_config_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_config_generator.py:generate_workspace_rules_file (lines 33-37)):
    ```python
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    rules_content = self._generate_rules_content(bot_config, behaviors, behavior_info, behavior_sections...
    ```

  Location 2 (rules_action.py:do_execute (lines 11-16)):
    ```python
    instructions = self.instructions.copy()
    rules = Rules(behavior=self.behavior, bot_paths=self.behavior.bot_paths)
    rules_digest = rules.formatted_rules_digest()
    rule_names = self._get_rule_names(rules)
    self._add_rules_list_to_display(instructions, rule_names)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_server_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_server_generator.py:128): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 128-134)):
    ```python
    behavior_data = read_json_file(behavior_file)
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```

  Location 2 (server_deployer.py:_load_trigger_words (lines 110-115)):
    ```python
    behavior_data = json.loads(behavior_file.read_text(encoding='utf-8'))
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_server_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_server_generator.py:128): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 128-134)):
    ```python
    behavior_data = read_json_file(behavior_file)
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```

  Location 2 (file_link_builder.py:get_file_uri (lines 58-65)):
    ```python
    resolved_path = self._resolve_file_path(location)
    file_str = self._normalize_path_string(str(resolved_path))
    return self._build_vscode_uri(file_str, line_number)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\server_deployer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/server_deployer.py:110): Duplicate code detected across files - extract to shared function.

  Location 1 (server_deployer.py:_load_trigger_words (lines 110-115)):
    ```python
    behavior_data = json.loads(behavior_file.read_text(encoding='utf-8'))
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```

  Location 2 (file_link_builder.py:get_file_uri (lines 58-65)):
    ```python
    resolved_path = self._resolve_file_path(location)
    file_str = self._normalize_path_string(str(resolved_path))
    return self._build_vscode_uri(file_str, line_number)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, content, file_path,...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(node, file_path, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_path, content, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            if violation:
      ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_abstraction_levels(node, content, file_path, rule_obj)
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-44)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-46)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-48)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 46-54)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-66)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 43-52)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 51-60)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 55-64)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 59-68)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 59-70)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 50-58)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 50-58)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 50-58)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 62-70)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-44)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-46)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-54)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-54)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 46-56)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-64)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 46-56)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-68)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 46-56)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-70)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-58)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-58)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-58)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 43-52)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-58)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 51-60)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-58)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 55-64)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-58)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 59-68)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 47-58)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 59-70)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 46-58)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-64)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 46-58)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-68)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 46-58)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-70)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 46-58)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 55-70)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-54)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-54)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-54)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-56)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-56)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 43-58)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-42)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-54)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-54)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-54)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-56)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-40)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-56)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 38-56)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 40-58)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-56)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-40)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-56)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:41): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 41-49)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_outline_scanner.py:scan_story_node (lines 29-37)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:41): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 41-49)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_specific_given_scanner.py:scan_story_node (lines 29-37)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:41): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 41-49)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 43-51)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, content, file_path,...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(node, file_path, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, content, file_path,...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_path, content, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_aaa_structure(node, con...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:156): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:_validate_aaa_structure (lines 156-161)):
    ```python
    test_lines = file_path.read_text(encoding='utf-8').split('\n')
    start_line = test_node.lineno - 1
    end_line = test_node.end_lineno if hasattr(test_node, 'end_lineno') else start_line + 50
    test_body_lines = test_lines[start_line:end_line]
    has_given_comment = any(('# Given' in line or '# Arrange' in lin...
    ```

  Location 2 (given_when_then_helpers_scanner.py:_check_test_method (lines 148-155)):
    ```python
    test_lines = content.split('\n')
    start_line = test_node.lineno - 1
    end_line = test_node.end_lineno if hasattr(test_node, 'end_lineno') else len(test_lines)
    test_body_lines = test_lines[start_line:end_line]
    inline_blocks = self._find_inline_code_blocks(test_node, test_body_lines, helper_functions, tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\background_common_setup_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/background_common_setup_scanner.py:112): Duplicate code detected across files - extract to shared function.

  Location 1 (background_common_setup_scanner.py:_get_scenario_steps (lines 112-118)):
    ```python
    if 'steps' in scenario:
        return scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            return [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (given_precondition_scanner.py:_get_scenario_steps (lines 35-41)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\background_common_setup_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/background_common_setup_scanner.py:112): Duplicate code detected across files - extract to shared function.

  Location 1 (background_common_setup_scanner.py:_get_scenario_steps (lines 112-118)):
    ```python
    if 'steps' in scenario:
        return scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            return [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (scenario_specific_given_scanner.py:_get_scenario_steps (lines 44-50)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 23-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 23-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 33-40)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 23-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 23-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 31-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 23-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 23-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(self._check_misleading_todos(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 21-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.extend(self._check_assertions(tree, content, file_path, rule_obj...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-40)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 23-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(self._check_mislead...
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.extend(self._check...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 21-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(sel...
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.ex...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 36-45)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 27-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 22-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(node, file_path, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_path, content, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
                violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_timing_exposure(node, file_path, rule_obj)
            if violation:
        ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 26-34)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 26-34)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-35)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 26-34)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-37)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 28-36)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 21-35)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 28-36)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 25-37)):
    ```python
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 28-36)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 21-37)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 26-36)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 26-36)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-35)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 26-36)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-37)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:74): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_extract_story_names (lines 74-83)):
    ```python
    sub_epics = epic.get('sub_epics', [])
    for sub_epic in sub_epics:
        story_groups = sub_epic.get('story_groups', [])
        for story_group in story_groups:
            stories = story_group.get('stories', [])
            for story in stories:
                story_name = story.get('name', '')
                if story...
    ```

  Location 2 (story_graph_match_scanner.py:_extract_story_names (lines 40-49)):
    ```python
    sub_epics = epic.get('sub_epics', [])
    for sub_epic in sub_epics:
        story_groups = sub_epic.get('story_groups', [])
        for story_group in story_groups:
            stories = story_group.get('stories', [])
            for story in stories:
                story_name = story.get('name', '')
                if story...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_extract_story_names (lines 76-83)):
    ```python
    story_groups = sub_epic.get('story_groups', [])
    for story_group in story_groups:
        stories = story_group.get('stories', [])
        for story in stories:
            story_name = story.get('name', '')
            if story_name:
                story_names.append(story_name)
    ```

  Location 2 (story_graph_match_scanner.py:_extract_story_names (lines 42-49)):
    ```python
    story_groups = sub_epic.get('story_groups', [])
    for story_group in story_groups:
        stories = story_group.get('stories', [])
        for story in stories:
            story_name = story.get('name', '')
            if story_name:
                story_names.append(story_name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_extract_story_names (lines 78-83)):
    ```python
    stories = story_group.get('stories', [])
    for story in stories:
        story_name = story.get('name', '')
        if story_name:
            story_names.append(story_name)
    ```

  Location 2 (story_graph_match_scanner.py:_extract_story_names (lines 44-49)):
    ```python
    stories = story_group.get('stories', [])
    for story in stories:
        story_name = story.get('name', '')
        if story_name:
            story_names.append(story_name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:156): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_expected_scenario_name (lines 156-164)):
    ```python
    full_method_name = f'test_{method_name}' if not method_name.startswith('test_') else method_name
    method_name_norm = self._normalize_name(method_name)
    story_name_from_class = class_name[4:] if class_name.startswith('Test') else class_name
    story_name_normalized = self._normalize_name(story_name_from_c...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rule_line (lines 77-81)):
    ```python
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self._get_status_indicator(rule_info)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    anchor_link = self._rule_name_to_anchor(rule_name)
    rule_display_name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-84)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-84)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-385)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-234)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:378): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 378-388)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:378): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 378-388)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-84)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:378): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 378-388)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-84)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:378): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 378-388)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-86)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:381): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 381-390)):
    ```python
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 78-86)):
    ```python
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-388)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-388)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-84)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-388)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-84)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-388)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-86)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-388)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-86)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:378): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 378-390)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-84)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:378): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 378-390)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-84)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:378): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 378-390)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-86)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:378): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 378-390)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-86)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-390)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-84)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-390)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 76-86)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 376-390)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 74-86)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:432): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 432-443)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 115-127)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if suggestions_normalized:
        sugges...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:432): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 432-443)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 111-127)):
    ```python
    if matches:
        return None
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if sugges...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:435): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 435-450)):
    ```python
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}.py' for s in suggestions[:5]])
        suggestion_text = f' Suggested names: {suggestion_list}'...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 118-134)):
    ```python
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if suggestions_normalized:
        suggestion_list = ', '.join([f'test_{s}.py' for s in suggestions_normalized[:5]])
        suggestion_t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:432): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 432-450)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 115-134)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if suggestions_normalized:
        sugges...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:466): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_get_sub_epics_spanned_by_test_methods (lines 466-477)):
    ```python
    class_name = node.name
    for item in node.body:
        if isinstance(item, ast.FunctionDef):
            if item.name.startswith('test_'):
                sub_epic = self._find_sub_epic_for_method(item.name, class_name, knowledge_graph)
                if sub_epic:
                    sub_epics.add(self._to_snake_case(s...
    ```

  Location 2 (test_file_naming_scanner.py:_get_sub_epics_spanned_by_test_methods (lines 150-161)):
    ```python
    class_name = node.name
    for item in node.body:
        if isinstance(item, ast.FunctionDef):
            if item.name.startswith('test_'):
                sub_epic = self._find_sub_epic_for_method(item.name, class_name, knowledge_graph)
                if sub_epic:
                    sub_epics.add(self._to_snake_case(s...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:516): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_sub_epic_for_method (lines 516-527)):
    ```python
    scenario_name = scenario.get('name', '')
    if not scenario_name:
        continue
    scenario_name_norm = self._normalize_name(scenario_name)
    if method_name_norm in scenario_name_norm or scenario_name_norm.startswith(method_name_norm) or method_name_norm.startswith(scenario_name_norm):
        return sub_epic_na...
    ```

  Location 2 (test_file_naming_scanner.py:_find_sub_epic_for_method (lines 198-209)):
    ```python
    scenario_name = scenario.get('name', '')
    if not scenario_name:
        continue
    scenario_name_norm = self._to_snake_case(scenario_name)
    if method_name_norm in scenario_name_norm or scenario_name_norm.startswith(method_name_norm) or method_name_norm.startswith(scenario_name_norm):
        return sub_epic_nam...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:539): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_sub_epic_for_method (lines 539-549)):
    ```python
    ac_text = ac.get('text', '') if isinstance(ac, dict) else str(ac)
    if not ac_text:
        continue
    ac_norm = self._normalize_name(ac_text)
    if method_name_norm in ac_norm or ac_norm.startswith(method_name_norm) or method_name_norm.startswith(ac_norm):
        return sub_epic_name
    ```

  Location 2 (test_file_naming_scanner.py:_find_sub_epic_for_method (lines 221-231)):
    ```python
    ac_text = ac.get('text', '') if isinstance(ac, dict) else str(ac)
    if not ac_text:
        continue
    ac_norm = self._to_snake_case(ac_text)
    if method_name_norm in ac_norm or ac_norm.startswith(method_name_norm) or method_name_norm.startswith(ac_norm):
        return sub_epic_name
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(node, file_path, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
                viol...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_path, content, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 23-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_size(node, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 44-54)):
    ```python
    if self._is_test_file(file_path):
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 48-58)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:48): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 48-56)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 52-60)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:48): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 48-56)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 48-60)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(node, file_path, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_path, content, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 52-64)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_parameters(node, file_path, rule_obj, domain_terms)
            if violation:
               ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 44-56)):
    ```python
    if self._is_test_file(file_path):
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 48-60)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:68): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:_is_test_file (lines 68-83)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_'):
        return True
    if file_name == 'conftest.py':
        return True
    return False
    ```

  Location 2 (scanner.py:_is_test_file (lines 152-163)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:186): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_extract_domain_terms (lines 186-192)):
    ```python
    if isinstance(ac, dict):
        ac_text = ac.get('criterion', '')
        if ac_text:
            domain_terms.update(self._extract_words_from_text(ac_text))
    elif isinstance(ac, str):
        domain_terms.update(self._extract_words_from_text(ac))
    ```

  Location 2 (specification_match_scanner.py:_extract_domain_terms (lines 341-347)):
    ```python
    if isinstance(ac, dict):
        ac_text = ac.get('criterion', '')
        if ac_text:
            domain_terms.update(self._extract_words_from_text(ac_text))
    elif isinstance(ac, str):
        domain_terms.update(self._extract_words_from_text(ac))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:292): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_parse_code_file (lines 292-297)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:_parse_test_file (lines 135-140)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:292): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_parse_code_file (lines 292-297)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (test_scanner.py:_parse_test_file (lines 116-121)):
    ```python
    content = test_file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(test_file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:317): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_read_and_parse_file (lines 317-324)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    lines = content.split('\n')
    tree = ast.parse(content, filename=str(file_path))
    return (content, lines, tree)
    ```

  Location 2 (test_scanner.py:_read_and_parse_file (lines 141-148)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    lines = content.split('\n')
    tree = ast.parse(content, filename=str(file_path))
    return (content, lines, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:350): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_get_all_code_files_parsed (lines 350-354)):
    ```python
    parsed = self._parse_code_file(file_path)
    if parsed:
        content, tree = parsed
        parsed_files.append((file_path, content, tree))
    ```

  Location 2 (test_scanner.py:_get_all_test_files_parsed (lines 166-170)):
    ```python
    parsed = self._parse_test_file(test_file_path)
    if parsed:
        content, tree = parsed
        parsed_files.append((test_file_path, content, tree))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-44)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-46)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-48)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 30-38)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 30-38)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 30-38)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 62-70)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-44)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-46)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-48)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 15-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 13-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    test_methods = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 36-45)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 27-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 22-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 29-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 56-66)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 22-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 30-41)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 23-34)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 19-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 27-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 29-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 22-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violati...
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 27-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violati...
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violati...
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violati...
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 23-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 23-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations.extend(class_vi...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 25-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations.extend(class_vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations.extend(class_vi...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violations.extend(class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations.extend(class_vi...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 34-46)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_class(node, file_path, rule_obj, lines, content)
            violations.extend(class_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 23-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 23-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_delegation(node, content, file_path, rule_obj)
            violations...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 23-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 23-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 25-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations.extend(class_vi...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violations.extend(class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 25-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations.extend(class_vi...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 34-46)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_class(node, file_path, rule_obj, lines, content)
            violations.extend(class_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 25-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations.extend(class_vi...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 31-46)):
    ```python
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_class(node, file_path, ru...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 23-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 23-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_dependency_chaining(node, file_path, rule_obj)
            violations...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(node, file_path, r...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_path, content, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if not node.name.startswith('test_'):
                violation = self._check_descriptive_name(no...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
                viol...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_path, content, r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-55)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_class_name(node, file_path, rule_obj)
            if violation:
                violations.appen...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
                viol...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
                viol...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
                viol...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
                viol...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
                viol...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            if node.name.startswith('test_'):
                violation = self._check_one_concept(node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_encapsulation(node, content, file_path, rule_obj)
            if violation:
    ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\enumerate_stories_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/enumerate_stories_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (enumerate_stories_scanner.py:scan_story_node (lines 21-29)):
    ```python
    location = node.map_location('description')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~X stories" notation - all stories must be explicitly enumerated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenarios_on_story_docs_scanner.py:scan_story_node (lines 201-209)):
    ```python
    location = node.map_location()
    violation = Violation(rule=rule_obj, violation_message=f'Story "{node.name}" has no scenarios or scenario_outlines in story-graph.json - scenarios should be in JSON (scenarios or scenario_outlines fields)', location=location, severity='error').to_dict()
    violations.appe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\enumerate_stories_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/enumerate_stories_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (enumerate_stories_scanner.py:scan_story_node (lines 21-29)):
    ```python
    location = node.map_location('description')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~X stories" notation - all stories must be explicitly enumerated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (story_enumeration_scanner.py:scan_story_node (lines 26-34)):
    ```python
    location = node.map_location('estimated_stories')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~{estimated_stories}" notation - all stories must be explicitly enumerated, not estimated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 35-44)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{node.name}" has {len(try_blocks)} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=line_number, severity='warning'...
    ```

  Location 2 (exact_variable_names_scanner.py:_check_variable_names (lines 59-68)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 35-44)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{node.name}" has {len(try_blocks)} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=line_number, severity='warning'...
    ```

  Location 2 (exception_handling_scanner.py:_check_exception_misuse (lines 38-48)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 35-44)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{node.name}" has {len(try_blocks)} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=line_number, severity='warning'...
    ```

  Location 2 (fixture_placement_scanner.py:_check_fixture_imports (lines 34-43)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Fixtures imported from "{node.module}" - fixtures should be defined in test file, not imported', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:35): Duplicate code detected across files - extract to shared function.

Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 35-44)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Function "{node.name}" has {len(try_blocks)} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=line_number, severity='warning'...
```

Location 2 (story_graph_match_scanner.py:_check_test_classes_match_stories (lines 66-75)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Test class [{node.name}](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:35) does not match any story name - test classes must match story names exactly', location=str(file_path), line_number=line_number, severity='error').to_dict()...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 35-44)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{node.name}" has {len(try_blocks)} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=line_number, severity='warning'...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 52-61)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='e...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 35-44)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{node.name}" has {len(try_blocks)} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=line_number, severity='warning'...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 78-87)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 35-44)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{node.name}" has {len(try_blocks)} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=line_number, severity='warning'...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 119-128)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 36-45)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 27-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 22-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 59-68)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (exception_handling_scanner.py:_check_exception_misuse (lines 38-48)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 59-68)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (fixture_placement_scanner.py:_check_fixture_imports (lines 34-43)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Fixtures imported from "{node.module}" - fixtures should be defined in test file, not imported', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:59): Duplicate code detected across files - extract to shared function.

Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 59-68)):
```python
line_number = target.lineno if hasattr(target, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
viol...
```

Location 2 (story_graph_match_scanner.py:_check_test_classes_match_stories (lines 66-75)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Test class [{node.name}](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:59) does not match any story name - test classes must match story names exactly', location=str(file_path), line_number=line_number, severity='error').to_dict()...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 59-68)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 52-61)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='e...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 59-68)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 78-87)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 59-68)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 119-128)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 38-48)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (fixture_placement_scanner.py:_check_fixture_imports (lines 34-43)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Fixtures imported from "{node.module}" - fixtures should be defined in test file, not imported', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:38): Duplicate code detected across files - extract to shared function.

Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 38-48)):
```python
line_number = handler.lineno if hasattr(handler, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
```

Location 2 (story_graph_match_scanner.py:_check_test_classes_match_stories (lines 66-75)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Test class [{node.name}](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:38) does not match any story name - test classes must match story names exactly', location=str(file_path), line_number=line_number, severity='error').to_dict()...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 38-48)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 52-61)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='e...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 38-48)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 78-87)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 38-48)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 119-128)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:133): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:_get_violation_message (lines 133-137)):
    ```python
    violation_messages = rule_obj.rule_content.get('violation_messages', {})
    if message_key in violation_messages:
        template = violation_messages[message_key]
        return template.format(line=line_number, **format_args)
    ```

  Location 2 (type_safety_scanner.py:_get_violation_message (lines 236-240)):
    ```python
    violation_messages = rule_obj.rule_content.get('violation_messages', {})
    if message_key in violation_messages:
        template = violation_messages[message_key]
        return template.format(line=line_number, **format_args)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:34): Duplicate code detected across files - extract to shared function.

Location 1 (fixture_placement_scanner.py:_check_fixture_imports (lines 34-43)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Fixtures imported from "{node.module}" - fixtures should be defined in test file, not imported', location=str(file_path), line_number=line_number, severity='warning').to_dict()
vio...
```

Location 2 (story_graph_match_scanner.py:_check_test_classes_match_stories (lines 66-75)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Test class [{node.name}](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:34) does not match any story name - test classes must match story names exactly', location=str(file_path), line_number=line_number, severity='error').to_dict()...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:_check_fixture_imports (lines 34-43)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Fixtures imported from "{node.module}" - fixtures should be defined in test file, not imported', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    vio...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 52-61)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='e...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:_check_fixture_imports (lines 34-43)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Fixtures imported from "{node.module}" - fixtures should be defined in test file, not imported', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    vio...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 78-87)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:_check_fixture_imports (lines 34-43)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Fixtures imported from "{node.module}" - fixtures should be defined in test file, not imported', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    vio...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 119-128)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-44)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-46)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-48)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 30-38)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-62)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 43-52)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 47-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 51-60)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 55-64)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 59-68)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 59-70)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 62-70)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-44)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-46)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 30-40)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-64)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 30-40)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-68)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 43-52)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 51-60)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 55-64)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 59-68)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 59-70)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-40)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-40)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 30-42)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-70)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-40)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-40)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-40)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-40)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-42)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-52)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-40)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-42)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_precondition_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_precondition_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (given_precondition_scanner.py:scan_story_node (lines 20-28)):
    ```python
    scenario_steps = self._get_scenario_steps(scenario)
    for step_idx, step in enumerate(scenario_steps):
        if step.startswith('Given') or step.startswith('And'):
            violation = self._check_given_is_functionality(step, node, scenario_idx, step_idx, rule_obj)
            if violation:
                viola...
    ```

  Location 2 (given_state_not_actions_scanner.py:scan_story_node (lines 23-31)):
    ```python
    scenario_steps = self._get_scenario_steps(scenario)
    for step_idx, step in enumerate(scenario_steps):
        if step.startswith('Given') or step.startswith('And'):
            violation = self._check_given_is_action(step, node, scenario_idx, step_idx, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_precondition_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_precondition_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (given_precondition_scanner.py:_get_scenario_steps (lines 35-41)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (scenario_specific_given_scanner.py:_get_scenario_steps (lines 44-50)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 36-45)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 27-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 36-45)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 22-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 36-45)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 36-45)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:135): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:_parse_test_file (lines 135-140)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (test_scanner.py:_parse_test_file (lines 116-121)):
    ```python
    content = test_file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(test_file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 27-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 22-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 27-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 27-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 29-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 56-66)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 29-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 22-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 29-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 30-41)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 23-34)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 30-41)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 19-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 27-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 27-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 29-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 22-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 29-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 27-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 27-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\intention_revealing_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/intention_revealing_names_scanner.py:56): Duplicate code detected across files - extract to shared function.

  Location 1 (intention_revealing_names_scanner.py:scan_file (lines 56-66)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 22-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\intention_revealing_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/intention_revealing_names_scanner.py:56): Duplicate code detected across files - extract to shared function.

  Location 1 (intention_revealing_names_scanner.py:scan_file (lines 56-66)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\invest_principles_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/invest_principles_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (invest_principles_scanner.py:scan_story_node (lines 23-30)):
    ```python
    story_data = node.data if hasattr(node, 'data') else {}
    scenarios = story_data.get('scenarios', [])
    scenario_outlines = story_data.get('scenario_outlines', [])
    acceptance_criteria = story_data.get('acceptance_criteria', [])
    has_scenarios = scenarios and len(scenarios) > 0 or (scenario_outlines and l...
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 33-40)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 31-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 34-42)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 32-41)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-40)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 33-42)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 31-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-42)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 21-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 19-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 33-40)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 33-40)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 31-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 33-40)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 33-40)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 34-42)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 32-41)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 34-42)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 34-42)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-40)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-40)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-40)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 33-42)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 31-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 33-42)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 33-42)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-42)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 31-42)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_encapsulation(node, content, file_path, rule_obj)
            violations.extend(class...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 34-46)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            class_violations = self._check_class(node, file_path, rule_obj, lines, content)
            violations.extend(class_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 22-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (story_graph_match_scanner.py:scan_file (lines 20-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 22-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 31-40)):
    ```python
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    violations.extend(method_violations)
    fake_violations = self._check_fake_implementations(lines, file_path, rule_obj)
    violations.extend(fake_violations)
    return violations
    ```

  Location 2 (type_safety_scanner.py:_check_function_type_safety (lines 93-100)):
    ```python
    return_violations = self._check_return_type(func_node, file_path, rule_obj, content)
    violations.extend(return_violations)
    get_violations = self._check_parameters_get_pattern(func_node, file_path, rule_obj, content)
    violations.extend(get_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner.py:152): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner.py:_is_test_file (lines 152-163)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```

  Location 2 (file.py:is_test_file (lines 108-119)):
    ```python
    path_str = str(self._path).lower()
    file_name = self._path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner_loader.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner_loader.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_loader.py:_load_scanner_class (lines 42-52)):
    ```python
    try:
        module = importlib.import_module(path)
        if hasattr(module, class_name):
            scanner_class = getattr(module, class_name)
            if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
                if issubclass(scanner_class, Scanner):
                    return (scanner_cl...
    ```

  Location 2 (scanner_registry.py:loads_scanner_class_with_error (lines 84-94)):
    ```python
    try:
        module = importlib.import_module(path)
        if hasattr(module, class_name):
            scanner_class = getattr(module, class_name)
            if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
                if issubclass(scanner_class, Scanner):
                    return (scanner_cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner_loader.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner_loader.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_loader.py:_load_scanner_class (lines 43-52)):
    ```python
    module = importlib.import_module(path)
    if hasattr(module, class_name):
        scanner_class = getattr(module, class_name)
        if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
            if issubclass(scanner_class, Scanner):
                return (scanner_class, None)
    ```

  Location 2 (scanner_registry.py:loads_scanner_class_with_error (lines 85-94)):
    ```python
    module = importlib.import_module(path)
    if hasattr(module, class_name):
        scanner_class = getattr(module, class_name)
        if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
            if issubclass(scanner_class, Scanner):
                return (scanner_class, None)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenarios_on_story_docs_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenarios_on_story_docs_scanner.py:201): Duplicate code detected across files - extract to shared function.

  Location 1 (scenarios_on_story_docs_scanner.py:scan_story_node (lines 201-209)):
    ```python
    location = node.map_location()
    violation = Violation(rule=rule_obj, violation_message=f'Story "{node.name}" has no scenarios or scenario_outlines in story-graph.json - scenarios should be in JSON (scenarios or scenario_outlines fields)', location=location, severity='error').to_dict()
    violations.appe...
    ```

  Location 2 (story_enumeration_scanner.py:scan_story_node (lines 26-34)):
    ```python
    location = node.map_location('estimated_stories')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~{estimated_stories}" notation - all stories must be explicitly enumerated, not estimated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_outline_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_outline_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_outline_scanner.py:scan_story_node (lines 29-37)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_specific_given_scanner.py:scan_story_node (lines 29-37)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_outline_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_outline_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_outline_scanner.py:scan_story_node (lines 29-37)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 43-51)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_specific_given_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_specific_given_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_specific_given_scanner.py:scan_story_node (lines 29-37)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 43-51)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violation:
                ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_mixed_concerns(node, content, file_path, rule_obj)
            if violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
                violations...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_nesting_depth(node, file_path, rule_obj)
            if violation:
          ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 26-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 28-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
                violations.a...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 26-41)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_function_sr(node, file_path, rule_obj)
            if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 31-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-44)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-46)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-48)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 62-70)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-44)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-46)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-48)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 40-48)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 37-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 35-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (story_graph_match_scanner.py:scan_file (lines 20-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (story_graph_match_scanner.py:scan_file (lines 22-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (story_graph_match_scanner.py:scan_file (lines 23-34)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 19-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (story_graph_match_scanner.py:scan_file (lines 20-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (story_graph_match_scanner.py:scan_file (lines 22-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (story_graph_match_scanner.py:scan_file (lines 20-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    story_names = self._extract_story_names(knowledge_graph)
    violations.extend(self._check_test_classes_match_stories(tree, story_names, file_path, rule_obj))
    return violation...
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 16-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:66): Duplicate code detected across files - extract to shared function.

Location 1 (story_graph_match_scanner.py:_check_test_classes_match_stories (lines 66-75)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Test class [{node.name}](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:66) does not match any story name - test classes must match story names exactly', location=str(file_path), line_number=line_number, severity='error').to_dict()...
```

Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 52-61)):
```python
line_number = handler.lineno if hasattr(handler, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='e...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:66): Duplicate code detected across files - extract to shared function.

Location 1 (story_graph_match_scanner.py:_check_test_classes_match_stories (lines 66-75)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Test class [{node.name}](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:66) does not match any story name - test classes must match story names exactly', location=str(file_path), line_number=line_number, severity='error').to_dict()...
```

Location 2 (test_quality_scanner.py:_check_test_independence (lines 78-87)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
violations.appen...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_graph_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:66): Duplicate code detected across files - extract to shared function.

Location 1 (story_graph_match_scanner.py:_check_test_classes_match_stories (lines 66-75)):
```python
line_number = node.lineno if hasattr(node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Test class [{node.name}](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_graph_match_scanner.py:66) does not match any story name - test classes must match story names exactly', location=str(file_path), line_number=line_number, severity='error').to_dict()...
```

Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 119-128)):
```python
line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_map.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_map.py:275): Duplicate code detected across files - extract to shared function.

  Location 1 (story_map.py:from_bot (lines 275-292)):
    ```python
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise TypeError(f...
    ```

  Location 2 (nodes.py:from_bot (lines 327-340)):
    ```python
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise TypeError(f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 52-61)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='e...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 78-87)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 52-61)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='e...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 119-128)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
                viola...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
            if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.ClassDef):
            violation = self._check_technical_abstraction(node, file_path, rule_obj)
            if violation:
     ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef):
            violation = self._check_variable_declaration_distance(node, content, file_path, rule_obj)
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 20-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_file_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_file_naming_scanner.py:74): Duplicate code detected across files - extract to shared function.

  Location 1 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_file_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_file_naming_scanner.py:74): Duplicate code detected across files - extract to shared function.

  Location 1 (test_file_naming_scanner.py:_to_snake_case (lines 74-82)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-234)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 22-33)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 25-36)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 24-35)):
    ```python
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 27-38)):
    ```python
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 31-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 31-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 32-41)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 32-41)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 22-35)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 25-38)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 31-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 31-41)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:_check_test_independence (lines 78-87)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 119-128)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 15-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 22-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-34)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 22-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 22-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_check_useless_docstrings (lines 50-59)):
    ```python
    line_number = content[:match.start()].count('\n') + 1
    violation = Violation(rule=rule_obj, violation_message=f'Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.append(vio...
    ```

  Location 2 (useless_comments_scanner.py:_check_useless_docstrings (lines 49-58)):
    ```python
    line_number = content[:match.start()].count('\n') + 1
    violation = Violation(rule=rule_obj, violation_message=f'Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.append(vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\clarify\requirements_clarifications.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/clarify/requirements_clarifications.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (requirements_clarifications.py:save (lines 17-21)):
    ```python
    original_questions = self.required_context.key_questions.questions
    original_evidence_requirements = self.required_context.evidence.evidence_list
    new_data = {'key_questions': {'questions': original_questions, 'answers': self.key_questions_answered}, 'evidence': {'required': original_evidence_requirem...
    ```

  Location 2 (strategy_decision.py:save (lines 20-24)):
    ```python
    original_assumptions = self.strategy.assumptions.assumptions
    original_recommended_activities = self.strategy.recommended_activities.recommended_activities
    new_data = {'strategy_criteria': {'criteria': original_strategy_criteria, 'decisions_made': self.decisions_made}, 'assumptions': {'typical_assump...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```

  Location 2 (scanner_status_formatter.py:format_executed_rule_line (lines 117-121)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 229-233)):
    ```python
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule_filter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule_filter.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (rule_filter.py:_is_file_in_folder (lines 80-87)):
    ```python
    folder_path = Path(folder_pattern)
    if not (folder_path.is_absolute() or file_path.is_absolute()):
        return False
    return self._check_path_relative(file_path, folder_path)
    ```

  Location 2 (file_discovery.py:_is_file_in_folder (lines 60-66)):
    ```python
    folder_path = Path(folder_pattern)
    if not (folder_path.is_absolute() or file_path.is_absolute()):
        return False
    return self._check_path_relative(file_path, folder_path)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\file_link_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/file_link_builder.py:92): Duplicate code detected across files - extract to shared function.

  Location 1 (file_link_builder.py:get_relative_path (lines 92-100)):
    ```python
    if file_path.is_absolute() and self.workspace_directory:
        return str(file_path.relative_to(self.workspace_directory))
    if self.workspace_directory and (not file_path.is_absolute()):
        return self.get_relative_path_for_relative_file(file_path)
    return file_path.name
    ```

  Location 2 (validation_report_formatter.py:get_relative_path (lines 159-167)):
    ```python
    if file_path.is_absolute() and self.workspace_directory:
        return str(file_path.relative_to(self.workspace_directory))
    if self.workspace_directory and (not file_path.is_absolute()):
        return self._get_relative_path_for_relative_file(file_path)
    return file_path.name
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-23)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-24)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-24)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-25)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 268-272)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-25)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 264-268)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 265-269)):
    ```python
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanne...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 267-271)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 268-272)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 23-27)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 269-273)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 23-27)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 23-27)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 23-27)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-27)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-24)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-24)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-25)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 264-269)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 266-271)):
    ```python
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 267-272)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 22-27)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 268-273)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 22-27)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 23-27)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 22-27)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-27)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-25)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-25)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-25)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-25)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-27)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-26)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-26)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-26)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-26)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-27)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-27)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 267-273)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-27)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-27)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 21-27)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-27)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-27)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-26)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-25)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-26)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-26)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-27)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-27)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-27)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-27)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-27)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-27)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-27)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-27)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-27)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-27)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-27)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 103-109)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-72)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 103-109)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-73)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:105): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 105-110)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 68-73)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:105): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 105-110)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-73)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 103-110)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-72)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 103-110)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 68-73)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 103-110)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-73)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:116): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 116-120)):
    ```python
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(in...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 117-121)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 229-233)):
    ```python
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 117-121)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-234)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:118): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 118-122)):
    ```python
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, violations)
    status_suffix = f'({exec_status})' if exec_status != 'SUCCESS' else f'({s...
    ```

  Location 2 (validation_report_formatter.py:format_violation_line (lines 14-18)):
    ```python
    location = violation.get('location', 'unknown')
    message = violation.get('violation_message', 'No message')
    severity = violation.get('severity', 'error')
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:118): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 118-122)):
    ```python
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, violations)
    status_suffix = f'({exec_status})' if exec_status != 'SUCCESS' else f'({s...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 231-235)):
    ```python
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    violations_link = f' - [View Details](#{anchor_id}-violations)' if info.get('violations', 0) > 0 else ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:114): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 114-119)):
    ```python
    violations = rule_info['violations']
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = r...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rule_line (lines 76-80)):
    ```python
    violations = rule_info['violations']
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self._get_status_indicator(rule_info)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    anchor_link = self._rule_name_t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:116): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 116-121)):
    ```python
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-233)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(in...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 117-122)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-235)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:144): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_failed_rules_section (lines 144-155)):
    ```python
    if not failed_rules:
        return []
    lines = [f'### 🟥 {title}', '']
    for rule_info in failed_rules:
        rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
        anchor_link = self.rule_name_to_anchor(rule_name)
        rule_display_name = rule_name.replace('_', ' ').title()
        line...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_failed_rules (lines 97-108)):
    ```python
    if not failed_rules:
        return []
    lines = [f'### 🟥 {title}', '']
    for rule_info in failed_rules:
        rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
        anchor_link = self._rule_name_to_anchor(rule_name)
        rule_display_name = rule_name.replace('_', ' ').title()
        lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:194): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:_get_violation_status (lines 194-201)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```

  Location 2 (validation_scanner_status_builder.py:_get_overall_status (lines 145-157)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:194): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:_get_violation_status (lines 194-201)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```

  Location 2 (validation_scanner_status_builder.py:_get_overall_status (lines 140-157)):
    ```python
    if not stats.has_violations:
        return ('🟩', 'ALL CLEAN')
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ER...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:248): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_totals_summary (lines 248-255)):
    ```python
    lines = [f'**Total Rules:** {stats.total_rules}', f'- **Rules with Scanners:** {stats.total_with_scanners}', f'  - 🟩 **Executed Successfully:** {stats.executed_count}']
    if stats.load_failed_count > 0:
        lines.append(f'  - 🟥 **Load Failed:** {stats.load_failed_count}')
    if stats.execution_failed_cou...
    ```

  Location 2 (validation_scanner_status_builder.py:_build_totals_summary (lines 182-189)):
    ```python
    lines = [f'**Total Rules:** {stats.total_rules}', f'- **Rules with Scanners:** {stats.total_with_scanners}', f'  - 🟩 **Executed Successfully:** {stats.executed_count}']
    if stats.load_failed_count > 0:
        lines.append(f'  - 🟥 **Load Failed:** {stats.load_failed_count}')
    if stats.execution_failed_cou...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_executor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_executor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_executor.py:_collect_scanner_status (lines 78-82)):
    ```python
    scanner_status = rule_dict.get('scanner_status', {})
    status = scanner_status.get('status', 'UNKNOWN')
    rule_file = rule_dict.get('rule_file', 'unknown')
    self._process_scanner_status(status, counts, scanner_status_lines, rule_file, scanner_status)
    ```

  Location 2 (validation_scanner_status_builder.py:_categorize_rules (lines 31-35)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    scanner_status = rule_dict.get('scanner_status', {})
    status = scanner_status.get('status', 'UNKNOWN')
    self._categorize_rule_by_status(status, rule_dict, rule_file, scanner_status, result)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-23)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-306)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-25)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-308)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-26)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-309)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-27)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-310)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-311)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-312)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-312)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-312)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-313)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 26-30)):
    ```python
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_line}']
    lines.extend(self._format_multiline_message_parts(parts[1:]))
    return lines
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-313)):
    ```python
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_line}']
    lines.extend(self.violation_formatter.format_multiline_message_parts(parts[1:]))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-25)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-308)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-26)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-309)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-27)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-310)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-28)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-311)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-28)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-312)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-311)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-312)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-312)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-312)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-313)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-26)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-309)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-26)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-310)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-27)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-310)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-27)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-311)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-311)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-311)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-312)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-313)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-311)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-312)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-312)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-313)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-311)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-312)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-313)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-26)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-309)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-309)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-310)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-311)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-310)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-311)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-313)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-311)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-312)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-313)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-311)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-312)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 304-313)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-313)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-27)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-310)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-310)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-311)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-312)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-311)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-312)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-313)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-311)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-312)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 303-313)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-313)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-28)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-311)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-28)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-312)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-310)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-311)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 301-312)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-311)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-312)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 302-313)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:_format_multiline_message_parts (lines 35-39)):
    ```python
    formatted = self._format_message_part(part, in_code_block)
    if part.strip().startswith('```'):
        in_code_block = not in_code_block
    result.append(formatted)
    ```

  Location 2 (violation_formatter.py:format_multiline_message_parts (lines 83-87)):
    ```python
    formatted = self._format_message_part(part, in_code_block)
    if part.strip().startswith('```'):
        in_code_block = not in_code_block
    result.append(formatted)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 230-234)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:264): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 264-268)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:265): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 265-269)):
    ```python
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanne...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:267): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 267-271)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:268): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 268-272)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:269): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 269-273)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 23-27)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:269): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 269-273)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:build_status_summary (lines 126-130)):
    ```python
    lines.append(f'### {overall_status} Overall Status: {overall_text}')
    lines.append('')
    lines.extend(self._build_summary_table(stats))
    lines.extend(self._build_totals_summary(stats))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:264): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 264-269)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:266): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 266-271)):
    ```python
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(sel...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:267): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 267-272)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:268): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 268-273)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-27)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:267): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 267-273)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-27)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 10-14)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule....
    ```

  Location 2 (violation_formatter.py:build_violations (lines 18-22)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 11-26)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_...
    ```

  Location 2 (violation_formatter.py:build_violations (lines 19-34)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 11-26)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_...
    ```

  Location 2 (violation_formatter.py:build_violations (lines 19-35)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 12-27)):
    ```python
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_file_by_file + total_cross_file
    if total_violations == 0:
        lines.append('🟢 **No violations found.** All ru...
    ```

  Location 2 (violation_formatter.py:build_violations (lines 20-35)):
    ```python
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_file_by_file + total_cross_file
    if total_violations == 0:
        lines.append('🟩 **No violations found.** All ru...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 10-26)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule....
    ```

  Location 2 (violation_formatter.py:build_violations (lines 18-34)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 10-26)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule....
    ```

  Location 2 (violation_formatter.py:build_violations (lines 18-35)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 11-27)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_...
    ```

  Location 2 (violation_formatter.py:build_violations (lines 19-34)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 11-27)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_...
    ```

  Location 2 (violation_formatter.py:build_violations (lines 19-35)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 11-27)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_...
    ```

  Location 2 (violation_formatter.py:build_violations (lines 18-35)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 10-27)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule....
    ```

  Location 2 (violation_formatter.py:build_violations (lines 18-34)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 10-27)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule....
    ```

  Location 2 (violation_formatter.py:build_violations (lines 19-35)):
    ```python
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.values()))
    total_violations = total_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_violations_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_violations_builder.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_violations_builder.py:build_violations (lines 10-27)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self._organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule....
    ```

  Location 2 (violation_formatter.py:build_violations (lines 18-35)):
    ```python
    lines = ['## Violations Found', '']
    file_by_file_violations_by_rule, cross_file_violations_by_rule = self.organize_violations(validation_rules)
    total_file_by_file = sum((len(v) for v in file_by_file_violations_by_rule.values()))
    total_cross_file = sum((len(v) for v in cross_file_violations_by_rule.v...
    ```

#### <span id="use-resource-oriented-design-violations">Use Resource Oriented Design: 2 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\background_validation_handler.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/background_validation_handler.py:12): Class "BackgroundValidationHandler" uses manager/doer/loader pattern but is not owned by a domain object. Use resource-oriented design instead (e.g., make it a property of a domain object like "BackgroundValidation").
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_executor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_executor.py:13): Class "ValidationExecutor" uses manager/doer/loader pattern but is not owned by a domain object. Use resource-oriented design instead (e.g., make it a property of a domain object like "Validation").

## Validation Instructions

The following validation steps were performed:

1. **MANDATORY: Before validating any content, you MUST load and review the project's context files:**
2. 1. Load `{project_area}/docs/stories/clarification.json` - Contains key questions and evidence (generated file)
3. 2. Load `{project_area}/docs/stories/planning.json` - Contains assumptions and decisions (generated file)
4. 3. Load `{project_area}/docs/context/input.txt` (or similar) - Original input/source material if needed for validation (original context)
5. 
6. **CRITICAL: File locations:**
7. - **Generated files:** `{project_area}/docs/stories/clarification.json`, `{project_area}/docs/stories/planning.json`
8. - **Original input:** `{project_area}/docs/context/input.txt` and other original context files
9. 
10. These files contain critical requirements, decisions, and context that MUST be checked against during validation.
*... and 249 more instructions*

## Report Location

This report was automatically generated and saved to:
`C:\dev\augmented-teams\agile_bot\bots\base_bot\docs\stories\reports\code-validation-report-2025-12-21_16-42-15.md`

