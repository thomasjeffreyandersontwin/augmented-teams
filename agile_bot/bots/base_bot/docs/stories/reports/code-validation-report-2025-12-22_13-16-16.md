# Validation Report - Code

**Generated:** 2025-12-22 13:58:52
**Project:** base_bot
**Behavior:** code
**Action:** validate

## Summary

Validated story map and domain model and 242 code file(s) against **32 validation rules**.

## Content Validated

- **Clarification:** `clarification.json`
- **Rendered Outputs:**
  - `solution-domain-model-description.md`
  - `story-graph.json`
  - `story-map-increments.md`
- **Code Files Scanned:**
  - `src\actions\action.py`
  - `src\actions\action_context.py`
  - `src\actions\action_factory.py`
  - `src\actions\action_scope.py`
  - `src\actions\action_state_manager.py`
  - `src\actions\actions.py`
  - `src\actions\activity_tracker.py`
  - `src\actions\build\build_action.py`
  - `src\actions\build\build_scope.py`
  - `src\actions\build\knowledge.py`
  - `src\actions\build\knowledge_graph_spec.py`
  - `src\actions\build\knowledge_graph_template.py`
  - `src\actions\clarify\clarify_action.py`
  - `src\actions\clarify\evidence.py`
  - `src\actions\clarify\key_questions.py`
  - `src\actions\clarify\recommended_activities.py`
  - `src\actions\clarify\required_context.py`
  - `src\actions\clarify\requirements_clarifications.py`
  - `src\actions\content.py`
  - `src\actions\context_data_injector.py`
  - `src\actions\display\display_section.py`
  - `src\actions\display\list_items.py`
  - `src\actions\display\markdown_formatter.py`
  - `src\actions\guardrails.py`
  - `src\actions\help_action.py`
  - `src\actions\instructions.py`
  - `src\actions\render\evidence.py`
  - `src\actions\render\render_action.py`
  - `src\actions\render\render_config_loader.py`
  - `src\actions\render\render_instruction_formatter.py`
  - `src\actions\render\render_scope.py`
  - `src\actions\render\render_spec.py`
  - `src\actions\render\synchronizer.py`
  - `src\actions\render\template.py`
  - `src\actions\rules\rule.py`
  - `src\actions\rules\rule_filter.py`
  - `src\actions\rules\rule_loader.py`
  - `src\actions\rules\rules.py`
  - `src\actions\rules\rules_action.py`
  - `src\actions\rules\rules_digest_guidance.py`
  - `src\actions\scoping_parameter.py`
  - `src\actions\strategy\assumptions.py`
  - `src\actions\strategy\json_persistent.py`
  - `src\actions\strategy\recommended_activities.py`
  - `src\actions\strategy\strategy.py`
  - `src\actions\strategy\strategy_action.py`
  - `src\actions\strategy\strategy_criteria.py`
  - `src\actions\strategy\strategy_criterias.py`
  - `src\actions\strategy\strategy_decision.py`
  - `src\actions\validate\background_validation_handler.py`
  - `src\actions\validate\file_discovery.py`
  - `src\actions\validate\file_link_builder.py`
  - `src\actions\validate\knowledge_graph.py`
  - `src\actions\validate\path_resolver.py`
  - `src\actions\validate\scanner_status_formatter.py`
  - `src\actions\validate\scanners\useless_comments_scanner.py`
  - `src\actions\validate\story_graph.py`
  - `src\actions\validate\validate_action.py`
  - `src\actions\validate\validation_executor.py`
  - `src\actions\validate\validation_report_builder.py`
  - `src\actions\validate\validation_report_formatter.py`
  - `src\actions\validate\validation_report_writer.py`
  - `src\actions\validate\validation_scanner_status_builder.py`
  - `src\actions\validate\validation_scope.py`
  - `src\actions\validate\validation_stats.py`
  - `src\actions\validate\validation_violations_builder.py`
  - `src\actions\validate\violation_formatter.py`
  - `src\actions\workflow_status_builder.py`
  - `src\base_bot_cli.py`
  - `src\bot\behavior.py`
  - `src\bot\behaviors.py`
  - `src\bot\bot.py`
  - `src\bot\bot_paths.py`
  - `src\bot\merged_instructions.py`
  - `src\bot\reminders.py`
  - `src\bot\trigger_words.py`
  - `src\bot\workspace.py`
  - `src\cli\action_data_collector.py`
  - `src\cli\base_bot_cli.py`
  - `src\cli\behavior_matcher.py`
  - `src\cli\bot_matcher.py`
  - `src\cli\cli_code_visitor.py`
  - `src\cli\cli_command_router.py`
  - `src\cli\cli_context_builder.py`
  - `src\cli\cli_executor.py`
  - `src\cli\cli_generator.py`
  - `src\cli\cli_help_generator.py`
  - `src\cli\cli_help_renderer.py`
  - `src\cli\cli_parameter_parser.py`
  - `src\cli\cli_parser_generator.py`
  - `src\cli\cli_script_generator.py`
  - `src\cli\cli_visitor.py`
  - `src\cli\command_renderer.py`
  - `src\cli\cursor_command_generator.py`
  - `src\cli\cursor_help_renderer.py`
  - `src\cli\description_extractor.py`
  - `src\cli\help_context.py`
  - `src\cli\help_renderer.py`
  - `src\cli\mcp_code_visitor.py`
  - `src\cli\parameter_info_builder.py`
  - `src\cli\trigger_domain.py`
  - `src\cli\trigger_router.py`
  - `src\cli\trigger_router_entry.py`
  - `src\cli\type_hint_converter.py`
  - `src\cli\unified_help_generator.py`
  - `src\generator\orchestrator.py`
  - `src\generator\visitor.py`
  - `src\mcp\behavior_tool_generator.py`
  - `src\mcp\bot_tool_generator.py`
  - `src\mcp\mcp_code_generator.py`
  - `src\mcp\mcp_config_generator.py`
  - `src\mcp\mcp_server.py`
  - `src\mcp\mcp_server_generator.py`
  - `src\mcp\mcp_tool_registrar.py`
  - `src\mcp\server_deployer.py`
  - `src\mcp\server_restart.py`
  - `src\scanners\abstraction_levels_scanner.py`
  - `src\scanners\ac_consolidation_scanner.py`
  - `src\scanners\active_language_scanner.py`
  - `src\scanners\arrange_act_assert_scanner.py`
  - `src\scanners\ascii_only_scanner.py`
  - `src\scanners\background_common_setup_scanner.py`
  - `src\scanners\bad_comments_scanner.py`
  - `src\scanners\behavioral_ac_scanner.py`
  - `src\scanners\business_readable_test_names_scanner.py`
  - `src\scanners\calculation_timing_code_scanner.py`
  - `src\scanners\calculation_timing_scanner.py`
  - `src\scanners\class_based_organization_scanner.py`
  - `src\scanners\class_size_scanner.py`
  - `src\scanners\clear_parameters_scanner.py`
  - `src\scanners\code_representation_code_scanner.py`
  - `src\scanners\code_representation_scanner.py`
  - `src\scanners\code_scanner.py`
  - `src\scanners\communication_verb_scanner.py`
  - `src\scanners\complete_refactoring_scanner.py`
  - `src\scanners\complexity_metrics.py`
  - `src\scanners\consistent_indentation_scanner.py`
  - `src\scanners\consistent_naming_scanner.py`
  - `src\scanners\consistent_vocabulary_scanner.py`
  - `src\scanners\cover_all_paths_scanner.py`
  - `src\scanners\delegation_code_scanner.py`
  - `src\scanners\delegation_scanner.py`
  - `src\scanners\dependency_chaining_code_scanner.py`
  - `src\scanners\dependency_chaining_scanner.py`
  - `src\scanners\descriptive_function_names_scanner.py`
  - `src\scanners\domain_concept_node.py`
  - `src\scanners\domain_grouping_code_scanner.py`
  - `src\scanners\domain_grouping_scanner.py`
  - `src\scanners\domain_language_code_scanner.py`
  - `src\scanners\domain_language_scanner.py`
  - `src\scanners\domain_scanner.py`
  - `src\scanners\duplication_scanner.py`
  - `src\scanners\encapsulation_scanner.py`
  - `src\scanners\enumerate_ac_permutations_scanner.py`
  - `src\scanners\enumerate_stories_scanner.py`
  - `src\scanners\error_handling_isolation_scanner.py`
  - `src\scanners\exact_variable_names_scanner.py`
  - `src\scanners\exception_classification_scanner.py`
  - `src\scanners\exception_handling_scanner.py`
  - `src\scanners\excessive_guards_scanner.py`
  - `src\scanners\exhaustive_decomposition_scanner.py`
  - `src\scanners\explicit_dependencies_scanner.py`
  - `src\scanners\fixture_placement_scanner.py`
  - `src\scanners\function_size_scanner.py`
  - `src\scanners\generic_capability_scanner.py`
  - `src\scanners\given_precondition_scanner.py`
  - `src\scanners\given_state_not_actions_scanner.py`
  - `src\scanners\given_when_then_helpers_scanner.py`
  - `src\scanners\implementation_details_scanner.py`
  - `src\scanners\import_placement_scanner.py`
  - `src\scanners\increment_folder_structure_scanner.py`
  - `src\scanners\intention_revealing_names_scanner.py`
  - `src\scanners\invest_principles_scanner.py`
  - `src\scanners\meaningful_context_scanner.py`
  - `src\scanners\minimize_mutable_state_scanner.py`
  - `src\scanners\mock_boundaries_scanner.py`
  - `src\scanners\natural_english_code_scanner.py`
  - `src\scanners\natural_english_scanner.py`
  - `src\scanners\no_fallbacks_scanner.py`
  - `src\scanners\no_guard_clauses_scanner.py`
  - `src\scanners\noun_redundancy_scanner.py`
  - `src\scanners\observable_behavior_scanner.py`
  - `src\scanners\one_concept_per_test_scanner.py`
  - `src\scanners\open_closed_principle_scanner.py`
  - `src\scanners\parameterized_tests_scanner.py`
  - `src\scanners\plain_english_scenarios_scanner.py`
  - `src\scanners\prefer_object_model_over_config_scanner.py`
  - `src\scanners\present_ac_consolidation_scanner.py`
  - `src\scanners\primitive_vs_object_scanner.py`
  - `src\scanners\property_encapsulation_code_scanner.py`
  - `src\scanners\property_encapsulation_scanner.py`
  - `src\scanners\real_implementations_scanner.py`
  - `src\scanners\resource_oriented_code_scanner.py`
  - `src\scanners\resource_oriented_design_scanner.py`
  - `src\scanners\resources\ast_elements.py`
  - `src\scanners\resources\block.py`
  - `src\scanners\resources\block_extractor.py`
  - `src\scanners\resources\file.py`
  - `src\scanners\resources\line.py`
  - `src\scanners\resources\scan.py`
  - `src\scanners\resources\scope.py`
  - `src\scanners\resources\violation.py`
  - `src\scanners\scanner.py`
  - `src\scanners\scanner_loader.py`
  - `src\scanners\scanner_orchestrator.py`
  - `src\scanners\scanner_registry.py`
  - `src\scanners\scenario_outline_scanner.py`
  - `src\scanners\scenario_specific_given_scanner.py`
  - `src\scanners\scenarios_cover_all_cases_scanner.py`
  - `src\scanners\scenarios_on_story_docs_scanner.py`
  - `src\scanners\separate_concerns_scanner.py`
  - `src\scanners\simplify_control_flow_scanner.py`
  - `src\scanners\single_responsibility_scanner.py`
  - `src\scanners\specification_match_scanner.py`
  - `src\scanners\specificity_scanner.py`
  - `src\scanners\spine_optional_scanner.py`
  - `src\scanners\story_enumeration_scanner.py`
  - `src\scanners\story_filename_scanner.py`
  - `src\scanners\story_graph_match_scanner.py`
  - `src\scanners\story_map.py`
  - `src\scanners\story_scanner.py`
  - `src\scanners\story_sizing_scanner.py`
  - `src\scanners\swallowed_exceptions_scanner.py`
  - `src\scanners\technical_abstraction_code_scanner.py`
  - `src\scanners\technical_abstraction_scanner.py`
  - `src\scanners\technical_language_scanner.py`
  - `src\scanners\test_boundary_behavior_scanner.py`
  - `src\scanners\test_file_naming_scanner.py`
  - `src\scanners\test_quality_scanner.py`
  - `src\scanners\test_scanner.py`
  - `src\scanners\third_party_isolation_scanner.py`
  - `src\scanners\type_safety_scanner.py`
  - `src\scanners\ubiquitous_language_scanner.py`
  - `src\scanners\unnecessary_parameter_passing_scanner.py`
  - `src\scanners\useless_comments_scanner.py`
  - `src\scanners\verb_noun_scanner.py`
  - `src\scanners\vertical_density_scanner.py`
  - `src\scanners\vertical_slice_scanner.py`
  - `src\scanners\violation.py`
  - `src\story_graph\domain.py`
  - `src\story_graph\nodes.py`
  - `src\utils.py`
  - **Total:** 242 src file(s)

## Scanner Execution Status

### 🟨 Overall Status: NEEDS ATTENTION

| Status | Count | Description |
|--------|-------|-------------|
| 🟩 Executed Successfully | 30 | Scanners ran without errors |
| 🟩 Clean Rules | 25 | No violations found |
| 🟨 Rules with Warnings | 2 | Found 5 warning violation(s) |
| 🟥 Rules with Errors | 2 | Found 2080 error violation(s) |
| [i] No Scanner | 2 | Rule has no scanner configured |

**Total Rules:** 32
- **Rules with Scanners:** 30
  - 🟩 **Executed Successfully:** 30
- [i] **Rules without Scanners:** 2

### 🟩 Successfully Executed Scanners

- 🟥 **[Eliminate Duplication](#eliminate-duplication)** - 2078 violation(s) (EXECUTION_SUCCESS) - [View Details](#eliminate-duplication-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.duplication_scanner.DuplicationScanner`
- 🟨 **[Chain Dependencies Properly](#chain-dependencies-properly)** - 4 violation(s) (EXECUTION_SUCCESS) - [View Details](#chain-dependencies-properly-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.dependency_chaining_code_scanner.DependencyChainingCodeScanner`
- 🟥 **[Stop Writing Useless Comments](#stop-writing-useless-comments)** - 2 violation(s) (EXECUTION_SUCCESS) - [View Details](#stop-writing-useless-comments-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.scanners.useless_comments_scanner.UselessCommentsScanner`
- 🟨 **[Delegate To Lowest Level](#delegate-to-lowest-level)** - 1 violation(s) (EXECUTION_SUCCESS) - [View Details](#delegate-to-lowest-level-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.delegation_code_scanner.DelegationCodeScanner`
- 🟨 **[Simplify Control Flow](#simplify-control-flow)** - 1 violation(s) (EXECUTION_SUCCESS) - [View Details](#simplify-control-flow-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.simplify_control_flow_scanner.SimplifyControlFlowScanner`
- 🟩 **[Avoid Excessive Guards](#avoid-excessive-guards)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.excessive_guards_scanner.ExcessiveGuardsScanner`
- 🟩 **[Avoid Unnecessary Parameter Passing](#avoid-unnecessary-parameter-passing)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.scanners.unnecessary_parameter_passing_scanner.UnnecessaryParameterPassingScanner`
- 🟩 **[Classify Exceptions By Caller Needs](#classify-exceptions-by-caller-needs)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_classification_scanner.ExceptionClassificationScanner`
- 🟩 **[Enforce Encapsulation](#enforce-encapsulation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.encapsulation_scanner.EncapsulationScanner`
- 🟩 **[Favor Code Representation](#favor-code-representation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.code_representation_code_scanner.CodeRepresentationCodeScanner`
- 🟩 **[Group By Domain](#group-by-domain)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_grouping_code_scanner.DomainGroupingCodeScanner`
- 🟩 **[Hide Business Logic Behind Properties](#hide-business-logic-behind-properties)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
- 🟩 **[Hide Calculation Timing](#hide-calculation-timing)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
- 🟩 **[Keep Classes Small With Single Responsibility](#keep-classes-small-with-single-responsibility)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.class_size_scanner.ClassSizeScanner`
- 🟩 **[Keep Functions Single Responsibility](#keep-functions-single-responsibility)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.single_responsibility_scanner.SingleResponsibilityScanner`
- 🟩 **[Keep Functions Small Focused](#keep-functions-small-focused)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.function_size_scanner.FunctionSizeScanner`
- 🟩 **[Maintain Vertical Density](#maintain-vertical-density)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.vertical_density_scanner.VerticalDensityScanner`
- 🟩 **[Never Swallow Exceptions](#never-swallow-exceptions)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.swallowed_exceptions_scanner.SwallowedExceptionsScanner`
- 🟩 **[Place Imports At Top](#place-imports-at-top)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.import_placement_scanner.ImportPlacementScanner`
- 🟩 **[Prefer Object Model Over Config](#prefer-object-model-over-config)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.scanners.prefer_object_model_over_config_scanner.PreferObjectModelOverConfigScanner`
- 🟩 **[Provide Meaningful Context](#provide-meaningful-context)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.meaningful_context_scanner.MeaningfulContextScanner`
- 🟩 **[Refactor Completely Not Partially](#refactor-completely-not-partially)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.complete_refactoring_scanner.CompleteRefactoringScanner`
- 🟩 **[Use Clear Function Parameters](#use-clear-function-parameters)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.clear_parameters_scanner.ClearParametersScanner`
- 🟩 **[Use Consistent Indentation](#use-consistent-indentation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.consistent_indentation_scanner.ConsistentIndentationScanner`
- 🟩 **[Use Consistent Naming](#use-consistent-naming)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.consistent_naming_scanner.ConsistentNamingScanner`
- 🟩 **[Use Domain Language](#use-domain-language)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_language_code_scanner.DomainLanguageCodeScanner`
- 🟩 **[Use Exceptions Properly](#use-exceptions-properly)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_handling_scanner.ExceptionHandlingScanner`
- 🟩 **[Use Explicit Dependencies](#use-explicit-dependencies)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.explicit_dependencies_scanner.ExplicitDependenciesScanner`
- 🟩 **[Use Natural English](#use-natural-english)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.natural_english_code_scanner.NaturalEnglishCodeScanner`
- 🟩 **[Use Resource Oriented Design](#use-resource-oriented-design)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.resource_oriented_code_scanner.ResourceOrientedCodeScanner`

### <span style="color: gray;">[i] Rules Without Scanners</span>

- <span style="color: gray;">[i]</span> **[Detect Legacy Unused Code](#detect-legacy-unused-code)** - No scanner configured
- <span style="color: gray;">[i]</span> **[Refactor Tests With Production Code](#refactor-tests-with-production-code)** - No scanner configured

## Validation Rules Checked

### 🟥 Rule: <span id="eliminate-duplication">Eliminate Duplication</span> - 2078 ERROR(S) - [View Details](#eliminate-duplication-violations)
**Description:** CRITICAL: Every piece of knowledge should have a single, authoritative representation (DRY principle). Extract repeated logic into reusable functions and use abstraction to capture common patterns.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.duplication_scanner.DuplicationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟥 Rule: <span id="stop-writing-useless-comments">Stop Writing Useless Comments</span> - 2 ERROR(S) - [View Details](#stop-writing-useless-comments-violations)
**Description:** CRITICAL: DO NOT WRITE COMMENTS. Delete all comments written by the AI chat. Code must be self-explanatory through clear naming and structure. ONLY exception: legal/license requirements. If you think a comment is needed, the code is wrong - fix the code instead.
**Scanner:** `agile_bot.bots.base_bot.src.actions.scanners.useless_comments_scanner.UselessCommentsScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="chain-dependencies-properly">Chain Dependencies Properly</span> - 4 WARNING(S) - [View Details](#chain-dependencies-properly-violations)
**Description:** CRITICAL: Code must chain dependencies properly with constructor injection. Map dependencies in a chain: highest-level object → collaborator → sub-collaborator. Inject collaborators at construction time so methods can use them without passing them as parameters. Access sub-collaborators through their owning objects.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.dependency_chaining_code_scanner.DependencyChainingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="simplify-control-flow">Simplify Control Flow</span> - 1 WARNING(S) - [View Details](#simplify-control-flow-violations)
**Description:** Keep nesting minimal and control flow straightforward. Use guard clauses to reduce nesting and extract nested blocks into separate functions.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.simplify_control_flow_scanner.SimplifyControlFlowScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="avoid-excessive-guards">Avoid Excessive Guards</span> - CLEAN (0 violations)
**Description:** Excessive guard clauses add to cyclomatic complexity and make code harder to read. Centralize error handling in one place rather than scattering defensive checks throughout the code. Let code fail fast with clear errors rather than silently handling missing components.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.excessive_guards_scanner.ExcessiveGuardsScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="avoid-unnecessary-parameter-passing">Avoid Unnecessary Parameter Passing</span> - CLEAN (0 violations)
**Description:** Don't pass parameters to internal methods when the value is already accessible through instance variables. Access instance properties directly instead of passing them around unnecessarily.
**Scanner:** `agile_bot.bots.base_bot.src.scanners.unnecessary_parameter_passing_scanner.UnnecessaryParameterPassingScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="classify-exceptions-by-caller-needs">Classify Exceptions By Caller Needs</span> - CLEAN (0 violations)
**Description:** Design exceptions based on how callers will handle them. Create exception types based on caller's needs, use special case objects for predictable failures, and wrap third-party exceptions at boundaries.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_classification_scanner.ExceptionClassificationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="enforce-encapsulation">Enforce Encapsulation</span> - CLEAN (0 violations)
**Description:** CRITICAL: Hide implementation details and expose minimal interface. Make fields private by default, expose behavior not data. NEVER pass raw dicts/lists that expose internal structure - use typed objects that encapsulate the data. Follow Law of Demeter (principle of least knowledge).
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.encapsulation_scanner.EncapsulationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="favor-code-representation">Favor Code Representation</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code should represent domain concepts directly. Domain models should match code. If code doesn't match domain concepts, refactor the code rather than creating abstract domain models.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.code_representation_code_scanner.CodeRepresentationCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="group-by-domain">Group By Domain</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must be organized by domain area and relationships, not by technical layers, object types, or architectural concerns.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_grouping_code_scanner.DomainGroupingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="hide-business-logic-behind-properties">Hide Business Logic Behind Properties</span> - CLEAN (0 violations)
**Description:** CRITICAL: Hide business logic behind properties. Properties hide logic that occurs—it may be computed on-demand, cached, pre-computed, or loaded from storage. The caller shouldn't know or care when the values are calculated / determined.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="hide-calculation-timing">Hide Calculation Timing</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must hide calculations. Properties hide logic that occurs—it may be computed on-demand, cached, pre-computed, or loaded from storage. The caller shouldn't know or care when the values are calculated / determined.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="keep-classes-small-with-single-responsibility">Keep Classes Small With Single Responsibility</span> - CLEAN (0 violations)
**Description:** CRITICAL: Classes should be small (under 200-300 lines) with a single responsibility. Keep classes cohesive (methods/data interdependent), eliminate dead code, and favor many small focused classes over few large ones.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.class_size_scanner.ClassSizeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="keep-functions-single-responsibility">Keep Functions Single Responsibility</span> - CLEAN (0 violations)
**Description:** CRITICAL: Functions should do one thing and do it well, with no hidden side effects. Each function must have a single, well-defined responsibility.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.single_responsibility_scanner.SingleResponsibilityScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="keep-functions-small-focused">Keep Functions Small Focused</span> - CLEAN (0 violations)
**Description:** Functions should be small enough to understand at a glance. Keep functions under 20 lines when possible and extract complex logic into named helper functions.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.function_size_scanner.FunctionSizeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="maintain-vertical-density">Maintain Vertical Density</span> - CLEAN (0 violations)
**Description:** Related code should be visually close. Group related concepts together, declare variables close to usage, and keep files under 500 lines when possible.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.vertical_density_scanner.VerticalDensityScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="never-swallow-exceptions">Never Swallow Exceptions</span> - CLEAN (0 violations)
**Description:** CRITICAL: Never swallow exceptions silently. Empty catch blocks hide failures and make debugging impossible. Always log, handle, or rethrow exceptions with context.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.swallowed_exceptions_scanner.SwallowedExceptionsScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="place-imports-at-top">Place Imports At Top</span> - CLEAN (0 violations)
**Description:** Place all import statements at the top of the file, after module docstrings and comments, but before any executable code. This improves readability and makes dependencies clear.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.import_placement_scanner.ImportPlacementScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="prefer-object-model-over-config">Prefer Object Model Over Config</span> - CLEAN (0 violations)
**Description:** Use existing object model to access information instead of directly accessing configuration files
**Scanner:** `agile_bot.bots.base_bot.src.scanners.prefer_object_model_over_config_scanner.PreferObjectModelOverConfigScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="provide-meaningful-context">Provide Meaningful Context</span> - CLEAN (0 violations)
**Description:** Names should provide appropriate context without redundancy. Use longer names for longer scopes and replace magic numbers with named constants.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.meaningful_context_scanner.MeaningfulContextScanner`
**Execution Status:** EXECUTION_SUCCESS

*... and 12 more rules*

## Violations Found

**Total Violations:** 2086
- **File-by-File Violations:** 13
- **Cross-File Violations:** 2073

### File-by-File Violations (Pass 1)

These violations were detected by scanning each file individually.

#### <span id="chain-dependencies-properly-violations">Chain Dependencies Properly: 4 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:14): Method "visit_header" in Test class [CliCodeVisitor](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:14) takes parameter "bot_name" that is already injected in __init__. Use self.bot_name instead.

```python
        self.bot_name = bot_name
    
    def visit_header(self, bot_name: str) -> None:
        pass
    
```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\command_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/command_renderer.py:14): Method "visit_header" in Test class [CursorCommandVisitor](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/command_renderer.py:14) takes parameter "bot_name" that is already injected in __init__. Use self.bot_name instead.

```python
        self.output_lines = output_lines
    
    def visit_header(self, bot_name: str) -> None:
        pass
    
```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\cursor_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer.py:12): Method "visit_header" in Test class [CursorHelpVisitor](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer.py:12) takes parameter "bot_name" that is already injected in __init__. Use self.bot_name instead.

```python
        self.formatter = formatter
    
    def visit_header(self, bot_name: str) -> None:
        name = bot_name if bot_name is not None else self.bot_name
        print(f"## Available Cursor Commands for {name}:")
    # ... (truncated)
```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:20): Method "visit_header" in Test class [MCPCodeVisitor](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:20) takes parameter "bot_name" that is already injected in __init__. Use self.bot_name instead.

```python
        self.server_file_path = None
    
    def visit_header(self, bot_name: str) -> None:
        for behavior in self.behaviors:
            trigger_words = self._load_trigger_words(behavior)
    # ... (truncated)
```

#### <span id="delegate-to-lowest-level-violations">Delegate To Lowest Level: 1 violation(s)</span>

- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:21): Method "visit_header" in Test class [MCPCodeVisitor](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:21) iterates through "behaviors" instead of delegating to collection class. Delegate to collection class instead.

#### <span id="eliminate-duplication-violations">Eliminate Duplication: 5 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:14): Duplicate code detected: functions visit_header, visit_behavior, visit_action, visit_action_help_section_header have identical bodies - extract to shared function
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\command_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/command_renderer.py:14): Duplicate code detected: functions visit_header, visit_behavior, visit_action_help_section_header have identical bodies - extract to shared function
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/help_renderer.py:13): Duplicate code detected: functions render_header, _format_behavior_command, _format_behavior_title, _format_action_command have identical bodies - extract to shared function
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:33): Duplicate code detected: functions visit_action, visit_action_help_section_header have identical bodies - extract to shared function
- <span style="color: red;">[X]</span> **ERROR** - [`src\generator\visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/visitor.py:7): Duplicate code detected: functions visit_header, visit_behavior, visit_action, visit_action_help_section_header, visit_footer have identical bodies - extract to shared function

#### <span id="simplify-control-flow-violations">Simplify Control Flow: 1 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\command_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/command_renderer.py:86): Function "_build_example_params" has nesting depth of 4 - use guard clauses and extract nested blocks to reduce nesting

    ```python
            self.output_lines.append(f"  # {example_cmd}")
        
        def _build_example_params(self, params: List[str]) -> List[str]:
            example_params = []
            for param in params:
                param_name = param.split()[0]
                if '<dict>' in param:
                    example_params.append(f"{param_name} '{{\"key\": \"value\"}}'")
                elif '<list>' in param:
                    example_params.append(f'{param_name} "value1" "value2"')
                elif '<flag>' in param:
                    example_params.append(param_name)
                else:
                    example_params.append(f'{param_name} "value"')
            return example_params
        # ... (truncated)
    ```

#### <span id="stop-writing-useless-comments-violations">Stop Writing Useless Comments: 2 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\generator\visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/visitor.py:24): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        @property
        def data_collector(self):
            """Override in subclasses that need data collection."""
            return None
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_server_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_server_generator.py:62): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
    
        def register_all_tools(self, mcp_server: FastMCP):
            """Deprecated: Tools are now statically generated at code generation time.
            
            This method is kept for backward compatibility but does nothing.
            The MCP server file is generated with static tool registrations via MCPCodeGenerator.
            """
            pass
    ```

### Cross-File Violations (Pass 2)

These violations were detected by analyzing all files together to find patterns that span multiple files.

#### <span id="eliminate-duplication-violations">Eliminate Duplication: 2073 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/action.py:205): Duplicate code detected across files - extract to shared function.

  Location 1 (action.py:_handle_execution_error (lines 205-209)):
    ```python
    error_type = type(e).__name__
    error_message = str(e)
    full_traceback = traceback.format_exc()
    error_instructions = self._build_error_instructions(error_type, error_message, full_traceback, parameters)
    self.track_activity_on_completion(outputs={'error': error_message})
    ```

  Location 2 (mcp_code_visitor.py:visit_behavior (lines 26-31)):
    ```python
    behavior_name = context.behavior_name
    trigger_patterns = self.trigger_words_map.get(behavior_name, [])
    description = self._build_behavior_tool_description(behavior_name, trigger_patterns)
    tool_code = self._generate_behavior_tool_code(behavior_name, description)
    self.tool_registrations.append(tool_co...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_add_help_content_to_display (lines 31-35)):
    ```python
    instructions.add_display('---')
    instructions.add_display('')
    instructions.add_display('## Action Help')
    instructions.add_display('')
    self._add_action_help(instructions)
    ```

  Location 2 (cli_help_generator.py:_build_help_instructions (lines 77-81)):
    ```python
    help_inst.add_display('---')
    help_inst.add_display('')
    help_inst.add_display('## Action Help')
    help_inst.add_display('')
    self._add_action_help_to_display(help_inst)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-171)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 45-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 46-57)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 49-59)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 46-59)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 46-57)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 49-59)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 46-59)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-175)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 45-57)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-175)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 45-59)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 46-57)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 49-59)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 46-59)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-177)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 45-57)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-177)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 45-59)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:get_command_order (lines 57-62)):
    ```python
    cmd_name = cmd_file.stem
    behavior_name = cmd_name.replace(f'{bot_name}-', '').replace('-', '_')
    if behavior_name in ['', 'continue', 'help', 'get_working_dir', 'set_working_dir'] or cmd_name == bot_name:
        return (0, cmd_name)
    order = self._get_behavior_order(bot_directory, behavior_name)
    return (...
    ```

  Location 2 (cli_help_generator.py:get_command_order (lines 114-119)):
    ```python
    cmd_name = cmd_file.stem
    behavior_name = cmd_name.replace(f'{self.bot_name}-', '').replace('-', '_')
    if behavior_name in ['', 'continue', 'help', 'get_working_dir', 'set_working_dir'] or cmd_name == self.bot_name:
        return (0, cmd_name)
    order = self._get_behavior_order_from_config(behavior_name)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 305-315)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 305-315)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 305-315)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-39)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', ...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 303-315)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-39)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', ...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-40)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 303-315)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-40)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\bot_paths.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/bot_paths.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_paths.py:_load_documentation_path (lines 23-29)):
    ```python
    config = read_json_file(bot_config_path)
    docs_path = config.get('docs_path', 'docs/stories')
    return Path(docs_path)
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 129-135)):
    ```python
    behavior_data = read_json_file(behavior_file)
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\bot_matcher.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/bot_matcher.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_matcher.py:_load_patterns_from_file (lines 44-49)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    data = json.loads(content)
    return data.get('patterns', [])
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 129-135)):
    ```python
    behavior_data = read_json_file(behavior_file)
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\bot_matcher.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/bot_matcher.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_matcher.py:_load_patterns_from_file (lines 44-49)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    data = json.loads(content)
    return data.get('patterns', [])
    ```

  Location 2 (server_deployer.py:_load_trigger_words (lines 110-115)):
    ```python
    behavior_data = json.loads(behavior_file.read_text(encoding='utf-8'))
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\bot_matcher.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/bot_matcher.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_matcher.py:_load_patterns_from_file (lines 44-49)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    data = json.loads(content)
    return data.get('patterns', [])
    ```

  Location 2 (file_link_builder.py:get_file_uri (lines 52-59)):
    ```python
    resolved_path = self._resolve_file_path(location)
    file_str = self._normalize_path_string(str(resolved_path))
    return self._build_vscode_uri(file_str, line_number)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:115): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_shell_script (lines 115-119)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIP...
    ```

  Location 2 (cli_script_generator.py:generate_shell_script (lines 23-27)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIP...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:115): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_shell_script (lines 115-119)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIP...
    ```

  Location 2 (cli_script_generator.py:generate_shell_script (lines 23-28)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIP...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:116): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_shell_script (lines 116-120)):
    ```python
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIPT_DIR/src/{self.bot_name}_cli.py" "$@"\n    '
    scri...
    ```

  Location 2 (cli_script_generator.py:generate_python_cli_script (lines 16-20)):
    ```python
    cli_file = src_dir / f'{self.bot_name}_cli.py'
    cli_code = f'''#!/usr/bin/env python3\n"""\n{self.bot_name.title().replace('_', ' ')} CLI Entry Point\n\nCommand-line interface for {self.bot_name} using BaseBotCli.\n\nUsage:\n    {self.bot_name} [--behavior <name>] [--action <name>] [--options]\n    {...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:116): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_shell_script (lines 116-120)):
    ```python
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIPT_DIR/src/{self.bot_name}_cli.py" "$@"\n    '
    scri...
    ```

  Location 2 (cli_script_generator.py:generate_shell_script (lines 24-28)):
    ```python
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIPT_DIR/src/{self.bot_name}_cli.py" "$@"\n    '
    scri...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:115): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_shell_script (lines 115-120)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIP...
    ```

  Location 2 (cli_script_generator.py:generate_shell_script (lines 23-27)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIP...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:115): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_shell_script (lines 115-120)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIP...
    ```

  Location 2 (cli_script_generator.py:generate_shell_script (lines 23-28)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli'
    script_content = f'#!/bin/bash\n\n    SCRIPT_DIR="$(cd "$(dirname "${{BASH_SOURCE[0]}}")" && pwd)"\n\n    export WORKING_DIR="${{WORKING_DIR:-$(cd "$SCRIPT_DIR/../../.." && pwd)}}"\n\n    python3 "$SCRIP...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:123): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_powershell_script (lines 123-136)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli.ps1'
    script_content = f"""# {self.bot_name.title().replace('_', ' ')} CLI Wrapper (PowerShell)\n\n    $SCRIPT_DIR = Split-Path -Parent $MyInvocation.MyCommand.Path\n\n    if (-not $env:WORKING_DIR) {{\n  ...
    ```

  Location 2 (cli_script_generator.py:generate_powershell_script (lines 31-35)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli.ps1'
    script_content = f"""# {self.bot_name.title().replace('_', ' ')} CLI Wrapper (PowerShell)\n\n    $SCRIPT_DIR = Split-Path -Parent $MyInvocation.MyCommand.Path\n\n    if (-not $env:WORKING_DIR) {{\n  ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_generator.py:48): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_generator.py:_discover_behaviors_from_folders (lines 48-56)):
    ```python
    behaviors_dir = self.workspace_root / self.bot_location / 'behaviors'
    if not behaviors_dir.exists():
        return []
    behaviors = []
    for item in sorted(behaviors_dir.iterdir()):
        if item.is_dir() and (not item.name.startswith('_')) and (not item.name.startswith('.')):
            if (item / 'behavior.js...
    ```

  Location 2 (server_deployer.py:_discover_behaviors_from_folders (lines 53-61)):
    ```python
    behaviors_dir = self.bot_directory / 'behaviors'
    if not behaviors_dir.exists():
        return []
    behaviors = []
    for item in sorted(behaviors_dir.iterdir()):
        if item.is_dir() and (not item.name.startswith('_')) and (not item.name.startswith('.')):
            if (item / 'behavior.json').exists():
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 17-21)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 21-25)):
    ```python
    print(f'\n## {cmd_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 17-21)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 38-42)):
    ```python
    print(f'\n{self._format_behavior_title(context)}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 18-22)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 22-26)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 18-22)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 39-43)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    print()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 19-23)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 23-27)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 20-24)):
    ```python
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 24-28)):
    ```python
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 22-29)):
    ```python
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, description in context.additional_options.items():
            print(f'           {option}  {description}')
    print('```\n')
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 26-33)):
    ```python
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, description in context.additional_options.items():
            print(f'           {option}  {description}')
    print('```\n')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 17-22)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 21-26)):
    ```python
    print(f'\n## {cmd_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 17-22)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 38-43)):
    ```python
    print(f'\n{self._format_behavior_title(context)}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    print()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 18-23)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 22-27)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 19-24)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 23-28)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 20-28)):
    ```python
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additi...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 24-32)):
    ```python
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, description in context.additio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 21-29)):
    ```python
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, descript...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 25-33)):
    ```python
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, description in context.additional_options.items():
            print(f'   ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 17-23)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 21-27)):
    ```python
    print(f'\n## {cmd_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 18-24)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 22-28)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 19-28)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('    ...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 23-32)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, description in co...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 20-29)):
    ```python
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additi...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 24-33)):
    ```python
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, description in context.additio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 17-24)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 21-28)):
    ```python
    print(f'\n## {cmd_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 18-28)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if ...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 22-32)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 19-29)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('    ...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 24-33)):
    ```python
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, description in context.additio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 19-29)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('    ...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 23-33)):
    ```python
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional options:')
        for option, description in co...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 17-28)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 21-32)):
    ```python
    print(f'\n## {cmd_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        pr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 18-29)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if ...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 22-33)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        print('           Additional o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_behavior (lines 17-29)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    print(f'action:   {action_...
    ```

  Location 2 (cursor_help_renderer.py:visit_behavior (lines 21-33)):
    ```python
    print(f'\n## {cmd_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    print(f'action:   {action_list}')
    print('context:  Optional context or file path')
    if context.additional_options:
        pr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_action (lines 32-38)):
    ```python
    print(f'### {context.action_name}\n')
    print(f'{context.action_description}\n')
    print('```')
    print(f'python {self.cli_script_path} --behavior <behavior> --action {context.action_name} [parameters]')
    if context.parameters:
        print()
        self._print_parameters(context)
    ```

  Location 2 (cursor_help_renderer.py:visit_action (lines 36-42)):
    ```python
    print(f'### {context.action_name}\n')
    print(f'{context.action_description}\n')
    print('```')
    print(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        print()
        self._print_parameters(context)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_action (lines 33-39)):
    ```python
    print(f'{context.action_description}\n')
    print('```')
    print(f'python {self.cli_script_path} --behavior <behavior> --action {context.action_name} [parameters]')
    if context.parameters:
        print()
        self._print_parameters(context)
    print('```\n')
    ```

  Location 2 (cursor_help_renderer.py:visit_action (lines 37-43)):
    ```python
    print(f'{context.action_description}\n')
    print('```')
    print(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        print()
        self._print_parameters(context)
    print('```\n')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:visit_action (lines 32-39)):
    ```python
    print(f'### {context.action_name}\n')
    print(f'{context.action_description}\n')
    print('```')
    print(f'python {self.cli_script_path} --behavior <behavior> --action {context.action_name} [parameters]')
    if context.parameters:
        print()
        self._print_parameters(context)
    print('```\n')
    ```

  Location 2 (cursor_help_renderer.py:visit_action (lines 36-43)):
    ```python
    print(f'### {context.action_name}\n')
    print(f'{context.action_description}\n')
    print('```')
    print(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        print()
        self._print_parameters(context)
    print('```\n')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer.py:_print_parameters (lines 42-47)):
    ```python
    param_desc = context.parameter_descriptions.get(param, 'Optional parameter')
    if '\n' in param_desc:
        self._print_multiline_parameter(param, param_desc)
    else:
        print(f'{param}:   {param_desc}')
    ```

  Location 2 (cursor_help_renderer.py:_print_parameters (lines 46-51)):
    ```python
    param_desc = context.parameter_descriptions.get(param, 'Optional parameter')
    if '\n' in param_desc:
        self._print_multiline_parameter(param, param_desc)
    else:
        print(f'{param}:   {param_desc}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_script_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_script_generator.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_script_generator.py:generate_python_cli_script (lines 13-17)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    cli_file = src_dir / f'{self.bot_name}_cli.py'
    cli_code = f'''#!/usr/bin/env python3\n"""\n{self.bot_name.title().replace('_', ' ')} CLI Entry Point\n\nCommand-line interface for {s...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-88)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 305-315)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:_generate_base_commands (lines 61-69)):
    ```python
    commands[f'{self.bot_name}'] = self._write_command_file(commands_dir / f'{self.bot_name}.md', python_command)
    commands[f'{self.bot_name}-continue'] = self._write_command_file(commands_dir / f'{self.bot_name}-continue.md', f'{python_command} --close')
    help_content = self._build_help_command_content(p...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer.py:visit_behavior (lines 21-25)):
    ```python
    print(f'\n## {cmd_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 38-42)):
    ```python
    print(f'\n{self._format_behavior_title(context)}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer.py:visit_behavior (lines 22-26)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 39-43)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    print()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer.py:visit_behavior (lines 21-26)):
    ```python
    print(f'\n## {cmd_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'/{cmd_name} <{action_list}> <context>')
    print()
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 38-43)):
    ```python
    print(f'\n{self._format_behavior_title(context)}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    print()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:166): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 166-170)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:166): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 166-170)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 167-171)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 167-171)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:168): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 168-180)):
    ```python
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_code()
    return '\n\n'.join([bot_tool_code, get_working_dir_cod...
    ```

  Location 2 (render_spec.py:execute_synchronizer (lines 93-97)):
    ```python
    synchronizer_instance = self._import_synchronizer_class(self.synchronizer.synchronizer_class_path)()
    input_path = self._resolve_input_path()
    output_path = self._resolve_output_path(input_path)
    kwargs = self._build_synchronizer_kwargs()
    return synchronizer_instance.render(str(input_path), str(output_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:166): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 166-171)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-36)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    rules_content = self._g...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\generator\orchestrator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/orchestrator.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (orchestrator.py:_visit_behavior (lines 40-50)):
    ```python
    behavior_name = behavior.name
    behavior_description = self.data_collector.get_behavior_description(behavior_name)
    actions = self.data_collector.get_behavior_actions(behavior)
    additional_options = self._get_additional_options(behavior_name)
    context = BehaviorHelpContext(bot_name=self.bot_name, behavio...
    ```

  Location 2 (mcp_code_generator.py:generate_server_entry_point (lines 17-22)):
    ```python
    workspace_root = self.bot_directory.parent.parent.parent.parent.parent
    formatter = CliTerminalFormatter()
    description_extractor = DescriptionExtractor(self.bot_name, self.bot_directory, formatter)
    data_collector = ActionDataCollector(bot, self.bot_name, self.bot_directory, description_extractor)
    mcp...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\generator\orchestrator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/orchestrator.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (orchestrator.py:_visit_behavior (lines 40-50)):
    ```python
    behavior_name = behavior.name
    behavior_description = self.data_collector.get_behavior_description(behavior_name)
    actions = self.data_collector.get_behavior_actions(behavior)
    additional_options = self._get_additional_options(behavior_name)
    context = BehaviorHelpContext(bot_name=self.bot_name, behavio...
    ```

  Location 2 (build_action.py:inject_rules (lines 136-142)):
    ```python
    rules_obj = validate_action.rules
    rules_text = rules_obj.formatted_rules_digest()
    rules_data = validate_action.inject_behavior_specific_and_bot_rules()
    all_rules = rules_data.get('validation_rules', [])
    existing_instructions = instructions.get('base_instructions', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\bot_tool_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/bot_tool_generator.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_tool_generator.py:_get_current_behavior (lines 25-30)):
    ```python
    if bot.behaviors.first:
        bot.behaviors.navigate_to(bot.behaviors.first.name)
        current_behavior = bot.behaviors.current
    else:
        raise ValueError('No behaviors available')
    ```

  Location 2 (mcp_tool_registrar.py:register_bot_tool (lines 38-43)):
    ```python
    if self.bot.behaviors.first:
        self.bot.behaviors.navigate_to(self.bot.behaviors.first.name)
        current_behavior = self.bot.behaviors.current
    else:
        raise ValueError('No behaviors available')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_generator.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_generator.py:generate_server_entry_point (lines 18-29)):
    ```python
    formatter = CliTerminalFormatter()
    description_extractor = DescriptionExtractor(self.bot_name, self.bot_directory, formatter)
    data_collector = ActionDataCollector(bot, self.bot_name, self.bot_directory, description_extractor)
    mcp_visitor = MCPCodeVisitor(workspace_root, self.bot_directory, self.bot_...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 32-36)):
    ```python
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    rules_content = self._generate_rules_content(bot_config, behavio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_generator.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_generator.py:generate_server_entry_point (lines 20-31)):
    ```python
    data_collector = ActionDataCollector(bot, self.bot_name, self.bot_directory, description_extractor)
    mcp_visitor = MCPCodeVisitor(workspace_root, self.bot_directory, self.bot_name, behaviors, data_collector)
    config = GeneratorConfig(bot=bot, bot_name=self.bot_name, visitor=mcp_visitor, data_collector...
    ```

  Location 2 (rule.py:scan (lines 118-122)):
    ```python
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    self._initialize_scan_state()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_config_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_config_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_config_generator.py:generate_workspace_rules_file (lines 33-37)):
    ```python
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    rules_content = self._generate_rules_content(bot_config, behaviors, behavior_info, behavior_sections...
    ```

  Location 2 (rules_action.py:do_execute (lines 12-17)):
    ```python
    instructions = self.instructions.copy()
    rules = Rules(behavior=self.behavior, bot_paths=self.behavior.bot_paths)
    rules_digest = rules.formatted_rules_digest()
    rule_names = self._get_rule_names(rules)
    self._add_rules_list_to_display(instructions, rule_names)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_server_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_server_generator.py:129): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 129-135)):
    ```python
    behavior_data = read_json_file(behavior_file)
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```

  Location 2 (server_deployer.py:_load_trigger_words (lines 110-115)):
    ```python
    behavior_data = json.loads(behavior_file.read_text(encoding='utf-8'))
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_server_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_server_generator.py:129): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 129-135)):
    ```python
    behavior_data = read_json_file(behavior_file)
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```

  Location 2 (file_link_builder.py:get_file_uri (lines 52-59)):
    ```python
    resolved_path = self._resolve_file_path(location)
    file_str = self._normalize_path_string(str(resolved_path))
    return self._build_vscode_uri(file_str, line_number)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\server_deployer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/server_deployer.py:110): Duplicate code detected across files - extract to shared function.

  Location 1 (server_deployer.py:_load_trigger_words (lines 110-115)):
    ```python
    behavior_data = json.loads(behavior_file.read_text(encoding='utf-8'))
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```

  Location 2 (file_link_builder.py:get_file_uri (lines 52-59)):
    ```python
    resolved_path = self._resolve_file_path(location)
    file_str = self._normalize_path_string(str(resolved_path))
    return self._build_vscode_uri(file_str, line_number)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 20-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, content, file_path, rule_obj)
            if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-44)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-44)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-44)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-44)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-46)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-46)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-46)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-46)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-48)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-45)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-50)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 44-52)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-65)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-54)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-54)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-54)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 42-51)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-54)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 50-59)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-54)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-63)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-54)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-67)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-54)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:48): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 48-56)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:48): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 48-56)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:48): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 48-56)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 61-69)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-46)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-43)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-48)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-45)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-50)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-52)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-49)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 44-54)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 53-63)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 44-54)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-67)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 44-54)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-69)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 42-51)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 50-59)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-63)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-67)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 45-56)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-48)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-45)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-50)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 44-56)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 53-63)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 44-56)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-67)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 44-56)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-69)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 44-56)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-50)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-49)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-52)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-49)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-54)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-54)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 42-56)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-42)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-49)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-52)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-49)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-54)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-40)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-54)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-54)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-51)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 39-56)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-54)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-40)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 36-54)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-51)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:37): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 37-56)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 38-46)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_outline_scanner.py:scan_story_node (lines 25-33)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 38-46)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_specific_given_scanner.py:scan_story_node (lines 24-32)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 38-46)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 36-44)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 20-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, content, file_path, rule_obj)
            if violation:...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 20-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, content, file_path, rule_obj)
            if violation:...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 20-35)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = self._check_function_size(function.node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 20-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, content, file_path, rule_obj)
            if violation:...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 21-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_path, content, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:160): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:_validate_aaa_structure (lines 160-165)):
    ```python
    test_lines = file_path.read_text(encoding='utf-8').split('\n')
    start_line = test_node.lineno - 1
    end_line = test_node.end_lineno if hasattr(test_node, 'end_lineno') else start_line + 50
    test_body_lines = test_lines[start_line:end_line]
    has_given_comment = any(('# Given' in line or '# Arrange' in lin...
    ```

  Location 2 (given_when_then_helpers_scanner.py:_check_test_method (lines 116-122)):
    ```python
    test_lines = content.split('\n')
    start_line = test_node.lineno - 1
    end_line = test_node.end_lineno if hasattr(test_node, 'end_lineno') else len(test_lines)
    test_body_lines = test_lines[start_line:end_line]
    inline_blocks = self._find_inline_code_blocks(test_node, test_body_lines, helper_functions, tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\background_common_setup_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/background_common_setup_scanner.py:96): Duplicate code detected across files - extract to shared function.

  Location 1 (background_common_setup_scanner.py:_get_scenario_steps (lines 96-102)):
    ```python
    if 'steps' in scenario:
        return scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            return [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (given_precondition_scanner.py:_get_scenario_steps (lines 32-38)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\background_common_setup_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/background_common_setup_scanner.py:96): Duplicate code detected across files - extract to shared function.

  Location 1 (background_common_setup_scanner.py:_get_scenario_steps (lines 96-102)):
    ```python
    if 'steps' in scenario:
        return scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            return [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (scenario_specific_given_scanner.py:_get_scenario_steps (lines 38-44)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(self._check_misleading_todos(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 21-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.extend(self._check_assertions(tree, content, file_path, rule_obj...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(self._check_mislead...
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.extend(self._check...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(sel...
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.ex...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 20-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 20-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 30-38)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:_extract_domain_language (lines 45-62)):
    ```python
    domain_terms = set()
    common_domain_terms = {'json', 'data', 'param', 'params', 'parameter', 'parameters', 'var', 'vars', 'variable', 'variables', 'method', 'methods', 'class', 'classes', 'call', 'calls', 'config', 'configuration', 'configurations', 'agent', 'bot', 'workflow', 'story', 'epic', 'scena...
    ```

  Location 2 (code_scanner.py:_extract_domain_terms (lines 45-63)):
    ```python
    domain_terms = set()
    common_domain_terms = {'json', 'data', 'param', 'params', 'parameter', 'parameters', 'var', 'vars', 'variable', 'variables', 'method', 'methods', 'class', 'classes', 'call', 'calls', 'config', 'configuration', 'configurations', 'agent', 'bot', 'workflow', 'story', 'epic', 'scena...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 21-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_path, content, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-34)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 20-27)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 21-32)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 20-27)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 24-34)):
    ```python
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 20-27)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 21-34)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-34)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:118): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_expected_scenario_name (lines 118-124)):
    ```python
    full_method_name = f'test_{method_name}' if not method_name.startswith('test_') else method_name
    method_name_norm = self._normalize_name(method_name)
    story_name_from_class = class_name[4:] if class_name.startswith('Test') else class_name
    story_name_normalized = self._normalize_name(story_name_from_c...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rule_line (lines 75-79)):
    ```python
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self._get_status_indicator(rule_info)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    anchor_link = self._rule_name_to_anchor(rule_name)
    rule_display_name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-312)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:305): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 305-315)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:305): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 305-315)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:305): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 305-315)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:305): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 305-315)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-57)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:308): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 308-316)):
    ```python
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 51-57)):
    ```python
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-315)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-315)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-315)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-315)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-57)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-315)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-57)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:305): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 305-316)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:305): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 305-316)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:305): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 305-316)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-57)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:305): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 305-316)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-57)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-316)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-316)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-57)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:303): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 303-316)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-57)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:345): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 345-356)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 77-89)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if suggestions_normalized:
        sugges...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:345): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 345-356)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 73-89)):
    ```python
    if matches:
        return None
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if sugges...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:348): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 348-364)):
    ```python
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}.py' for s in suggestions[:5]])
        suggestion_text = f' Suggested names: {suggestion_list}'...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 80-97)):
    ```python
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if suggestions_normalized:
        suggestion_list = ', '.join([f'test_{s}.py' for s in suggestions_normalized[:5]])
        suggestion_t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:376): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_get_sub_epics_spanned_by_test_methods (lines 376-385)):
    ```python
    class_name = node.name
    for item in node.body:
        if isinstance(item, ast.FunctionDef):
            if item.name.startswith('test_'):
                sub_epic = self._find_sub_epic_for_method(item.name, class_name, knowledge_graph)
                if sub_epic:
                    sub_epics.add(self._to_snake_case(s...
    ```

  Location 2 (test_file_naming_scanner.py:_get_sub_epics_spanned_by_test_methods (lines 109-118)):
    ```python
    class_name = node.name
    for item in node.body:
        if isinstance(item, ast.FunctionDef):
            if item.name.startswith('test_'):
                sub_epic = self._find_sub_epic_for_method(item.name, class_name, knowledge_graph)
                if sub_epic:
                    sub_epics.add(self._to_snake_case(s...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:417): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_sub_epic_for_method (lines 417-427)):
    ```python
    scenario_name = scenario.get('name', '')
    if not scenario_name:
        continue
    scenario_name_norm = self._normalize_name(scenario_name)
    if method_name_norm in scenario_name_norm or scenario_name_norm.startswith(method_name_norm) or method_name_norm.startswith(scenario_name_norm):
        return sub_epic_na...
    ```

  Location 2 (test_file_naming_scanner.py:_find_sub_epic_for_method (lines 149-159)):
    ```python
    scenario_name = scenario.get('name', '')
    if not scenario_name:
        continue
    scenario_name_norm = self._to_snake_case(scenario_name)
    if method_name_norm in scenario_name_norm or scenario_name_norm.startswith(method_name_norm) or method_name_norm.startswith(scenario_name_norm):
        return sub_epic_nam...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:437): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_sub_epic_for_method (lines 437-447)):
    ```python
    ac_text = ac.get('text', '') if isinstance(ac, dict) else str(ac)
    if not ac_text:
        continue
    ac_norm = self._normalize_name(ac_text)
    if method_name_norm in ac_norm or ac_norm.startswith(method_name_norm) or method_name_norm.startswith(ac_norm):
        return sub_epic_name
    ```

  Location 2 (test_file_naming_scanner.py:_find_sub_epic_for_method (lines 169-179)):
    ```python
    ac_text = ac.get('text', '') if isinstance(ac, dict) else str(ac)
    if not ac_text:
        continue
    ac_norm = self._to_snake_case(ac_text)
    if method_name_norm in ac_norm or ac_norm.startswith(method_name_norm) or method_name_norm.startswith(ac_norm):
        return sub_epic_name
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 43-54)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 43-52)):
    ```python
    if self._is_test_file(file_path):
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 44-54)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 46-54)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 48-56)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 46-54)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 44-56)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 47-56)):
    ```python
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 49-58)):
    ```python
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 43-54)):
    ```python
    if self._is_test_file(file_path):
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 44-56)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 46-56)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 48-58)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:_is_test_file (lines 65-78)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_'):
        return True
    if file_name == 'conftest.py':
        return True
    return False
    ```

  Location 2 (scanner.py:_is_test_file (lines 74-83)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:133): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_extract_domain_terms (lines 133-139)):
    ```python
    if isinstance(ac, dict):
        ac_text = ac.get('criterion', '')
        if ac_text:
            domain_terms.update(self._extract_words_from_text(ac_text))
    elif isinstance(ac, str):
        domain_terms.update(self._extract_words_from_text(ac))
    ```

  Location 2 (specification_match_scanner.py:_extract_domain_terms (lines 299-305)):
    ```python
    if isinstance(ac, dict):
        ac_text = ac.get('criterion', '')
        if ac_text:
            domain_terms.update(self._extract_words_from_text(ac_text))
    elif isinstance(ac, str):
        domain_terms.update(self._extract_words_from_text(ac))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:193): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_parse_code_file (lines 193-198)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:_parse_test_file (lines 105-110)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:193): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_parse_code_file (lines 193-198)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (test_scanner.py:_parse_test_file (lines 51-56)):
    ```python
    content = test_file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(test_file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:207): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_read_and_parse_file (lines 207-214)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    lines = content.split('\n')
    tree = ast.parse(content, filename=str(file_path))
    return (content, lines, tree)
    ```

  Location 2 (test_scanner.py:_read_and_parse_file (lines 65-72)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    lines = content.split('\n')
    tree = ast.parse(content, filename=str(file_path))
    return (content, lines, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:228): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_get_all_code_files_parsed (lines 228-232)):
    ```python
    parsed = self._parse_code_file(file_path)
    if parsed:
        content, tree = parsed
        parsed_files.append((file_path, content, tree))
    ```

  Location 2 (test_scanner.py:_get_all_test_files_parsed (lines 80-84)):
    ```python
    parsed = self._parse_test_file(test_file_path)
    if parsed:
        content, tree = parsed
        parsed_files.append((test_file_path, content, tree))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-45)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 30-38)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 30-38)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 30-38)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 61-69)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-43)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-45)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-45)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 52-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-25)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-25)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-27)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violati...
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violati...
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violati...
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 18-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 21-34)):
    ```python
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 18-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 21-36)):
    ```python
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 20-35)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = self._check_function_size(function.node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 21-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_path, content, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-54)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-54)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_scanner.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_scanner.py:_scan_domain_concepts (lines 70-80)):
    ```python
    domain_concept_node = DomainConceptNode(concept_data, epic_idx, sub_epic_path, concept_idx)
    concept_violations = self.scan_domain_concept(domain_concept_node, rule_obj)
    violations.extend(concept_violations)
    ```

  Location 2 (enumerate_stories_scanner.py:scan_story_node (lines 19-27)):
    ```python
    location = node.map_location('description')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~X stories" notation - all stories must be explicitly enumerated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_scanner.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_scanner.py:_scan_domain_concepts (lines 70-80)):
    ```python
    domain_concept_node = DomainConceptNode(concept_data, epic_idx, sub_epic_path, concept_idx)
    concept_violations = self.scan_domain_concept(domain_concept_node, rule_obj)
    violations.extend(concept_violations)
    ```

  Location 2 (scenarios_on_story_docs_scanner.py:scan_story_node (lines 128-136)):
    ```python
    location = node.map_location()
    violation = Violation(rule=rule_obj, violation_message=f'Story "{node.name}" has no scenarios or scenario_outlines in story-graph.json - scenarios should be in JSON (scenarios or scenario_outlines fields)', location=location, severity='error').to_dict()
    violations.appe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_scanner.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_scanner.py:_scan_domain_concepts (lines 70-80)):
    ```python
    domain_concept_node = DomainConceptNode(concept_data, epic_idx, sub_epic_path, concept_idx)
    concept_violations = self.scan_domain_concept(domain_concept_node, rule_obj)
    violations.extend(concept_violations)
    ```

  Location 2 (story_enumeration_scanner.py:scan_story_node (lines 20-28)):
    ```python
    location = node.map_location('estimated_stories')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~{estimated_stories}" notation - all stories must be explicitly enumerated, not estimated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\enumerate_stories_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/enumerate_stories_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (enumerate_stories_scanner.py:scan_story_node (lines 19-27)):
    ```python
    location = node.map_location('description')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~X stories" notation - all stories must be explicitly enumerated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenarios_on_story_docs_scanner.py:scan_story_node (lines 128-136)):
    ```python
    location = node.map_location()
    violation = Violation(rule=rule_obj, violation_message=f'Story "{node.name}" has no scenarios or scenario_outlines in story-graph.json - scenarios should be in JSON (scenarios or scenario_outlines fields)', location=location, severity='error').to_dict()
    violations.appe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\enumerate_stories_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/enumerate_stories_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (enumerate_stories_scanner.py:scan_story_node (lines 19-27)):
    ```python
    location = node.map_location('description')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~X stories" notation - all stories must be explicitly enumerated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (story_enumeration_scanner.py:scan_story_node (lines 20-28)):
    ```python
    location = node.map_location('estimated_stories')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~{estimated_stories}" notation - all stories must be explicitly enumerated, not estimated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 30-38)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 20-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.is_test_function:
            continue
        if function.name.startswith('_') and function.name != '__i...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 20-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.is_test_function:
            continue
        if function.name.startswith('_') and function.name != '__i...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:107): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:_get_violation_message (lines 107-111)):
    ```python
    violation_messages = rule_obj.rule_content.get('violation_messages', {})
    if message_key in violation_messages:
        template = violation_messages[message_key]
        return template.format(line=line_number, **format_args)
    ```

  Location 2 (type_safety_scanner.py:_get_violation_message (lines 213-217)):
    ```python
    violation_messages = rule_obj.rule_content.get('violation_messages', {})
    if message_key in violation_messages:
        template = violation_messages[message_key]
        return template.format(line=line_number, **format_args)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-32)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-34)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-45)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-36)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 30-38)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 53-61)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 42-51)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 46-55)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 50-59)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-63)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-67)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-40)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 34-42)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 61-69)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-32)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-43)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-34)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-45)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-36)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-38)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-49)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 30-40)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 53-63)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 30-40)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-67)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 42-51)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 50-59)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-63)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-67)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 31-42)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-34)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-45)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-36)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-38)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-49)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-40)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-51)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 28-40)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:30): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 30-42)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-69)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-36)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-49)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-38)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-49)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-40)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-40)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-49)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-38)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-49)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-40)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-40)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-51)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 25-42)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-40)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-51)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 23-42)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_precondition_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_precondition_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (given_precondition_scanner.py:scan_story_node (lines 19-26)):
    ```python
    scenario_steps = self._get_scenario_steps(scenario)
    for step_idx, step in enumerate(scenario_steps):
        if step.startswith('Given') or step.startswith('And'):
            violation = self._check_given_is_functionality(step, node, scenario_idx, step_idx, rule_obj)
            if violation:
                viola...
    ```

  Location 2 (given_state_not_actions_scanner.py:scan_story_node (lines 19-26)):
    ```python
    scenario_steps = self._get_scenario_steps(scenario)
    for step_idx, step in enumerate(scenario_steps):
        if step.startswith('Given') or step.startswith('And'):
            violation = self._check_given_is_action(step, node, scenario_idx, step_idx, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_precondition_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_precondition_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (given_precondition_scanner.py:_get_scenario_steps (lines 32-38)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (scenario_specific_given_scanner.py:_get_scenario_steps (lines 38-44)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:105): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:_parse_test_file (lines 105-110)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (test_scanner.py:_parse_test_file (lines 51-56)):
    ```python
    content = test_file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(test_file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 52-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 18-27)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\intention_revealing_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/intention_revealing_names_scanner.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (intention_revealing_names_scanner.py:scan_file (lines 52-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_ubiquitous_language(content, domain_terms, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\invest_principles_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/invest_principles_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (invest_principles_scanner.py:scan_story_node (lines 22-29)):
    ```python
    story_data = node.data if hasattr(node, 'data') else {}
    scenarios = story_data.get('scenarios', [])
    scenario_outlines = story_data.get('scenario_outlines', [])
    acceptance_criteria = story_data.get('acceptance_criteria', [])
    has_scenarios = scenarios and len(scenarios) > 0 or (scenario_outlines and l...
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-29)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 20-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-29)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 28-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-29)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-29)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 20-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 28-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 20-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 20-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 18-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 25-33)):
    ```python
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    violations.extend(method_violations)
    fake_violations = self._check_fake_implementations(lines, file_path, rule_obj)
    violations.extend(fake_violations)
    return violations
    ```

  Location 2 (type_safety_scanner.py:_check_function_type_safety (lines 80-86)):
    ```python
    return_violations = self._check_return_type(func_node, file_path, rule_obj, content)
    violations.extend(return_violations)
    get_violations = self._check_parameters_get_pattern(func_node, file_path, rule_obj, content)
    violations.extend(get_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner.py:74): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner.py:_is_test_file (lines 74-83)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```

  Location 2 (file.py:is_test_file (lines 81-90)):
    ```python
    path_str = str(self._path).lower()
    file_name = self._path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner_loader.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner_loader.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_loader.py:_load_scanner_class (lines 39-49)):
    ```python
    try:
        module = importlib.import_module(path)
        if hasattr(module, class_name):
            scanner_class = getattr(module, class_name)
            if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
                if issubclass(scanner_class, Scanner):
                    return (scanner_cl...
    ```

  Location 2 (scanner_registry.py:loads_scanner_class_with_error (lines 49-59)):
    ```python
    try:
        module = importlib.import_module(path)
        if hasattr(module, class_name):
            scanner_class = getattr(module, class_name)
            if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
                if issubclass(scanner_class, Scanner):
                    return (scanner_cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner_loader.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner_loader.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_loader.py:_load_scanner_class (lines 40-49)):
    ```python
    module = importlib.import_module(path)
    if hasattr(module, class_name):
        scanner_class = getattr(module, class_name)
        if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
            if issubclass(scanner_class, Scanner):
                return (scanner_class, None)
    ```

  Location 2 (scanner_registry.py:loads_scanner_class_with_error (lines 50-59)):
    ```python
    module = importlib.import_module(path)
    if hasattr(module, class_name):
        scanner_class = getattr(module, class_name)
        if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
            if issubclass(scanner_class, Scanner):
                return (scanner_class, None)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenarios_on_story_docs_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenarios_on_story_docs_scanner.py:128): Duplicate code detected across files - extract to shared function.

  Location 1 (scenarios_on_story_docs_scanner.py:scan_story_node (lines 128-136)):
    ```python
    location = node.map_location()
    violation = Violation(rule=rule_obj, violation_message=f'Story "{node.name}" has no scenarios or scenario_outlines in story-graph.json - scenarios should be in JSON (scenarios or scenario_outlines fields)', location=location, severity='error').to_dict()
    violations.appe...
    ```

  Location 2 (story_enumeration_scanner.py:scan_story_node (lines 20-28)):
    ```python
    location = node.map_location('estimated_stories')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~{estimated_stories}" notation - all stories must be explicitly enumerated, not estimated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_outline_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_outline_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_outline_scanner.py:scan_story_node (lines 25-33)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_specific_given_scanner.py:scan_story_node (lines 24-32)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_outline_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_outline_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_outline_scanner.py:scan_story_node (lines 25-33)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 36-44)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_specific_given_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_specific_given_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_specific_given_scanner.py:scan_story_node (lines 24-32)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 36-44)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-45)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 61-69)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-43)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-45)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-45)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 26-39)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_map.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_map.py:255): Duplicate code detected across files - extract to shared function.

  Location 1 (story_map.py:from_bot (lines 255-272)):
    ```python
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise TypeError(f...
    ```

  Location 2 (nodes.py:from_bot (lines 350-363)):
    ```python
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise TypeError(f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_map.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_map.py:253): Duplicate code detected across files - extract to shared function.

  Location 1 (story_map.py:from_bot (lines 253-272)):
    ```python
    import json
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise...
    ```

  Location 2 (nodes.py:from_bot (lines 350-363)):
    ```python
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise TypeError(f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_file_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_file_naming_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_file_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_file_naming_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 20-31)):
    ```python
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 17-28)):
    ```python
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 28-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 28-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 27-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 27-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ubiquitous_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ubiquitous_language_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (ubiquitous_language_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-133)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-120)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:127): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 127-142)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 114-129)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:127): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 127-142)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 114-131)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:129): Duplicate code detected across files - extract to shared function.

Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 129-144)):
```python
lines_before = before_docstring.strip()
if not lines_before or lines_before.count('\n') == 0:
    return False
lines = recent_context.split('\n')
for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    line = lines[i].strip()
    if line.startswith('def ') or line.startswith('Test class [):
     ...
```

Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 116-131)):
```python
lines_before = before_docstring.strip()
if not lines_before or lines_before.count(](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:129)\n') == 0:
    return False
lines = recent_context.split('\n')
for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    line = lines[i].strip()
    if line.startswith('def ') or line.startswith('class '):
     ...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-142)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-129)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-142)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-131)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:127): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 127-144)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 114-129)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:127): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 127-144)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 114-131)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-144)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-129)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-144)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-131)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\clarify\requirements_clarifications.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/clarify/requirements_clarifications.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (requirements_clarifications.py:save (lines 17-21)):
    ```python
    original_questions = self.required_context.key_questions.questions
    original_evidence_requirements = self.required_context.evidence.evidence_list
    new_data = {'key_questions': {'questions': original_questions, 'answers': self.key_questions_answered}, 'evidence': {'required': original_evidence_requirem...
    ```

  Location 2 (strategy_decision.py:save (lines 20-24)):
    ```python
    original_assumptions = self.strategy.assumptions.assumptions
    original_recommended_activities = self.strategy.recommended_activities.recommended_activities
    new_data = {'strategy_criteria': {'criteria': original_strategy_criteria, 'decisions_made': self.decisions_made}, 'assumptions': {'typical_assump...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```

  Location 2 (scanner_status_formatter.py:format_executed_rule_line (lines 104-108)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 227-231)):
    ```python
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule_filter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule_filter.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (rule_filter.py:_is_file_in_folder (lines 80-87)):
    ```python
    folder_path = Path(folder_pattern)
    if not (folder_path.is_absolute() or file_path.is_absolute()):
        return False
    return self._check_path_relative(file_path, folder_path)
    ```

  Location 2 (file_discovery.py:_is_file_in_folder (lines 60-66)):
    ```python
    folder_path = Path(folder_pattern)
    if not (folder_path.is_absolute() or file_path.is_absolute()):
        return False
    return self._check_path_relative(file_path, folder_path)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\file_link_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/file_link_builder.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (file_link_builder.py:get_relative_path (lines 82-90)):
    ```python
    if file_path.is_absolute() and self.workspace_directory:
        return str(file_path.relative_to(self.workspace_directory))
    if self.workspace_directory and (not file_path.is_absolute()):
        return self.get_relative_path_for_relative_file(file_path)
    return file_path.name
    ```

  Location 2 (validation_report_formatter.py:get_relative_path (lines 158-166)):
    ```python
    if file_path.is_absolute() and self.workspace_directory:
        return str(file_path.relative_to(self.workspace_directory))
    if self.workspace_directory and (not file_path.is_absolute()):
        return self._get_relative_path_for_relative_file(file_path)
    return file_path.name
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-19)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-21)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-20)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-22)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-21)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-23)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-22)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 272-276)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-22)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-24)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 268-272)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 269-273)):
    ```python
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanne...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 271-275)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 272-276)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-24)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 273-277)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-24)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-24)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-20)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-21)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-23)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-22)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-24)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 268-273)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 270-275)):
    ```python
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 271-276)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-24)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 272-277)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-24)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-24)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-21)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-22)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-24)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-22)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-22)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-23)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-24)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-23)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-25)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-23)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-26)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-24)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 271-277)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-24)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-24)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-26)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-23)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-24)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-23)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-23)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-24)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-26)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-24)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-24)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-97)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-70)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-97)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-71)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:93): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 93-98)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-71)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:93): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 93-98)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-71)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-98)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-70)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-98)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-71)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-98)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-71)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 103-107)):
    ```python
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 226-230)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(in...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:104): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 104-108)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 227-231)):
    ```python
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:104): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 104-108)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:105): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 105-109)):
    ```python
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, violations)
    status_suffix = f'({exec_status})' if exec_status != 'SUCCESS' else f'({s...
    ```

  Location 2 (validation_report_formatter.py:format_violation_line (lines 14-18)):
    ```python
    location = violation.get('location', 'unknown')
    message = violation.get('violation_message', 'No message')
    severity = violation.get('severity', 'error')
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:105): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 105-109)):
    ```python
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, violations)
    status_suffix = f'({exec_status})' if exec_status != 'SUCCESS' else f'({s...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 229-233)):
    ```python
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    violations_link = f' - [View Details](#{anchor_id}-violations)' if info.get('violations', 0) > 0 else ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:101): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 101-106)):
    ```python
    violations = rule_info['violations']
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = r...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rule_line (lines 74-78)):
    ```python
    violations = rule_info['violations']
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self._get_status_indicator(rule_info)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    anchor_link = self._rule_name_t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 103-108)):
    ```python
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 226-231)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(in...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:104): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 104-109)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-233)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:128): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_failed_rules_section (lines 128-139)):
    ```python
    if not failed_rules:
        return []
    lines = [f'### 🟥 {title}', '']
    for rule_info in failed_rules:
        rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
        anchor_link = self.rule_name_to_anchor(rule_name)
        rule_display_name = rule_name.replace('_', ' ').title()
        line...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_failed_rules (lines 95-106)):
    ```python
    if not failed_rules:
        return []
    lines = [f'### 🟥 {title}', '']
    for rule_info in failed_rules:
        rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
        anchor_link = self._rule_name_to_anchor(rule_name)
        rule_display_name = rule_name.replace('_', ' ').title()
        lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:173): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:_get_violation_status (lines 173-180)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```

  Location 2 (validation_scanner_status_builder.py:_get_overall_status (lines 143-155)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:173): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:_get_violation_status (lines 173-180)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```

  Location 2 (validation_scanner_status_builder.py:_get_overall_status (lines 138-155)):
    ```python
    if not stats.has_violations:
        return ('🟩', 'ALL CLEAN')
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ER...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:220): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_totals_summary (lines 220-227)):
    ```python
    lines = [f'**Total Rules:** {stats.total_rules}', f'- **Rules with Scanners:** {stats.total_with_scanners}', f'  - 🟩 **Executed Successfully:** {stats.executed_count}']
    if stats.load_failed_count > 0:
        lines.append(f'  - 🟥 **Load Failed:** {stats.load_failed_count}')
    if stats.execution_failed_cou...
    ```

  Location 2 (validation_scanner_status_builder.py:_build_totals_summary (lines 180-187)):
    ```python
    lines = [f'**Total Rules:** {stats.total_rules}', f'- **Rules with Scanners:** {stats.total_with_scanners}', f'  - 🟩 **Executed Successfully:** {stats.executed_count}']
    if stats.load_failed_count > 0:
        lines.append(f'  - 🟥 **Load Failed:** {stats.load_failed_count}')
    if stats.execution_failed_cou...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_executor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_executor.py:79): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_executor.py:_collect_scanner_status (lines 79-83)):
    ```python
    scanner_status = rule_dict.get('scanner_status', {})
    status = scanner_status.get('status', 'UNKNOWN')
    rule_file = rule_dict.get('rule_file', 'unknown')
    self._process_scanner_status(status, counts, scanner_status_lines, rule_file, scanner_status)
    ```

  Location 2 (validation_scanner_status_builder.py:_categorize_rules (lines 30-34)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    scanner_status = rule_dict.get('scanner_status', {})
    status = scanner_status.get('status', 'UNKNOWN')
    self._categorize_rule_by_status(status, rule_dict, rule_file, scanner_status, result)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-23)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-310)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-25)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-312)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-26)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-313)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-27)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-314)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-315)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-316)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-317)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-316)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-316)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-317)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 26-30)):
    ```python
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_line}']
    lines.extend(self._format_multiline_message_parts(parts[1:]))
    return lines
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 313-317)):
    ```python
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_line}']
    lines.extend(self.violation_formatter.format_multiline_message_parts(parts[1:]))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-25)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-312)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-26)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-313)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-27)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-314)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-28)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-315)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-28)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-316)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-315)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-316)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-316)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-317)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-316)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-317)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-317)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-26)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-26)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-27)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-314)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-27)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-315)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-316)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-317)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-315)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-316)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-316)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-317)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-315)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-316)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-317)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-317)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-26)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-315)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-314)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-317)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-315)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-316)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-317)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-315)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-316)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-317)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-317)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-27)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-315)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-316)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-316)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-317)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-315)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-316)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-317)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-317)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-28)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-315)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-28)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-316)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-315)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-316)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-316)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-317)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:_format_multiline_message_parts (lines 35-39)):
    ```python
    formatted = self._format_message_part(part, in_code_block)
    if part.strip().startswith('```'):
        in_code_block = not in_code_block
    result.append(formatted)
    ```

  Location 2 (violation_formatter.py:format_multiline_message_parts (lines 76-80)):
    ```python
    formatted = self._format_message_part(part, in_code_block)
    if part.strip().startswith('```'):
        in_code_block = not in_code_block
    result.append(formatted)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:268): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 268-272)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:269): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 269-273)):
    ```python
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanne...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:271): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 271-275)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:272): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 272-276)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:273): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 273-277)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:273): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 273-277)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:build_status_summary (lines 124-128)):
    ```python
    lines.append(f'### {overall_status} Overall Status: {overall_text}')
    lines.append('')
    lines.extend(self._build_summary_table(stats))
    lines.extend(self._build_totals_summary(stats))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:268): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 268-273)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:270): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 270-275)):
    ```python
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(sel...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:271): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 271-276)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:272): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 272-277)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:271): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 271-277)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-26)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```

## Validation Instructions

The following validation steps were performed:

1. **MANDATORY: Before validating any content, you MUST load and review the project's context files:**
2. 1. Load `{project_area}/docs/stories/clarification.json` - Contains key questions and evidence (generated file)
3. 2. Load `{project_area}/docs/stories/planning.json` - Contains assumptions and decisions (generated file)
4. 3. Load `{project_area}/docs/context/input.txt` (or similar) - Original input/source material if needed for validation (original context)
5. 
6. **CRITICAL: File locations:**
7. - **Generated files:** `{project_area}/docs/stories/clarification.json`, `{project_area}/docs/stories/planning.json`
8. - **Original input:** `{project_area}/docs/context/input.txt` and other original context files
9. 
10. These files contain critical requirements, decisions, and context that MUST be checked against during validation.
*... and 251 more instructions*

## Report Location

This report was automatically generated and saved to:
`C:\dev\augmented-teams\agile_bot\bots\base_bot\docs\stories\reports\code-validation-report-2025-12-22_13-16-16.md`

