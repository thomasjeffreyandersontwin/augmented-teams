# Validation Report - Code

**Generated:** 2025-12-24 02:32:58
**Project:** base_bot
**Behavior:** code
**Action:** validate

## Summary

Validated story map and domain model and 258 code file(s) against **32 validation rules**.

## Content Validated

- **Clarification:** `clarification.json`
- **Rendered Outputs:**
  - `story-graph.json`
- **Code Files Scanned:**
  - `src\actions\action.py`
  - `src\actions\action_context.py`
  - `src\actions\action_factory.py`
  - `src\actions\action_scope.py`
  - `src\actions\action_state_manager.py`
  - `src\actions\actions.py`
  - `src\actions\activity_tracker.py`
  - `src\actions\build\build_action.py`
  - `src\actions\build\build_scope.py`
  - `src\actions\build\knowledge.py`
  - `src\actions\build\knowledge_graph_spec.py`
  - `src\actions\build\knowledge_graph_template.py`
  - `src\actions\clarify\clarify_action.py`
  - `src\actions\clarify\evidence.py`
  - `src\actions\clarify\key_questions.py`
  - `src\actions\clarify\recommended_activities.py`
  - `src\actions\clarify\required_context.py`
  - `src\actions\clarify\requirements_clarifications.py`
  - `src\actions\content.py`
  - `src\actions\context_data_injector.py`
  - `src\actions\display\display_section.py`
  - `src\actions\display\list_items.py`
  - `src\actions\display\markdown_formatter.py`
  - `src\actions\guardrails.py`
  - `src\actions\help_action.py`
  - `src\actions\instructions.py`
  - `src\actions\render\evidence.py`
  - `src\actions\render\render_action.py`
  - `src\actions\render\render_config_loader.py`
  - `src\actions\render\render_instruction_formatter.py`
  - `src\actions\render\render_scope.py`
  - `src\actions\render\render_spec.py`
  - `src\actions\render\synchronizer.py`
  - `src\actions\render\template.py`
  - `src\actions\rules\rule.py`
  - `src\actions\rules\rule_filter.py`
  - `src\actions\rules\rule_loader.py`
  - `src\actions\rules\rules.py`
  - `src\actions\rules\rules_action.py`
  - `src\actions\rules\rules_digest_guidance.py`
  - `src\actions\scoping_parameter.py`
  - `src\actions\strategy\assumptions.py`
  - `src\actions\strategy\json_persistent.py`
  - `src\actions\strategy\recommended_activities.py`
  - `src\actions\strategy\strategy.py`
  - `src\actions\strategy\strategy_action.py`
  - `src\actions\strategy\strategy_criteria.py`
  - `src\actions\strategy\strategy_criterias.py`
  - `src\actions\strategy\strategy_decision.py`
  - `src\actions\validate\background_validation_handler.py`
  - `src\actions\validate\file_discovery.py`
  - `src\actions\validate\file_link_builder.py`
  - `src\actions\validate\knowledge_graph.py`
  - `src\actions\validate\path_resolver.py`
  - `src\actions\validate\scanner_status_formatter.py`
  - `src\actions\validate\scanners\useless_comments_scanner.py`
  - `src\actions\validate\story_graph.py`
  - `src\actions\validate\validate_action.py`
  - `src\actions\validate\validation_executor.py`
  - `src\actions\validate\validation_report_builder.py`
  - `src\actions\validate\validation_report_formatter.py`
  - `src\actions\validate\validation_report_writer.py`
  - `src\actions\validate\validation_scanner_status_builder.py`
  - `src\actions\validate\validation_scope.py`
  - `src\actions\validate\validation_stats.py`
  - `src\actions\validate\validation_violations_builder.py`
  - `src\actions\validate\violation_formatter.py`
  - `src\actions\workflow_status_builder.py`
  - `src\base_bot_cli.py`
  - `src\bot\behavior.py`
  - `src\bot\behaviors.py`
  - `src\bot\bot.py`
  - `src\bot\bot_paths.py`
  - `src\bot\merged_instructions.py`
  - `src\bot\reminders.py`
  - `src\bot\workspace.py`
  - `src\cli\action_data_collector.py`
  - `src\cli\base_bot_cli.py`
  - `src\cli\cli_action_parsers.py`
  - `src\cli\cli_code_visitor.py`
  - `src\cli\cli_command_router.py`
  - `src\cli\cli_context_builder.py`
  - `src\cli\cli_executor.py`
  - `src\cli\cli_generator.py`
  - `src\cli\cli_help_generator.py`
  - `src\cli\cli_help_renderer_visitor.py`
  - `src\cli\cli_parameter_parser.py`
  - `src\cli\cli_parser_generator.py`
  - `src\cli\cli_parser_generator_visitor.py`
  - `src\cli\cli_script_generator.py`
  - `src\cli\cursor\command_file_visitor.py`
  - `src\cli\cursor\command_generator.py`
  - `src\cli\cursor\command_renderer_visitor.py`
  - `src\cli\cursor\help_renderer_visitor.py`
  - `src\cli\cursor_command_file_visitor.py`
  - `src\cli\cursor_command_generator.py`
  - `src\cli\cursor_command_renderer_visitor.py`
  - `src\cli\cursor_help_renderer_visitor.py`
  - `src\cli\description_extractor.py`
  - `src\cli\formatter.py`
  - `src\cli\help_renderer.py`
  - `src\cli\mcp_code_visitor.py`
  - `src\cli\parameter_info_builder.py`
  - `src\cli\type_hint_converter.py`
  - `src\ext\behavior_matcher.py`
  - `src\ext\bot_matcher.py`
  - `src\ext\trigger_domain.py`
  - `src\ext\trigger_router.py`
  - `src\ext\trigger_router_entry.py`
  - `src\ext\trigger_words.py`
  - `src\generator\action_data_collector.py`
  - `src\generator\help_context.py`
  - `src\generator\orchestrator.py`
  - `src\generator\visitor.py`
  - `src\mcp\mcp_code_generator.py`
  - `src\mcp\mcp_code_visitor.py`
  - `src\mcp\mcp_config_generator.py`
  - `src\mcp\mcp_server.py`
  - `src\mcp\mcp_server_generator.py`
  - `src\mcp\server_deployer.py`
  - `src\mcp\server_restart.py`
  - `src\repl_cli\repl_commands\dot_notation.py`
  - `src\repl_cli\repl_commands\meta.py`
  - `src\repl_cli\repl_commands\navigation.py`
  - `src\repl_cli\repl_commands\repl_command.py`
  - `src\repl_cli\repl_commands\state.py`
  - `src\repl_cli\repl_commands\workflow.py`
  - `src\repl_cli\repl_help.py`
  - `src\repl_cli\repl_main.py`
  - `src\repl_cli\repl_results.py`
  - `src\repl_cli\repl_session.py`
  - `src\repl_cli\repl_status.py`
  - `src\scanners\abstraction_levels_scanner.py`
  - `src\scanners\ac_consolidation_scanner.py`
  - `src\scanners\active_language_scanner.py`
  - `src\scanners\arrange_act_assert_scanner.py`
  - `src\scanners\ascii_only_scanner.py`
  - `src\scanners\background_common_setup_scanner.py`
  - `src\scanners\bad_comments_scanner.py`
  - `src\scanners\behavioral_ac_scanner.py`
  - `src\scanners\business_readable_test_names_scanner.py`
  - `src\scanners\calculation_timing_code_scanner.py`
  - `src\scanners\calculation_timing_scanner.py`
  - `src\scanners\class_based_organization_scanner.py`
  - `src\scanners\class_size_scanner.py`
  - `src\scanners\clear_parameters_scanner.py`
  - `src\scanners\code_representation_code_scanner.py`
  - `src\scanners\code_representation_scanner.py`
  - `src\scanners\code_scanner.py`
  - `src\scanners\communication_verb_scanner.py`
  - `src\scanners\complete_refactoring_scanner.py`
  - `src\scanners\complexity_metrics.py`
  - `src\scanners\consistent_indentation_scanner.py`
  - `src\scanners\consistent_naming_scanner.py`
  - `src\scanners\consistent_vocabulary_scanner.py`
  - `src\scanners\cover_all_paths_scanner.py`
  - `src\scanners\delegation_code_scanner.py`
  - `src\scanners\delegation_scanner.py`
  - `src\scanners\dependency_chaining_code_scanner.py`
  - `src\scanners\dependency_chaining_scanner.py`
  - `src\scanners\descriptive_function_names_scanner.py`
  - `src\scanners\domain_concept_node.py`
  - `src\scanners\domain_grouping_code_scanner.py`
  - `src\scanners\domain_grouping_scanner.py`
  - `src\scanners\domain_language_code_scanner.py`
  - `src\scanners\domain_language_scanner.py`
  - `src\scanners\domain_scanner.py`
  - `src\scanners\duplication_scanner.py`
  - `src\scanners\encapsulation_scanner.py`
  - `src\scanners\enumerate_ac_permutations_scanner.py`
  - `src\scanners\enumerate_stories_scanner.py`
  - `src\scanners\error_handling_isolation_scanner.py`
  - `src\scanners\exact_variable_names_scanner.py`
  - `src\scanners\exception_classification_scanner.py`
  - `src\scanners\exception_handling_scanner.py`
  - `src\scanners\excessive_guards_scanner.py`
  - `src\scanners\exhaustive_decomposition_scanner.py`
  - `src\scanners\explicit_dependencies_scanner.py`
  - `src\scanners\fixture_placement_scanner.py`
  - `src\scanners\function_size_scanner.py`
  - `src\scanners\generic_capability_scanner.py`
  - `src\scanners\given_precondition_scanner.py`
  - `src\scanners\given_state_not_actions_scanner.py`
  - `src\scanners\given_when_then_helpers_scanner.py`
  - `src\scanners\implementation_details_scanner.py`
  - `src\scanners\import_placement_scanner.py`
  - `src\scanners\increment_folder_structure_scanner.py`
  - `src\scanners\intention_revealing_names_scanner.py`
  - `src\scanners\invest_principles_scanner.py`
  - `src\scanners\meaningful_context_scanner.py`
  - `src\scanners\minimize_mutable_state_scanner.py`
  - `src\scanners\mock_boundaries_scanner.py`
  - `src\scanners\natural_english_code_scanner.py`
  - `src\scanners\natural_english_scanner.py`
  - `src\scanners\no_fallbacks_scanner.py`
  - `src\scanners\no_guard_clauses_scanner.py`
  - `src\scanners\noun_redundancy_scanner.py`
  - `src\scanners\observable_behavior_scanner.py`
  - `src\scanners\one_concept_per_test_scanner.py`
  - `src\scanners\open_closed_principle_scanner.py`
  - `src\scanners\parameterized_tests_scanner.py`
  - `src\scanners\plain_english_scenarios_scanner.py`
  - `src\scanners\prefer_object_model_over_config_scanner.py`
  - `src\scanners\present_ac_consolidation_scanner.py`
  - `src\scanners\primitive_vs_object_scanner.py`
  - `src\scanners\property_encapsulation_code_scanner.py`
  - `src\scanners\property_encapsulation_scanner.py`
  - `src\scanners\real_implementations_scanner.py`
  - `src\scanners\resource_oriented_code_scanner.py`
  - `src\scanners\resource_oriented_design_scanner.py`
  - `src\scanners\resources\ast_elements.py`
  - `src\scanners\resources\block.py`
  - `src\scanners\resources\block_extractor.py`
  - `src\scanners\resources\file.py`
  - `src\scanners\resources\line.py`
  - `src\scanners\resources\scan.py`
  - `src\scanners\resources\scope.py`
  - `src\scanners\resources\violation.py`
  - `src\scanners\scanner.py`
  - `src\scanners\scanner_loader.py`
  - `src\scanners\scanner_orchestrator.py`
  - `src\scanners\scanner_registry.py`
  - `src\scanners\scenario_outline_scanner.py`
  - `src\scanners\scenario_specific_given_scanner.py`
  - `src\scanners\scenarios_cover_all_cases_scanner.py`
  - `src\scanners\scenarios_on_story_docs_scanner.py`
  - `src\scanners\separate_concerns_scanner.py`
  - `src\scanners\simplify_control_flow_scanner.py`
  - `src\scanners\single_responsibility_scanner.py`
  - `src\scanners\specification_match_scanner.py`
  - `src\scanners\specificity_scanner.py`
  - `src\scanners\spine_optional_scanner.py`
  - `src\scanners\story_enumeration_scanner.py`
  - `src\scanners\story_filename_scanner.py`
  - `src\scanners\story_graph_match_scanner.py`
  - `src\scanners\story_map.py`
  - `src\scanners\story_scanner.py`
  - `src\scanners\story_sizing_scanner.py`
  - `src\scanners\swallowed_exceptions_scanner.py`
  - `src\scanners\technical_abstraction_code_scanner.py`
  - `src\scanners\technical_abstraction_scanner.py`
  - `src\scanners\technical_language_scanner.py`
  - `src\scanners\test_boundary_behavior_scanner.py`
  - `src\scanners\test_file_naming_scanner.py`
  - `src\scanners\test_quality_scanner.py`
  - `src\scanners\test_scanner.py`
  - `src\scanners\third_party_isolation_scanner.py`
  - `src\scanners\type_safety_scanner.py`
  - `src\scanners\ubiquitous_language_scanner.py`
  - `src\scanners\unnecessary_parameter_passing_scanner.py`
  - `src\scanners\useless_comments_scanner.py`
  - `src\scanners\verb_noun_scanner.py`
  - `src\scanners\vertical_density_scanner.py`
  - `src\scanners\vertical_slice_scanner.py`
  - `src\scanners\violation.py`
  - `src\story_graph\domain.py`
  - `src\story_graph\nodes.py`
  - `src\utils.py`
  - **Total:** 258 src file(s)

## Scanner Execution Status

### 🟨 Overall Status: NEEDS ATTENTION

| Status | Count | Description |
|--------|-------|-------------|
| 🟩 Executed Successfully | 30 | Scanners ran without errors |
| 🟩 Clean Rules | 17 | No violations found |
| 🟨 Rules with Warnings | 7 | Found 76 warning violation(s) |
| 🟥 Rules with Errors | 5 | Found 2574 error violation(s) |
| [i] No Scanner | 2 | Rule has no scanner configured |

**Total Rules:** 32
- **Rules with Scanners:** 30
  - 🟩 **Executed Successfully:** 30
- [i] **Rules without Scanners:** 2

### 🟩 Successfully Executed Scanners

- 🟥 **[Eliminate Duplication](#eliminate-duplication)** - 2546 violation(s) (EXECUTION_SUCCESS) - [View Details](#eliminate-duplication-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.duplication_scanner.DuplicationScanner`
- 🟨 **[Use Domain Language](#use-domain-language)** - 52 violation(s) (EXECUTION_SUCCESS) - [View Details](#use-domain-language-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_language_code_scanner.DomainLanguageCodeScanner`
- 🟥 **[Stop Writing Useless Comments](#stop-writing-useless-comments)** - 22 violation(s) (EXECUTION_SUCCESS) - [View Details](#stop-writing-useless-comments-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.scanners.useless_comments_scanner.UselessCommentsScanner`
- 🟨 **[Keep Functions Small Focused](#keep-functions-small-focused)** - 8 violation(s) (EXECUTION_SUCCESS) - [View Details](#keep-functions-small-focused-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.function_size_scanner.FunctionSizeScanner`
- 🟨 **[Avoid Excessive Guards](#avoid-excessive-guards)** - 6 violation(s) (EXECUTION_SUCCESS) - [View Details](#avoid-excessive-guards-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.excessive_guards_scanner.ExcessiveGuardsScanner`
- 🟨 **[Use Clear Function Parameters](#use-clear-function-parameters)** - 5 violation(s) (EXECUTION_SUCCESS) - [View Details](#use-clear-function-parameters-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.clear_parameters_scanner.ClearParametersScanner`
- 🟥 **[Place Imports At Top](#place-imports-at-top)** - 4 violation(s) (EXECUTION_SUCCESS) - [View Details](#place-imports-at-top-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.import_placement_scanner.ImportPlacementScanner`
- 🟨 **[Maintain Vertical Density](#maintain-vertical-density)** - 3 violation(s) (EXECUTION_SUCCESS) - [View Details](#maintain-vertical-density-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.vertical_density_scanner.VerticalDensityScanner`
- 🟨 **[Provide Meaningful Context](#provide-meaningful-context)** - 2 violation(s) (EXECUTION_SUCCESS) - [View Details](#provide-meaningful-context-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.meaningful_context_scanner.MeaningfulContextScanner`
- 🟨 **[Simplify Control Flow](#simplify-control-flow)** - 2 violation(s) (EXECUTION_SUCCESS) - [View Details](#simplify-control-flow-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.simplify_control_flow_scanner.SimplifyControlFlowScanner`
- 🟨 **[Keep Classes Small With Single Responsibility](#keep-classes-small-with-single-responsibility)** - 1 violation(s) (EXECUTION_SUCCESS) - [View Details](#keep-classes-small-with-single-responsibility-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.class_size_scanner.ClassSizeScanner`
- 🟥 **[Never Swallow Exceptions](#never-swallow-exceptions)** - 1 violation(s) (EXECUTION_SUCCESS) - [View Details](#never-swallow-exceptions-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.swallowed_exceptions_scanner.SwallowedExceptionsScanner`
- 🟥 **[Use Resource Oriented Design](#use-resource-oriented-design)** - 1 violation(s) (EXECUTION_SUCCESS) - [View Details](#use-resource-oriented-design-violations)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.resource_oriented_code_scanner.ResourceOrientedCodeScanner`
- 🟩 **[Avoid Unnecessary Parameter Passing](#avoid-unnecessary-parameter-passing)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.scanners.unnecessary_parameter_passing_scanner.UnnecessaryParameterPassingScanner`
- 🟩 **[Chain Dependencies Properly](#chain-dependencies-properly)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.dependency_chaining_code_scanner.DependencyChainingCodeScanner`
- 🟩 **[Classify Exceptions By Caller Needs](#classify-exceptions-by-caller-needs)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_classification_scanner.ExceptionClassificationScanner`
- 🟩 **[Delegate To Lowest Level](#delegate-to-lowest-level)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.delegation_code_scanner.DelegationCodeScanner`
- 🟩 **[Enforce Encapsulation](#enforce-encapsulation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.encapsulation_scanner.EncapsulationScanner`
- 🟩 **[Favor Code Representation](#favor-code-representation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.code_representation_code_scanner.CodeRepresentationCodeScanner`
- 🟩 **[Group By Domain](#group-by-domain)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_grouping_code_scanner.DomainGroupingCodeScanner`
- 🟩 **[Hide Business Logic Behind Properties](#hide-business-logic-behind-properties)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
- 🟩 **[Hide Calculation Timing](#hide-calculation-timing)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
- 🟩 **[Keep Functions Single Responsibility](#keep-functions-single-responsibility)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.single_responsibility_scanner.SingleResponsibilityScanner`
- 🟩 **[Prefer Object Model Over Config](#prefer-object-model-over-config)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.scanners.prefer_object_model_over_config_scanner.PreferObjectModelOverConfigScanner`
- 🟩 **[Refactor Completely Not Partially](#refactor-completely-not-partially)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.complete_refactoring_scanner.CompleteRefactoringScanner`
- 🟩 **[Use Consistent Indentation](#use-consistent-indentation)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.consistent_indentation_scanner.ConsistentIndentationScanner`
- 🟩 **[Use Consistent Naming](#use-consistent-naming)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.consistent_naming_scanner.ConsistentNamingScanner`
- 🟩 **[Use Exceptions Properly](#use-exceptions-properly)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_handling_scanner.ExceptionHandlingScanner`
- 🟩 **[Use Explicit Dependencies](#use-explicit-dependencies)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.explicit_dependencies_scanner.ExplicitDependenciesScanner`
- 🟩 **[Use Natural English](#use-natural-english)** - 0 violations (EXECUTION_SUCCESS)
  - Scanner: `agile_bot.bots.base_bot.src.actions.validate.scanners.natural_english_code_scanner.NaturalEnglishCodeScanner`

### <span style="color: gray;">[i] Rules Without Scanners</span>

- <span style="color: gray;">[i]</span> **[Detect Legacy Unused Code](#detect-legacy-unused-code)** - No scanner configured
- <span style="color: gray;">[i]</span> **[Refactor Tests With Production Code](#refactor-tests-with-production-code)** - No scanner configured

## Validation Rules Checked

### 🟥 Rule: <span id="eliminate-duplication">Eliminate Duplication</span> - 2546 ERROR(S) - [View Details](#eliminate-duplication-violations)
**Description:** CRITICAL: Every piece of knowledge should have a single, authoritative representation (DRY principle). Extract repeated logic into reusable functions and use abstraction to capture common patterns.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.duplication_scanner.DuplicationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟥 Rule: <span id="stop-writing-useless-comments">Stop Writing Useless Comments</span> - 22 ERROR(S) - [View Details](#stop-writing-useless-comments-violations)
**Description:** CRITICAL: DO NOT WRITE COMMENTS. Delete all comments written by the AI chat. Code must be self-explanatory through clear naming and structure. ONLY exception: legal/license requirements. If you think a comment is needed, the code is wrong - fix the code instead.
**Scanner:** `agile_bot.bots.base_bot.src.actions.scanners.useless_comments_scanner.UselessCommentsScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟥 Rule: <span id="place-imports-at-top">Place Imports At Top</span> - 4 ERROR(S) - [View Details](#place-imports-at-top-violations)
**Description:** Place all import statements at the top of the file, after module docstrings and comments, but before any executable code. This improves readability and makes dependencies clear.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.import_placement_scanner.ImportPlacementScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟥 Rule: <span id="never-swallow-exceptions">Never Swallow Exceptions</span> - 1 ERROR(S) - [View Details](#never-swallow-exceptions-violations)
**Description:** CRITICAL: Never swallow exceptions silently. Empty catch blocks hide failures and make debugging impossible. Always log, handle, or rethrow exceptions with context.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.swallowed_exceptions_scanner.SwallowedExceptionsScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟥 Rule: <span id="use-resource-oriented-design">Use Resource Oriented Design</span> - 1 ERROR(S) - [View Details](#use-resource-oriented-design-violations)
**Description:** CRITICAL: Code must use resource-oriented, object-oriented design. Use object-oriented classes (singular or collection) with responsibilities that encapsulate logic over manager/doer/loader patterns. Maximize encapsulation through collaborator relationships.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.resource_oriented_code_scanner.ResourceOrientedCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="use-domain-language">Use Domain Language</span> - 52 WARNING(S) - [View Details](#use-domain-language-violations)
**Description:** CRITICAL: Code must use domain-specific language, not generic terms. NEVER use Dict[str, Any], List[str], or generic 'data'/'config'/'parameters' - use typed domain objects. Objects should expose properties representing what they contain (e.g., recommended_trades), not methods that 'generate' or 'calculate' things.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_language_code_scanner.DomainLanguageCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="keep-functions-small-focused">Keep Functions Small Focused</span> - 8 WARNING(S) - [View Details](#keep-functions-small-focused-violations)
**Description:** Functions should be small enough to understand at a glance. Keep functions under 20 lines when possible and extract complex logic into named helper functions.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.function_size_scanner.FunctionSizeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="avoid-excessive-guards">Avoid Excessive Guards</span> - 6 WARNING(S) - [View Details](#avoid-excessive-guards-violations)
**Description:** Excessive guard clauses add to cyclomatic complexity and make code harder to read. Centralize error handling in one place rather than scattering defensive checks throughout the code. Let code fail fast with clear errors rather than silently handling missing components.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.excessive_guards_scanner.ExcessiveGuardsScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="use-clear-function-parameters">Use Clear Function Parameters</span> - 5 WARNING(S) - [View Details](#use-clear-function-parameters-violations)
**Description:** CRITICAL: Function signatures must be simple and intention-revealing. Prefer 0-2 parameters. NEVER pass Dict[str, Any] or List[str] for complex data - create typed objects instead. Examples: parameters dict → ParametersObject, files dict → FilesCollection, exclude list → ExcludePatterns.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.clear_parameters_scanner.ClearParametersScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="provide-meaningful-context">Provide Meaningful Context</span> - 2 WARNING(S) - [View Details](#provide-meaningful-context-violations)
**Description:** Names should provide appropriate context without redundancy. Use longer names for longer scopes and replace magic numbers with named constants.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.meaningful_context_scanner.MeaningfulContextScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="simplify-control-flow">Simplify Control Flow</span> - 2 WARNING(S) - [View Details](#simplify-control-flow-violations)
**Description:** Keep nesting minimal and control flow straightforward. Use guard clauses to reduce nesting and extract nested blocks into separate functions.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.simplify_control_flow_scanner.SimplifyControlFlowScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟨 Rule: <span id="keep-classes-small-with-single-responsibility">Keep Classes Small With Single Responsibility</span> - 1 WARNING(S) - [View Details](#keep-classes-small-with-single-responsibility-violations)
**Description:** CRITICAL: Classes should be small (under 200-300 lines) with a single responsibility. Keep classes cohesive (methods/data interdependent), eliminate dead code, and favor many small focused classes over few large ones.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.class_size_scanner.ClassSizeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="avoid-unnecessary-parameter-passing">Avoid Unnecessary Parameter Passing</span> - CLEAN (0 violations)
**Description:** Don't pass parameters to internal methods when the value is already accessible through instance variables. Access instance properties directly instead of passing them around unnecessarily.
**Scanner:** `agile_bot.bots.base_bot.src.scanners.unnecessary_parameter_passing_scanner.UnnecessaryParameterPassingScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="chain-dependencies-properly">Chain Dependencies Properly</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must chain dependencies properly with constructor injection. Map dependencies in a chain: highest-level object → collaborator → sub-collaborator. Inject collaborators at construction time so methods can use them without passing them as parameters. Access sub-collaborators through their owning objects.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.dependency_chaining_code_scanner.DependencyChainingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="classify-exceptions-by-caller-needs">Classify Exceptions By Caller Needs</span> - CLEAN (0 violations)
**Description:** Design exceptions based on how callers will handle them. Create exception types based on caller's needs, use special case objects for predictable failures, and wrap third-party exceptions at boundaries.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.exception_classification_scanner.ExceptionClassificationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="delegate-to-lowest-level">Delegate To Lowest Level</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must delegate responsibilities to the lowest-level object that can handle them. If a collection class can do something, delegate to it rather than implementing it in the parent.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.delegation_code_scanner.DelegationCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="enforce-encapsulation">Enforce Encapsulation</span> - CLEAN (0 violations)
**Description:** CRITICAL: Hide implementation details and expose minimal interface. Make fields private by default, expose behavior not data. NEVER pass raw dicts/lists that expose internal structure - use typed objects that encapsulate the data. Follow Law of Demeter (principle of least knowledge).
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.encapsulation_scanner.EncapsulationScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="favor-code-representation">Favor Code Representation</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code should represent domain concepts directly. Domain models should match code. If code doesn't match domain concepts, refactor the code rather than creating abstract domain models.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.code_representation_code_scanner.CodeRepresentationCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="group-by-domain">Group By Domain</span> - CLEAN (0 violations)
**Description:** CRITICAL: Code must be organized by domain area and relationships, not by technical layers, object types, or architectural concerns.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.domain_grouping_code_scanner.DomainGroupingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

### 🟩 Rule: <span id="hide-business-logic-behind-properties">Hide Business Logic Behind Properties</span> - CLEAN (0 violations)
**Description:** CRITICAL: Hide business logic behind properties. Properties hide logic that occurs—it may be computed on-demand, cached, pre-computed, or loaded from storage. The caller shouldn't know or care when the values are calculated / determined.
**Scanner:** `agile_bot.bots.base_bot.src.actions.validate.scanners.calculation_timing_code_scanner.CalculationTimingCodeScanner`
**Execution Status:** EXECUTION_SUCCESS

*... and 12 more rules*

## Violations Found

**Total Violations:** 2653
- **File-by-File Violations:** 108
- **Cross-File Violations:** 2545

### File-by-File Violations (Pass 1)

These violations were detected by scanning each file individually.

#### <span id="avoid-excessive-guards-violations">Avoid Excessive Guards: 6 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\actions.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/actions.py:189): Line 189: None check guard clause detected. Assume variables are initialized - let code fail fast if None.

    ```python
        def is_final_action(self) -> bool:
            try:
                if self.current is None:
                    return False
                action_names = self.names
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\action_context.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/action_context.py:32): Line 32: Variable truthiness check detected (if not data:). Assume variable exists - let code fail fast if missing.

    ```python
        @classmethod
        def from_dict(cls, data: Dict[str, Any]) -> 'Scope':
            if not data:
                return cls()
            
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:344): Line 344: Variable truthiness check detected (if not args:). Assume variable exists - let code fail fast if missing.

    ```python
            """Parse --param value and --param "value with spaces" from command args."""
            params = {}
            if not args:
                return params
            
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:144): Line 144: Variable truthiness check detected (if changed:). Assume variable exists - let code fail fast if missing.

    ```python
            for file_type, file_list in files.items():
                changed = [f for f in file_list if f.stat().st_mtime > last_report_time]
                if changed:
                    changed_files[file_type] = changed
            
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:29): Line 29: Variable truthiness check detected (if not args:). Assume variable exists - let code fail fast if missing.

    ```python
            args = args.strip()
            
            if not args:
                output = self.help_resource.main_help
            else:
                if not self.has_current_behavior:
                    return self.error_no_current_behavior()
                action_help = self.help_resource.action_help(self.current_behavior_name, args)
                if not action_help:
                    behavior_help = self.help_resource.behavior_help(self.current_behavior_name)
                    if not behavior_help:
                        return self.error_behavior_not_found(self.current_behavior_name)
                    output = f"ERROR: Action '{args}' not found"
                else:
                    output = action_help.help_text
            
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:148): Line 148: Variable truthiness check detected (if not args:). Assume variable exists - let code fail fast if missing.

    ```python
        def execute(self, args: str = "") -> REPLCommandResponse:
            args = args.strip()
            if not args:
                return REPLCommandResponse(
                    output="ERROR: No scope specified",
                    response="ERROR: No scope specified",
                    status="error"
                )
            return REPLCommandResponse(
    ```

#### <span id="eliminate-duplication-violations">Eliminate Duplication: 2 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator_visitor.py:28): Duplicate code detected: functions visit_behavior, visit_action_help_section_header have identical bodies - extract to shared function
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\navigation.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/navigation.py:56): Duplicate code detected: functions _execute_instructions, _execute_instructions have identical bodies - extract to shared function

#### <span id="keep-classes-small-with-single-responsibility-violations">Keep Classes Small With Single Responsibility: 1 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:22): Class "REPLSession" is 382 lines - should be under 300 lines (extract related methods into separate classes)

```python


class REPLSession:
    STAGE_MAP = {
        'not_started': 'instructions',
        'instructions_given': 'instructions',
        'submitted': 'submitted'
    }
    
    def __init__(self, bot, workspace_directory: Path):
    # ... (truncated)
```

#### <span id="keep-functions-small-focused-violations">Keep Functions Small Focused: 8 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\actions.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/actions.py:15): Function "__init__" is 24 lines - should be under 20 lines (extract complex logic to helper functions)

    ```python
    class Actions:
    
        def __init__(self, behavior: 'Behavior'):
            self.behavior = behavior
            actions_list = behavior.actions_workflow
            
            # Separate workflow actions (have order) from non-workflow actions (no order)
            workflow_actions = [a for a in actions_list if a.get('order') is not None]
            non_workflow_actions = [a for a in actions_list if a.get('order') is None]
            
            # Sort workflow actions by order
            workflow_actions = sorted(workflow_actions, key=lambda x: x.get('order', 0))
            
            self._factory = ActionFactory(behavior)
            self._state_manager = ActionStateManager(behavior)
            
            # _actions contains only workflow actions (for sequencing)
            self._actions: List[Action] = []
            for action_dict in workflow_actions:
                action_name = action_dict.get('name', '')
                if action_name:
                    action_instance = self._factory.create_action_instance(action_name=action_name, action_config=action_dict)
                    self._actions.append(action_instance)
            
            # _non_workflow_actions contains actions that can be invoked but don't participate in workflow
            self._non_workflow_actions: List[Action] = []
            for action_dict in non_workflow_actions:
                action_name = action_dict.get('name', '')
                if action_name:
                    action_instance = self._factory.create_action_instance(action_name=action_name, action_config=action_dict)
                    self._non_workflow_actions.append(action_instance)
            
            self._current_index: Optional[int] = None
            self.load_state()
    
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\actions.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/actions.py:116): Function "navigate_to" is 24 lines - should be under 20 lines (extract complex logic to helper functions)

    ```python
            return self._state_manager.filter_completed_actions_after_target(completed_actions, target_index, self._actions)
    
        def navigate_to(self, action_name: str, out_of_order: bool=False):
            action = self.find_by_name(action_name)
            if action is None:
                raise ValueError(f"Action '{action_name}' not found")
            
            is_non_workflow = action in self._non_workflow_actions
            if is_non_workflow:
                # Non-workflow actions don't affect workflow state
                return
            
            target_index = None
            for i, a in enumerate(self._actions):
                if a.action_name == action_name:
                    target_index = i
                    self._current_index = i
                    break
            if not out_of_order or not self.behavior.bot_paths:
                self.save_state()
                return
            state_file = self._state_manager.get_state_file_path()
            state_data = json.loads(state_file.read_text(encoding='utf-8'))
            completed_actions = state_data.get('completed_actions', [])
            if completed_actions:
                state_data['completed_actions'] = self._filter_completed_actions_after_target(completed_actions, target_index)
                state_file.write_text(json.dumps(state_data, indent=2), encoding='utf-8')
            self.save_state()
    
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:105): Function "get_last_report_timestamp" is 22 lines - should be under 20 lines (extract complex logic to helper functions)

    ```python
            return rules_instance._rule_filter.filter_files(self.files, self.exclude)
    
        def get_last_report_timestamp(self) -> float:
            logger = logging.getLogger(__name__)
            docs_path = self.bot_paths.documentation_path
            reports_dir = self.bot_paths.workspace_directory / docs_path / 'reports'
            logger.info(f'Looking for previous reports in: {reports_dir}')
            if not reports_dir.exists():
                logger.info('Reports directory does not exist - returning 0.0')
                return 0.0
            
            report_files = list(reports_dir.glob(f'{self.behavior.name}-validation-status-*.md'))
            logger.info(f'Found {len(report_files)} report files')
            if not report_files:
                logger.info('No report files found - returning 0.0')
                return 0.0
            
            current_time = time.time()
            previous_run_files = [f for f in report_files if (current_time - f.stat().st_mtime) > 10]
            logger.info(f'Found {len(previous_run_files)} previous run files (excluding files < 10 seconds old)')
            
            if not previous_run_files:
                logger.info('No previous run files found - returning 0.0')
                return 0.0
            
            most_recent = max(previous_run_files, key=lambda p: p.stat().st_mtime)
            logger.info(f'Most recent previous report: {most_recent.name} (timestamp: {most_recent.stat().st_mtime})')
            return most_recent.stat().st_mtime
    
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:19): Function "execute_instructions" is 55 lines - should be under 20 lines (extract complex logic to helper functions)

    ```python
            return behavior.actions.find_by_name(action_name)
        
        def execute_instructions(self) -> REPLCommandResponse:
            """Execute the current action's get_instructions() method."""
            action = self.current_action
            if not action:
                return REPLCommandResponse(
                    output="ERROR: No current action",
                    response="ERROR: No current action",
                    status="error"
                )
            
            try:
                # Call the real action.get_instructions() method
                context = action.context_class()
                result = action.get_instructions(context)
                instructions_dict = result.get('instructions', {})
                
                # Format all instruction components
                output_lines = [
                    f"EXECUTING {self.current_behavior_name}.{self.current_action_name}.instructions",
                    ""
                ]
                
                # Add base instructions
                base_instructions = instructions_dict.get('base_instructions', [])
                output_lines.extend(base_instructions)
                
                # Add guardrails (questions and evidence) if present
                guardrails = instructions_dict.get('guardrails', {})
                if guardrails:
                    output_lines.append("")
                    output_lines.append("**GUARDRAILS:**")
                    
                    required_context = guardrails.get('required_context', {})
                    if required_context:
                        # Display key questions
                        key_questions = required_context.get('key_questions', {})
                        if key_questions:
                            output_lines.append("")
                            output_lines.append("**Required Key Questions:**")
                            for question_key, question_text in key_questions.items():
                                output_lines.append(f"- **{question_key}**: {question_text}")
                        
                        # Display evidence requirements
                        evidence = required_context.get('evidence', {})
                        if evidence:
                            output_lines.append("")
                            output_lines.append("**Required Evidence:**")
                            for evidence_key, evidence_desc in evidence.items():
        # ... (truncated)
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:105): Function "execute" is 21 lines - should be under 20 lines (extract complex logic to helper functions)

    ```python
            return True
        
        def execute(self, args: str = "") -> REPLCommandResponse:
            behavior_name = args.strip()
            if not behavior_name:
                return REPLCommandResponse(
                    output="ERROR: No behavior specified",
                    response="ERROR: No behavior specified",
                    status="error"
                )
            
            behavior = self.find_behavior(behavior_name)
            if not behavior:
                return self.error_behavior_not_found(behavior_name)
            
            if not behavior.actions.names:
                return REPLCommandResponse(
                    output=f"ERROR: behavior '{behavior_name}' has no actions",
                    response=f"ERROR: behavior '{behavior_name}' has no actions",
                    status="error"
                )
            
            self.bot.behaviors.navigate_to(behavior_name)
            first_action_name = behavior.actions.names[0]
            behavior.actions.navigate_to(first_action_name)
            return self.execute_instructions()
    
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:30): Function "execute_instructions" is 33 lines - should be under 20 lines (extract complex logic to helper functions)

    ```python
            return self.action_phase in ('not_started', 'instructions_given')
        
        def execute_instructions(self) -> REPLCommandResponse:
            """Execute the current action's get_instructions() method."""
            action = self.current_action
            if not action:
                return REPLCommandResponse(
                    output="ERROR: No current action",
                    response="ERROR: No current action",
                    status="error"
                )
            
            try:
                # Call the real action.get_instructions() method
                context = action.context_class()  # Create empty context for now
                result = action.get_instructions(context)
                instructions_dict = result.get('instructions', {})
                base_instructions = instructions_dict.get('base_instructions', [])
                
                # Build output with real instructions
                output_lines = [
                    f"EXECUTING {self.current_behavior_name}.{self.current_action_name}.instructions",
                    ""
                ]
                output_lines.extend(base_instructions)
                output_lines.append("")
                output_lines.append("Next: Provide your work using 'submit'.")
                
                output = "\n".join(output_lines)
                
                return REPLCommandResponse(
                    output=output, 
                    response=output, 
                    status="success", 
                    action=self.current_action_name
                )
            except Exception as e:
                error_msg = f"ERROR executing {self.current_action_name}.get_instructions(): {str(e)}"
                return REPLCommandResponse(
                    output=error_msg,
                    response=error_msg,
                    status="error",
                    action=self.current_action_name
                )
    
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:90): Function "execute" is 31 lines - should be under 20 lines (extract complex logic to helper functions)

    ```python
            return "submit"
        
        def execute(self, args: str = "") -> REPLCommandResponse:
            if not self.has_current_action:
                return self.error_no_current_action("submit for")
            
            action = self.current_action
            if not action:
                return REPLCommandResponse(
                    output="ERROR: No current action",
                    response="ERROR: No current action",
                    status="error"
                )
            
            try:
                # Call the real action.submit() method
                context = action.context_class()  # Create empty context for now
                result = action.submit(context)
                
                # Format output
                status = result.get('status', 'unknown')
                message = result.get('message', 'Work submitted')
                
                output = "\n".join([
                    f"EXECUTING {self.current_behavior_name}.{self.current_action_name}.submit",
                    "",
                    f"[{status.upper()}]",
                    f"- {message}",
                    "",
                    "Next: Type 'confirm' to mark complete and advance."
                ])
                
                return REPLCommandResponse(
                    output=output, 
                    response=output, 
                    status="success", 
                    action=self.current_action_name
                )
            except Exception as e:
                error_msg = f"ERROR executing {self.current_action_name}.submit(): {str(e)}"
                return REPLCommandResponse(
                    output=error_msg,
                    response=error_msg,
                    status="error",
                    action=self.current_action_name
                )
    
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:141): Function "execute" is 47 lines - should be under 20 lines (extract complex logic to helper functions)

    ```python
            return "confirm"
        
        def execute(self, args: str = "") -> REPLCommandResponse:
            if not self.has_current_action:
                return self.error_no_current_action("confirm")
            
            action = self.current_action
            behavior = self.current_behavior
            if not behavior or not action:
                return self.error_no_current_behavior()
            
            current_behavior_name = behavior.name
            current_action_name = action.action_name
            
            try:
                # Call the real action.confirm() method
                context = action.context_class()
                result = action.confirm(context)
                
                # Check if at last action BEFORE closing
                is_last_action = behavior.actions.next() is None
                
                # Mark current action as complete and advance
                behavior.actions.close_current()
                
                # If not at last action, show next action's instructions
                if not is_last_action:
                    return self.execute_instructions()
                
                # At last action - behavior is complete
                # Mark behavior as complete in state file
                self._mark_behavior_complete(current_behavior_name)
                
                # Check for next behavior BEFORE close_current since it advances the index
                next_behavior = self.bot.behaviors.next()
                
                if next_behavior:
                    # Advance to next behavior
                    self.bot.behaviors.close_current()
                    # Navigate to next behavior's first action
                    self.bot.behaviors.navigate_to(next_behavior.name)
                    if next_behavior.actions.names:
                        next_behavior.actions.navigate_to(next_behavior.actions.names[0])
                        return self.execute_instructions()
                
                # No more behaviors - all complete
                return REPLCommandResponse(
                    output=f"COMPLETE: {current_behavior_name} behavior finished\n\nALL BEHAVIORS COMPLETE!",
                    response="COMPLETE: All behaviors finished",
                    status="success"
        # ... (truncated)
    ```

#### <span id="maintain-vertical-density-violations">Maintain Vertical Density: 3 violation(s)</span>

- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:244): Function "_execute_action_with_args" is 82 lines - consider improving vertical density by declaring variables near usage

    ```python
                return args_str.split()
        
        def _execute_action_with_args(self, action_name: str, cli_args: list, operation: str = None) -> REPLCommandResponse:
            """Execute action with parsed CLI arguments."""
            if not self.has_current_behavior:
                return REPLCommandResponse(
                    output="ERROR: No current behavior set. Please select a behavior first.",
                    response="ERROR: No current behavior set",
                    status="error"
                )
        # ... (truncated)
    ```
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:19): Function "execute_instructions" is 75 lines - consider improving vertical density by declaring variables near usage

    ```python
            return behavior.actions.find_by_name(action_name)
        
        def execute_instructions(self) -> REPLCommandResponse:
            """Execute the current action's get_instructions() method."""
            action = self.current_action
            if not action:
                return REPLCommandResponse(
                    output="ERROR: No current action",
                    response="ERROR: No current action",
                    status="error"
        # ... (truncated)
    ```
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:141): Function "execute" is 57 lines - consider improving vertical density by declaring variables near usage

    ```python
            return "confirm"
        
        def execute(self, args: str = "") -> REPLCommandResponse:
            if not self.has_current_action:
                return self.error_no_current_action("confirm")
            
            action = self.current_action
            behavior = self.current_behavior
            if not behavior or not action:
                return self.error_no_current_behavior()
        # ... (truncated)
    ```

#### <span id="never-swallow-exceptions-violations">Never Swallow Exceptions: 1 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:211): Except block only contains pass at line 211 - exceptions must be logged or rethrown, never swallowed

    ```python
                state_data['completed_behaviors'] = completed
                state_file.write_text(json.dumps(state_data, indent=2))
            except (json.JSONDecodeError, IOError):
                pass
    
    ```

#### <span id="place-imports-at-top-violations">Place Imports At Top: 4 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:12): Import statement found after non-import code. Move all imports to the top of the file.

    ```python
        TTYDetectionResult
    )
    from agile_bot.bots.base_bot.src.repl_cli.repl_help import REPLHelp
    from agile_bot.bots.base_bot.src.repl_cli.repl_status import REPLStatus
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:13): Import statement found after non-import code. Move all imports to the top of the file.

    ```python
    )
    from agile_bot.bots.base_bot.src.repl_cli.repl_help import REPLHelp
    from agile_bot.bots.base_bot.src.repl_cli.repl_status import REPLStatus
    from agile_bot.bots.base_bot.src.repl_cli.repl_commands import (
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:14): Import statement found after non-import code. Move all imports to the top of the file.

    ```python
    from agile_bot.bots.base_bot.src.repl_cli.repl_help import REPLHelp
    from agile_bot.bots.base_bot.src.repl_cli.repl_status import REPLStatus
    from agile_bot.bots.base_bot.src.repl_cli.repl_commands import (
        register_commands,
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:19): Import statement found after non-import code. Move all imports to the top of the file.

    ```python
        DotNotationCommand
    )
    from agile_bot.bots.base_bot.src.actions.action_context import Scope, ScopeType
    
    ```

#### <span id="provide-meaningful-context-violations">Provide Meaningful Context: 2 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:222): Line 222 uses numbered variable "s1" - use meaningful descriptive name

    ```python
            import re
            s1 = re.sub('(.)([A-Z][a-z]+)', r'\1_\2', name)
            return re.sub('([a-z0-9])([A-Z])', r'\1_\2', s1).lower()
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\cli_parser_generator_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator_visitor.py:231): Line 231 uses numbered variable "s1" - use meaningful descriptive name

    ```python
            import re
            s1 = re.sub('(.)([A-Z][a-z]+)', r'\1_\2', name)
            return re.sub('([a-z0-9])([A-Z])', r'\1_\2', s1).lower()
    ```

#### <span id="simplify-control-flow-violations">Simplify Control Flow: 2 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\type_hint_converter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/type_hint_converter.py:4): Function "to_cli_type" has nesting depth of 4 - use guard clauses and extract nested blocks to reduce nesting

    ```python
        
        @staticmethod
        def to_cli_type(field_type) -> str:
            type_str = str(field_type)
            if 'Dict' in type_str:
                return 'dict'
            elif 'List' in type_str:
                return 'list'
            elif 'bool' in type_str:
                return 'flag'
            elif 'Scope' in type_str:
                return 'dict'
            return 'str'
    
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:19): Function "execute_instructions" has nesting depth of 5 - use guard clauses and extract nested blocks to reduce nesting

    ```python
            return behavior.actions.find_by_name(action_name)
        
        def execute_instructions(self) -> REPLCommandResponse:
            """Execute the current action's get_instructions() method."""
            action = self.current_action
            if not action:
                return REPLCommandResponse(
                    output="ERROR: No current action",
                    response="ERROR: No current action",
                    status="error"
                )
            
            try:
                # Call the real action.get_instructions() method
                context = action.context_class()
        # ... (truncated)
    ```

#### <span id="stop-writing-useless-comments-violations">Stop Writing Useless Comments: 22 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\generator\orchestrator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/orchestrator.py:93): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def generate_for_all_actions(self) -> None:
            """Traverse all (behavior, action) pairs - used for code generation."""
            self.visitor.visit_header(self.bot_name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\generator\orchestrator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/orchestrator.py:101): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def _visit_behavior_action(self, behavior, action) -> None:
            """Visit a specific behavior-action pair with full object access."""
            context = ActionHelpContext(
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:237): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def _tokenize_cli_args(self, args_str: str) -> list:
            """Tokenize CLI-style arguments, handling quoted strings."""
            import shlex
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:245): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def _execute_action_with_args(self, action_name: str, cli_args: list, operation: str = None) -> REPLCommandResponse:
            """Execute action with parsed CLI arguments."""
            if not self.has_current_behavior:
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:353): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def parse_command_parameters(self, args: str) -> Dict[str, Any]:
            """Parse --param value and --param "value with spaces" from command args."""
            params = {}
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:368): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def parse_scope_from_string(self, scope_str: str) -> Optional[Scope]:
            """Parse scope JSON/dict string into Scope object."""
            if not scope_str:
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:379): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def get_stored_scope(self) -> Optional[Dict[str, Any]]:
            """Get scope parameters from behavior action state file."""
            state_file = self._get_state_file_path()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:390): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def store_scope_parameters(self, scope: Scope) -> None:
            """Store scope parameters in behavior action state file."""
            state_file = self._get_state_file_path()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:402): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def _get_state_file_path(self) -> Path:
            """Get the path to behavior_action_state.json."""
            return self.workspace_directory / 'behavior_action_state.json'
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:372): Useless comment: "# Handle Python-style dict syntax" - delete it or improve the code instead

    ```python
                return None
            try:
                # Handle Python-style dict syntax
                data = json.loads(scope_str.replace("'", '"'))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\dot_notation.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/dot_notation.py:6): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
    
    class DotNotationCommand(REPLCommand):
        """Handles dot notation commands like behavior.action or behavior.action.operation."""
        
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:6): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
    
    class MetaCommand(REPLCommand):
        """Base for meta commands - provides access to help and status resources."""
        
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:91): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def _execute_instructions(self) -> REPLCommandResponse:
            """Execute the current action's get_instructions() method."""
            action = self.current_action
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\navigation.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/navigation.py:6): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
    
    class NavigationCommand(REPLCommand):
        """Base for navigation commands - provides navigation-specific state."""
        
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\navigation.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/navigation.py:83): Useless comment: "# Get previous action" - delete it or improve the code instead

    ```python
                return self.error_no_current_behavior()
            
            # Get previous action
            prev_action = behavior.actions.previous()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:7): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
    
    class StateCommand(REPLCommand):
        """Base for state commands - provides access to behavior/action lookup."""
        
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:20): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def execute_instructions(self) -> REPLCommandResponse:
            """Execute the current action's get_instructions() method."""
            action = self.current_action
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:189): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def _update_state_file(self, workspace_path: str) -> None:
            """Update behavior_action_state.json with working_directory."""
            state_file = self.session.workspace_directory / 'behavior_action_state.json'
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:179): Useless comment: "# Update state file with working_directory" - delete it or improve the code instead

    ```python
                )
            
            # Update state file with working_directory
            self._update_state_file(workspace_path)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:8): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
    
    class WorkflowCommand(REPLCommand):
        """Base for workflow commands - provides action phase/state properties."""
        
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:31): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def execute_instructions(self) -> REPLCommandResponse:
            """Execute the current action's get_instructions() method."""
            action = self.current_action
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:200): Useless docstring that repeats function/class name - delete it or explain WHY, not WHAT

    ```python
        
        def _mark_behavior_complete(self, behavior_name: str) -> None:
            """Add behavior to completed_behaviors in state file."""
            state_file = self.session.workspace_directory / 'behavior_action_state.json'
    ```

#### <span id="use-clear-function-parameters-violations">Use Clear Function Parameters: 5 violation(s)</span>

- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:293): Function "_process_scanner_result" has 7 parameters - consider using existing domain objects with properties instead of passing primitives. Extend domain objects (Behaviors, Behavior, Actions, RenderSpec, etc.) with properties that encapsulate the needed data rather than creating new parameter objects.

    ```python
                return data
    
        def _process_scanner_result(self, rule, rule_result: dict, scanner_results: Any, scanner_path: str, scanner_name: str, logger) -> str:
            timestamp = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
            execution_status = rule.scanner_execution_status or 'SUCCESS'
        # ... (truncated)
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:309): Function "_execute_scanner" has 9 parameters - consider using existing domain objects with properties instead of passing primitives. Extend domain objects (Behaviors, Behavior, Actions, RenderSpec, etc.) with properties that encapsulate the needed data rather than creating new parameter objects.

    ```python
            return f'  [OK] {rule.rule_file}: Scanner executed successfully ({violations_count} violations)'
    
        def _execute_scanner(self, rule, rule_result: dict, context: ValidationContext, scanner_path: str, logger, files: Dict, changed_files: Dict, all_files: Dict) -> str:
            scanner_name = scanner_path.split('.')[-1] if '.' in scanner_path else scanner_path
            timestamp = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
        # ... (truncated)
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:329): Function "_process_rule" has 8 parameters - consider using existing domain objects with properties instead of passing primitives. Extend domain objects (Behaviors, Behavior, Actions, RenderSpec, etc.) with properties that encapsulate the needed data rather than creating new parameter objects.

    ```python
                raise
    
        def _process_rule(self, rule, rule_result: dict, context: ValidationContext, logger, files: Dict, changed_files: Dict, all_files: Dict) -> str:
            scanner_path = rule.scanner_path
            if not scanner_path:
        # ... (truncated)
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:341): Function "validate" has 6 parameters - consider using existing domain objects with properties instead of passing primitives. Extend domain objects (Behaviors, Behavior, Actions, RenderSpec, etc.) with properties that encapsulate the needed data rather than creating new parameter objects.

    ```python
            return self._execute_scanner(rule, rule_result, context, scanner_path, logger, files, changed_files, all_files)
    
        def validate(self, context: ValidationContext, files: Optional[Dict[str, List[Path]]]=None, callbacks: Optional[ValidationCallbacks]=None, skiprule: Optional[List[str]]=None, exclude: Optional[List[str]]=None) -> List[Dict[str, Any]]:
            if isinstance(context, ValidationContext):
                return self._execute_validation(context)
        # ... (truncated)
    ```
- <span style="color: orange;">[!]</span> **WARNING** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:346): Function "_create_legacy_context" has 6 parameters - consider using existing domain objects with properties instead of passing primitives. Extend domain objects (Behaviors, Behavior, Actions, RenderSpec, etc.) with properties that encapsulate the needed data rather than creating new parameter objects.

    ```python
            return self._execute_validation(self._create_legacy_context(context, files, callbacks, skiprule, exclude))
    
        def _create_legacy_context(self, knowledge_graph: Dict, files: Optional[Dict], callbacks: Optional[ValidationCallbacks], skiprule: Optional[List[str]], exclude: Optional[List[str]]) -> ValidationContext:
            return ValidationContext(knowledge_graph=knowledge_graph, files=files or {}, callbacks=callbacks or ValidationCallbacks(), skiprule=skiprule or [], exclude=exclude or [], skip_cross_file=True, all_files=False, behavior=self.behavior, bot_paths=getattr(self, 'bot_paths', None), working_dir=Path.cwd())
    
    ```

#### <span id="use-domain-language-violations">Use Domain Language: 52 violation(s)</span>

- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\actions.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/actions.py:97): Function "previous" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\actions.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/actions.py:103): Function "__iter__" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\action_context.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/action_context.py:31): Function "from_dict" uses parameter name "cls" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_context_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_context_builder.py:24): Function "build_context" uses parameter name "cli_args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:44): Function "_add_header" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:124): Function "_add_bool_argument" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:131): Function "_add_optional_bool_argument" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:142): Function "_add_dict_argument" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: orange;">[!]</span> **WARNING** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:226): Function "generate_parsers_for_story_bot" uses generate/calculate. Use property instead (e.g., "recommended_trades" not "generate_recommendation").
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_parser_generator_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator_visitor.py:59): Function "_add_header" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_parser_generator_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator_visitor.py:138): Function "_add_bool_argument" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_parser_generator_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator_visitor.py:145): Function "_add_optional_bool_argument" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\cli\cli_parser_generator_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator_visitor.py:156): Function "_add_dict_argument" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: orange;">[!]</span> **WARNING** - [`src\generator\orchestrator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/orchestrator.py:92): Function "generate_for_all_actions" uses generate/calculate. Use property instead (e.g., "recommended_trades" not "generate_recommendation").
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:132): Function "display_current_state" uses parameter name "full" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:187): Function "_handle_action_shortcut" uses parameter name "args_str" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:236): Function "_tokenize_cli_args" uses parameter name "args_str" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:244): Function "_execute_action_with_args" uses parameter name "cli_args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_session.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_session.py:352): Function "parse_command_parameters" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\build\build_scope.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/build/build_scope.py:16): Function "from_context" uses parameter name "cls" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:38): Function "from_action_context" uses parameter name "cls" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:66): Function "from_parameters" uses parameter name "cls" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:190): Function "__iter__" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:195): Function "__len__" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:279): Function "_flush_logger_handlers" uses parameter name "logger" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:293): Function "_process_scanner_result" uses parameter name "logger" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:309): Function "_execute_scanner" uses parameter name "logger" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:329): Function "_process_rule" uses parameter name "logger" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:355): Function "_log_validation_start" uses parameter name "logger" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:370): Function "_process_all_rules" uses parameter name "logger" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\actions\rules\rules.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rules.py:396): Function "_log_scanner_status_summary" uses parameter name "logger" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\dot_notation.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/dot_notation.py:14): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:23): Function "takes_args" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:26): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:51): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:65): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:79): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\meta.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/meta.py:152): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\navigation.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/navigation.py:31): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\navigation.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/navigation.py:75): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\navigation.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/navigation.py:114): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:102): Function "takes_args" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:105): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:137): Function "takes_args" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:140): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:167): Function "takes_args" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:170): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:206): Function "takes_args" doesn't match domain terms. Use domain-specific language from specification: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\state.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/state.py:209): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:79): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:90): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...
- <span style="color: blue;">[i]</span> **INFO** - [`src\repl_cli\repl_commands\workflow.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/repl_cli/repl_commands/workflow.py:141): Function "execute" uses parameter name "args" that doesn't match domain terms. Use domain-specific language: action, action confirmation, action data collector, action executor, action help context, action instructions, action_confirmation, action_data_collector, action_executor, action_help_context...

### Cross-File Violations (Pass 2)

These violations were detected by analyzing all files together to find patterns that span multiple files.

#### <span id="eliminate-duplication-violations">Eliminate Duplication: 2544 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/action.py:205): Duplicate code detected across files - extract to shared function.

  Location 1 (action.py:_handle_execution_error (lines 205-209)):
    ```python
    error_type = type(e).__name__
    error_message = str(e)
    full_traceback = traceback.format_exc()
    error_instructions = self._build_error_instructions(error_type, error_message, full_traceback, parameters)
    self.track_activity_on_completion(outputs={'error': error_message})
    ```

  Location 2 (mcp_code_visitor.py:visit_behavior (lines 26-31)):
    ```python
    behavior_name = context.behavior_name
    trigger_patterns = self.trigger_words_map.get(behavior_name, [])
    description = self._build_behavior_tool_description(behavior_name, trigger_patterns)
    tool_code = self._generate_behavior_tool_code(behavior_name, description)
    self.tool_registrations.append(tool_co...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/action.py:205): Duplicate code detected across files - extract to shared function.

  Location 1 (action.py:_handle_execution_error (lines 205-209)):
    ```python
    error_type = type(e).__name__
    error_message = str(e)
    full_traceback = traceback.format_exc()
    error_instructions = self._build_error_instructions(error_type, error_message, full_traceback, parameters)
    self.track_activity_on_completion(outputs={'error': error_message})
    ```

  Location 2 (mcp_code_visitor.py:visit_behavior (lines 53-58)):
    ```python
    behavior_name = context.behavior_name
    trigger_patterns = self.trigger_words_map.get(behavior_name, [])
    description = self._build_behavior_tool_description(behavior_name, trigger_patterns)
    tool_code = self._generate_behavior_tool_code(behavior_name, description)
    self.tool_registrations.append(tool_co...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-171)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-47)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-171)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-47)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-175)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:167): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 167-177)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-175)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-51)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-175)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-175)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-51)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-175)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:164): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 164-177)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-177)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-51)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-177)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-177)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-51)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\help_action.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/help_action.py:163): Duplicate code detected across files - extract to shared function.

  Location 1 (help_action.py:_get_parameters_from_context_class (lines 163-177)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-38)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-39)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 36-40)):
    ```python
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-39)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', ...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 319-331)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 34-39)):
    ```python
    self.description = self._config.get('description', '')
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', ...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-40)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 319-331)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\bot\behavior.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/bot/behavior.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (behavior.py:_initialize_from_config (lines 35-40)):
    ```python
    self.goal = self._config.get('goal', '')
    self.inputs = self._config.get('inputs', [])
    self.outputs = self._config.get('outputs', [])
    self.instructions = self._config.get('instructions', {})
    self.trigger_words = self._config.get('trigger_words', [])
    self.order = self._config.get('order', 999)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:sort_behaviors_for_display (lines 25-31)):
    ```python
    behaviors_list = list(behaviors)
    behaviors_with_order = []
    for behavior in behaviors_list:
        order = self.get_behavior_order(behavior)
        behaviors_with_order.append((order, behavior))
    behaviors_with_order.sort(key=lambda x: x[0])
    return [behavior for _, behavior in behaviors_with_order]
    ```

  Location 2 (action_data_collector.py:sort_behaviors_for_display (lines 25-31)):
    ```python
    behaviors_list = list(behaviors)
    behaviors_with_order = []
    for behavior in behaviors_list:
        order = self.get_behavior_order(behavior)
        behaviors_with_order.append((order, behavior))
    behaviors_with_order.sort(key=lambda x: x[0])
    return [behavior for _, behavior in behaviors_with_order]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 39-47)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-47)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 39-51)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-51)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 39-51)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-51)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 43-53)):
    ```python
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
        type_hint = TypeHintConverter.t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 40-53)):
    ```python
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class):
        cli_name = f"--{field_info.name.replace('_', '-')}"
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 39-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-51)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:get_action_parameters (lines 39-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```

  Location 2 (action_data_collector.py:get_action_parameters (lines 39-53)):
    ```python
    action_class = ActionFactory.get_action_class(action_name)
    if not action_class:
        return []
    context_class = getattr(action_class, 'context_class', None)
    if not context_class or not dataclasses.is_dataclass(context_class):
        return []
    params = []
    for field_info in dataclasses.fields(context_class...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:_get_single_parameter_description (lines 65-74)):
    ```python
    if 'key_questions_answered' in param:
        return 'Dict mapping question keys to answer strings'
    if 'evidence_provided' in param:
        return 'Dict mapping evidence types to evidence content'
    if 'decisions_made' in param:
        return 'Dict mapping decision criteria keys to selected options/values'
    if '...
    ```

  Location 2 (action_data_collector.py:_get_single_parameter_description (lines 65-74)):
    ```python
    if 'key_questions_answered' in param:
        return 'Dict mapping question keys to answer strings'
    if 'evidence_provided' in param:
        return 'Dict mapping evidence types to evidence content'
    if 'decisions_made' in param:
        return 'Dict mapping decision criteria keys to selected options/values'
    if '...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:67): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:_get_single_parameter_description (lines 67-75)):
    ```python
    if 'evidence_provided' in param:
        return 'Dict mapping evidence types to evidence content'
    if 'decisions_made' in param:
        return 'Dict mapping decision criteria keys to selected options/values'
    if 'assumptions_made' in param:
        return 'List of assumption strings'
    if 'scope' in param:
        ret...
    ```

  Location 2 (action_data_collector.py:_get_single_parameter_description (lines 67-75)):
    ```python
    if 'evidence_provided' in param:
        return 'Dict mapping evidence types to evidence content'
    if 'decisions_made' in param:
        return 'Dict mapping decision criteria keys to selected options/values'
    if 'assumptions_made' in param:
        return 'List of assumption strings'
    if 'scope' in param:
        ret...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:67): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:_get_single_parameter_description (lines 67-75)):
    ```python
    if 'evidence_provided' in param:
        return 'Dict mapping evidence types to evidence content'
    if 'decisions_made' in param:
        return 'Dict mapping decision criteria keys to selected options/values'
    if 'assumptions_made' in param:
        return 'List of assumption strings'
    if 'scope' in param:
        ret...
    ```

  Location 2 (action_data_collector.py:_get_single_parameter_description (lines 65-75)):
    ```python
    if 'key_questions_answered' in param:
        return 'Dict mapping question keys to answer strings'
    if 'evidence_provided' in param:
        return 'Dict mapping evidence types to evidence content'
    if 'decisions_made' in param:
        return 'Dict mapping decision criteria keys to selected options/values'
    if '...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\action_data_collector.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/action_data_collector.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (action_data_collector.py:_get_single_parameter_description (lines 65-75)):
    ```python
    if 'key_questions_answered' in param:
        return 'Dict mapping question keys to answer strings'
    if 'evidence_provided' in param:
        return 'Dict mapping evidence types to evidence content'
    if 'decisions_made' in param:
        return 'Dict mapping decision criteria keys to selected options/values'
    if '...
    ```

  Location 2 (action_data_collector.py:_get_single_parameter_description (lines 65-75)):
    ```python
    if 'key_questions_answered' in param:
        return 'Dict mapping question keys to answer strings'
    if 'evidence_provided' in param:
        return 'Dict mapping evidence types to evidence content'
    if 'decisions_made' in param:
        return 'Dict mapping decision criteria keys to selected options/values'
    if '...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_action_parsers.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_action_parsers.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_action_parsers.py:build_validate_action_context_parser (lines 40-44)):
    ```python
    parser.add_argument('--scope', type=str, default=None, help='JSON scope config: {type, value, exclude, skiprule}')
    parser.add_argument('--background', action='store_true', default=None)
    parser.add_argument('--skip-cross-file', action='store_true', default=False)
    parser.add_argument('--all-files', ac...
    ```

  Location 2 (cli_parameter_parser.py:_create_argument_parser (lines 44-48)):
    ```python
    parser.add_argument('-h', '--help', action='store_true', help='Show this help message and exit')
    parser.add_argument('--skip-cross-file', action='store_true', help='Skip cross-file duplicate checking (default: False, meaning cross-file check runs)')
    parser.add_argument('--all-files', action='store_t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_action_parsers.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_action_parsers.py:41): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_action_parsers.py:build_validate_action_context_parser (lines 41-45)):
    ```python
    parser.add_argument('--background', action='store_true', default=None)
    parser.add_argument('--skip-cross-file', action='store_true', default=False)
    parser.add_argument('--all-files', action='store_true', default=False)
    parser.add_argument('--force-full', action='store_true', default=False)
    return pa...
    ```

  Location 2 (cli_parameter_parser.py:_create_argument_parser (lines 45-49)):
    ```python
    parser.add_argument('--skip-cross-file', action='store_true', help='Skip cross-file duplicate checking (default: False, meaning cross-file check runs)')
    parser.add_argument('--all-files', action='store_true', help='Scan all files instead of only changed files (default: False, meaning incremental sca...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_action_parsers.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_action_parsers.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_action_parsers.py:build_validate_action_context_parser (lines 40-45)):
    ```python
    parser.add_argument('--scope', type=str, default=None, help='JSON scope config: {type, value, exclude, skiprule}')
    parser.add_argument('--background', action='store_true', default=None)
    parser.add_argument('--skip-cross-file', action='store_true', default=False)
    parser.add_argument('--all-files', ac...
    ```

  Location 2 (cli_parameter_parser.py:_create_argument_parser (lines 44-49)):
    ```python
    parser.add_argument('-h', '--help', action='store_true', help='Show this help message and exit')
    parser.add_argument('--skip-cross-file', action='store_true', help='Skip cross-file duplicate checking (default: False, meaning cross-file check runs)')
    parser.add_argument('--all-files', action='store_t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_python_cli_script (lines 33-37)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    cli_file = src_dir / f'{self.bot.bot_name}_cli.py'
    cli_code = self._build_python_cli_code()
    ```

  Location 2 (cli_script_generator.py:generate_python_cli_script (lines 13-17)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    cli_file = src_dir / f'{self.bot_name}_cli.py'
    cli_code = f'''#!/usr/bin/env python3\n"""\n{self.bot_name.title().replace('_', ' ')} CLI Entry Point\n\nCommand-line interface for {s...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_python_cli_script (lines 33-37)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    cli_file = src_dir / f'{self.bot.bot_name}_cli.py'
    cli_code = self._build_python_cli_code()
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-84)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_python_cli_script (lines 33-37)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    cli_file = src_dir / f'{self.bot.bot_name}_cli.py'
    cli_code = self._build_python_cli_code()
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-112)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:41): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_build_python_cli_code (lines 41-52)):
    ```python
    docstring = self._build_cli_docstring()
    imports = self._build_cli_imports()
    bootstrap = self._build_cli_bootstrap()
    main_function = self._build_cli_main_function()
    return f'#!/usr/bin/env python3\n{docstring}\n{imports}\n\n{bootstrap}\n\n{main_function}\n'
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 177-189)):
    ```python
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_code()
    return '\n\n'.join([bot_tool_code, get_working_dir_cod...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:41): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_build_python_cli_code (lines 41-52)):
    ```python
    docstring = self._build_cli_docstring()
    imports = self._build_cli_imports()
    bootstrap = self._build_cli_bootstrap()
    main_function = self._build_cli_main_function()
    return f'#!/usr/bin/env python3\n{docstring}\n{imports}\n\n{bootstrap}\n\n{main_function}\n'
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 205-217)):
    ```python
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_code()
    return '\n\n'.join([bot_tool_code, get_working_dir_cod...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_code_visitor.py:150): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_code_visitor.py:_create_powershell_script (lines 150-163)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot.bot_name}_cli.ps1'
    script_content = f"""# {self.bot.bot_name.title().replace('_', ' ')} CLI Wrapper (PowerShell)\n\n    $SCRIPT_DIR = Split-Path -Parent $MyInvocation.MyCommand.Path\n\n    if (-not $env:WORKING_DIR...
    ```

  Location 2 (cli_script_generator.py:generate_powershell_script (lines 31-35)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    script_file = bot_dir / f'{self.bot_name}_cli.ps1'
    script_content = f"""# {self.bot_name.title().replace('_', ' ')} CLI Wrapper (PowerShell)\n\n    $SCRIPT_DIR = Split-Path -Parent $MyInvocation.MyCommand.Path\n\n    if (-not $env:WORKING_DIR) {{\n  ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer_visitor.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer_visitor.py:visit_behavior (lines 46-50)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 38-42)):
    ```python
    print(f'\n{self._format_behavior_title(context)}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer_visitor.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer_visitor.py:visit_behavior (lines 47-51)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 39-43)):
    ```python
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    print()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer_visitor.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer_visitor.py:visit_behavior (lines 46-51)):
    ```python
    print(f'\n## {context.bot_name}-{context.behavior_name}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(f'python {self.cli_script_path} --behavior {context.behavior_name} --action <{action_list}> [context]')
    print()
    ```

  Location 2 (help_renderer.py:render_behavior_section (lines 38-43)):
    ```python
    print(f'\n{self._format_behavior_title(context)}\n')
    print(f'{context.behavior_description}\n')
    print('```')
    action_list = '|'.join(context.actions)
    print(self._format_behavior_command(context, action_list))
    print()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer_visitor.py:74): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer_visitor.py:_print_parameters (lines 74-79)):
    ```python
    param_desc = context.parameter_descriptions.get(param, 'Optional parameter')
    if '\n' in param_desc:
        self._print_multiline_parameter(param, param_desc)
    else:
        print(f'{param}:   {param_desc}')
    ```

  Location 2 (cursor_help_renderer_visitor.py:_add_parameters (lines 89-94)):
    ```python
    param_desc = context.parameter_descriptions.get(param, 'Optional parameter')
    if '\n' in param_desc:
        self._add_multiline_parameter(param, param_desc)
    else:
        self._add_line(f'{param}:   {param_desc}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_help_renderer_visitor.py:74): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_help_renderer_visitor.py:_print_parameters (lines 74-79)):
    ```python
    param_desc = context.parameter_descriptions.get(param, 'Optional parameter')
    if '\n' in param_desc:
        self._print_multiline_parameter(param, param_desc)
    else:
        print(f'{param}:   {param_desc}')
    ```

  Location 2 (help_renderer_visitor.py:_add_parameters (lines 89-94)):
    ```python
    param_desc = context.parameter_descriptions.get(param, 'Optional parameter')
    if '\n' in param_desc:
        self._add_multiline_parameter(param, param_desc)
    else:
        self._add_line(f'{param}:   {param_desc}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:232): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:generate_parsers_for_story_bot (lines 232-238)):
    ```python
    workspace_root = Path(__file__).parent.parent.parent.parent.parent
    os.environ['BOT_DIRECTORY'] = str(story_bot_dir)
    os.environ['WORKING_AREA'] = str(workspace_root)
    bot = Bot('story_bot', story_bot_dir, config_path)
    generator = CliParserGenerator()
    ```

  Location 2 (orchestrator.py:_visit_behavior (lines 38-49)):
    ```python
    behavior_name = behavior.name
    behavior_description = self.data_collector.get_behavior_description(behavior_name)
    actions = self.data_collector.get_behavior_actions(behavior)
    additional_options = self._get_additional_options(behavior_name)
    context = BehaviorHelpContext(bot_name=self.bot_name, behavio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 80-88)):
    ```python
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._g...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 94-102)):
    ```python
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._gener...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 81-89)):
    ```python
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._generated_lines.append('    return parser')
    self._generated_lines.append('')
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 95-103)):
    ```python
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._generated_lines.append('    return parser')
    self._generated_lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 81-89)):
    ```python
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._generated_lines.append('    return parser')
    self._generated_lines.append('')
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 94-103)):
    ```python
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._gener...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 77-86)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 92-100)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:79): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 79-88)):
    ```python
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 94-102)):
    ```python
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._gener...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 80-89)):
    ```python
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._g...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 94-103)):
    ```python
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._gener...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 76-86)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parse...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 91-100)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 76-86)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parse...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 91-102)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 77-88)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 92-102)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 77-88)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 92-103)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:79): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 79-89)):
    ```python
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 94-103)):
    ```python
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fields:
        self._add_argument_for_field(field_info)
    self._gener...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 76-88)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parse...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 91-100)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 76-88)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parse...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 91-102)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 76-88)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parse...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 91-103)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 77-89)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 92-102)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 77-89)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 92-103)):
    ```python
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(context_class)
    for field_info in all_fi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 76-89)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parse...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 91-102)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_parser_function (lines 76-89)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    """Build argument parser for {class_name}."""')
    self._generated_lines.append(f'    parse...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_parser_function (lines 91-103)):
    ```python
    class_name = context_class.__name__
    func_name = f'build_{self._to_snake_case(class_name)}_parser'
    self._generated_lines.append(f'def {func_name}() -> argparse.ArgumentParser:')
    self._generated_lines.append(f'    parser = argparse.ArgumentParser(add_help=False)')
    all_fields = self._get_all_fields(con...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:97): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_add_argument_for_field (lines 97-104)):
    ```python
    name = field_info.name
    field_type = field_info.type
    default = field_info.default
    cli_name = f"--{name.replace('_', '-')}"
    if field_type == bool:
        self._add_bool_argument(cli_name, default)
        return
    ```

  Location 2 (cli_parser_generator_visitor.py:_add_argument_for_field (lines 111-118)):
    ```python
    name = field_info.name
    field_type = field_info.type
    default = field_info.default
    cli_name = f"--{name.replace('_', '-')}"
    if field_type == bool:
        self._add_bool_argument(cli_name, default)
        return
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:198): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_action_parser_mapping (lines 198-210)):
    ```python
    self._generated_lines.append('ACTION_PARSERS = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._genera...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_action_parser_mapping (lines 209-220)):
    ```python
    self._generated_lines.append('ACTION_PARSERS = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._genera...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:207): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_action_parser_mapping (lines 207-218)):
    ```python
    self._generated_lines.append('')
    self._generated_lines.append('# Mapping from (behavior, action) to context class')
    self._generated_lines.append('ACTION_CONTEXT_CLASSES = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        self._generated_lines.append(f"    ('{behavior_nam...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_action_parser_mapping (lines 217-227)):
    ```python
    self._generated_lines.append('}')
    self._generated_lines.append('')
    self._generated_lines.append('ACTION_CONTEXT_CLASSES = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {context_class_name},")
    se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:198): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_action_parser_mapping (lines 198-211)):
    ```python
    self._generated_lines.append('ACTION_PARSERS = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._genera...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_action_parser_mapping (lines 209-220)):
    ```python
    self._generated_lines.append('ACTION_PARSERS = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._genera...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:200): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_action_parser_mapping (lines 200-216)):
    ```python
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._generated_lines.append('}')
    self._generated_lines.append(...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_action_parser_mapping (lines 211-225)):
    ```python
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._generated_lines.append('}')
    self._generated_lines.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:206): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_action_parser_mapping (lines 206-218)):
    ```python
    self._generated_lines.append('}')
    self._generated_lines.append('')
    self._generated_lines.append('# Mapping from (behavior, action) to context class')
    self._generated_lines.append('ACTION_CONTEXT_CLASSES = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        self._generated_...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_action_parser_mapping (lines 217-227)):
    ```python
    self._generated_lines.append('}')
    self._generated_lines.append('')
    self._generated_lines.append('ACTION_CONTEXT_CLASSES = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {context_class_name},")
    se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:198): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_action_parser_mapping (lines 198-216)):
    ```python
    self._generated_lines.append('ACTION_PARSERS = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._genera...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_action_parser_mapping (lines 209-225)):
    ```python
    self._generated_lines.append('ACTION_PARSERS = {')
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._genera...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_parser_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_parser_generator.py:200): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_parser_generator.py:_generate_action_parser_mapping (lines 200-218)):
    ```python
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._generated_lines.append('}')
    self._generated_lines.append(...
    ```

  Location 2 (cli_parser_generator_visitor.py:_generate_action_parser_mapping (lines 211-227)):
    ```python
    for behavior_name, action_name, context_class_name in action_mappings:
        func_name = f'build_{self._to_snake_case(context_class_name)}_parser'
        self._generated_lines.append(f"    ('{behavior_name}', '{action_name}'): {func_name},")
    self._generated_lines.append('}')
    self._generated_lines.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_script_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_script_generator.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_script_generator.py:generate_python_cli_script (lines 13-17)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    cli_file = src_dir / f'{self.bot_name}_cli.py'
    cli_code = f'''#!/usr/bin/env python3\n"""\n{self.bot_name.title().replace('_', ' ')} CLI Entry Point\n\nCommand-line interface for {s...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-84)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_script_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_script_generator.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (cli_script_generator.py:generate_python_cli_script (lines 13-17)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    cli_file = src_dir / f'{self.bot_name}_cli.py'
    cli_code = f'''#!/usr/bin/env python3\n"""\n{self.bot_name.title().replace('_', ' ')} CLI Entry Point\n\nCommand-line interface for {s...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-112)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_get_current_command_files (lines 60-67)):
    ```python
    bot_prefix = f'{self.bot_name}'
    existing_files = set()
    for file_path in self.commands_dir.glob(f'{bot_prefix}*.md'):
        existing_files.add(file_path)
    return existing_files
    ```

  Location 2 (command_file_visitor.py:_get_current_command_files (lines 89-96)):
    ```python
    bot_prefix = f'{self.bot_name}'
    existing_files = set()
    for file_path in self.commands_dir.glob(f'{bot_prefix}*.md'):
        existing_files.add(file_path)
    return existing_files
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-179)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-180)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-207)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-208)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 70-86)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:74): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 74-90)):
    ```python
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self._build_help_command_content()
    self.commands[f'{self.bot_name}-help'] = self._write_command_file(self.commands_dir / f'{self....
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 103-119)):
    ```python
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self._build_help_command_content()
    self.commands[f'{self.bot_name}-help'] = self._write_command_file(self.commands_dir / f'{self....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_base_commands (lines 78-94)):
    ```python
    help_content = self._build_help_command_content()
    self.commands[f'{self.bot_name}-help'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-help.md', help_content)
    self.commands[f'{self.bot_name}-get_working_dir'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-get_wor...
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 107-123)):
    ```python
    help_content = self._build_help_command_content()
    self.commands[f'{self.bot_name}-help'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-help.md', help_content)
    self.commands[f'{self.bot_name}-get_working_dir'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-get_wor...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:129): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_generate_action_help_for_behavior (lines 129-138)):
    ```python
    command_visitor = CursorCommandRendererVisitor(self.python_command, behavior_name, lines, bot=self.bot)
    orchestrator = Orchestrator(command_visitor)
    orchestrator.visit_actions_for_behavior(behavior_name)
    self._visit_rules_action(command_visitor, behavior_name)
    command_visitor.visit_footer()
    ```

  Location 2 (command_file_visitor.py:_generate_action_help_for_behavior (lines 158-167)):
    ```python
    command_visitor = CursorCommandRendererVisitor(self.python_command, behavior_name, lines, bot=self.bot)
    orchestrator = Orchestrator(command_visitor)
    orchestrator.visit_actions_for_behavior(behavior_name)
    self._visit_rules_action(command_visitor, behavior_name)
    command_visitor.visit_footer()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:145): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_visit_rules_action (lines 145-157)):
    ```python
    data_collector = visitor.data_collector
    action_description = data_collector.get_action_description('rules')
    parameters = data_collector.get_action_parameters('rules')
    parameter_descriptions = data_collector.get_parameter_descriptions('rules', parameters)
    context = ActionHelpContext(bot_name=self.bot...
    ```

  Location 2 (orchestrator.py:_visit_behavior (lines 38-50)):
    ```python
    behavior_name = behavior.name
    behavior_description = self.data_collector.get_behavior_description(behavior_name)
    actions = self.data_collector.get_behavior_actions(behavior)
    additional_options = self._get_additional_options(behavior_name)
    context = BehaviorHelpContext(bot_name=self.bot_name, behavio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_file_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_file_visitor.py:145): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_file_visitor.py:_visit_rules_action (lines 145-157)):
    ```python
    data_collector = visitor.data_collector
    action_description = data_collector.get_action_description('rules')
    parameters = data_collector.get_action_parameters('rules')
    parameter_descriptions = data_collector.get_parameter_descriptions('rules', parameters)
    context = ActionHelpContext(bot_name=self.bot...
    ```

  Location 2 (command_file_visitor.py:_visit_rules_action (lines 174-186)):
    ```python
    data_collector = visitor.data_collector
    action_description = data_collector.get_action_description('rules')
    parameters = data_collector.get_action_parameters('rules')
    parameter_descriptions = data_collector.get_parameter_descriptions('rules', parameters)
    context = ActionHelpContext(bot_name=self.bot...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:update_bot_registry (lines 35-39)):
    ```python
    registry_path = self._get_registry_path()
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry...
    ```

  Location 2 (command_generator.py:update_bot_registry (lines 35-39)):
    ```python
    registry_path = self._get_registry_path()
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:update_bot_registry (lines 35-39)):
    ```python
    registry_path = self._get_registry_path()
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry...
    ```

  Location 2 (command_generator.py:update_bot_registry (lines 35-40)):
    ```python
    registry_path = self._get_registry_path()
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:update_bot_registry (lines 36-40)):
    ```python
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry, indent=2, sort_keys=True), encoding='utf...
    ```

  Location 2 (command_generator.py:update_bot_registry (lines 36-40)):
    ```python
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry, indent=2, sort_keys=True), encoding='utf...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:update_bot_registry (lines 35-40)):
    ```python
    registry_path = self._get_registry_path()
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry...
    ```

  Location 2 (command_generator.py:update_bot_registry (lines 35-39)):
    ```python
    registry_path = self._get_registry_path()
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_generator.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_generator.py:update_bot_registry (lines 35-40)):
    ```python
    registry_path = self._get_registry_path()
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry...
    ```

  Location 2 (command_generator.py:update_bot_registry (lines 35-40)):
    ```python
    registry_path = self._get_registry_path()
    registry = self._load_registry(registry_path)
    rel_cli_path = self._get_relative_cli_path(cli_script_path)
    registry[self.bot_name] = {'trigger_patterns': self._load_bot_trigger_patterns(), 'cli_path': rel_cli_path}
    registry_path.write_text(json.dumps(registry...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 50-59)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 50-59)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 50-59)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 50-60)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 51-60)):
    ```python
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (No optional parameters)')
        self.output_lines.append...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 51-60)):
    ```python
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (No optional parameters)')
        self.output_lines.append...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:52): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 52-61)):
    ```python
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (No optional parameters)')
        self.output_lines.append('')
        return
    self._append_parameter_help(context)
    self._append_exam...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 52-61)):
    ```python
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (No optional parameters)')
        self.output_lines.append('')
        return
    self._append_parameter_help(context)
    self._append_exam...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 50-60)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 50-59)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 50-60)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 50-60)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 50-60)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 50-61)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 51-61)):
    ```python
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (No optional parameters)')
        self.output_lines.append...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 51-61)):
    ```python
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (No optional parameters)')
        self.output_lines.append...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 50-61)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 50-60)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:visit_action (lines 50-61)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```

  Location 2 (command_renderer_visitor.py:visit_action (lines 50-61)):
    ```python
    short_desc = self._extract_short_description(context)
    self.output_lines.append(f'### {context.action_name} - {short_desc}')
    self.output_lines.append(f'{self.python_command} --behavior {self.behavior_name} --action {context.action_name}')
    if not context.parameters:
        self.output_lines.append('  # (...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:69): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_parameter_help (lines 69-74)):
    ```python
    self.output_lines.append(f'  # Optional: {param}')
    if param in context.parameter_descriptions:
        desc = context.parameter_descriptions[param]
        for desc_line in desc.split('\n'):
            self.output_lines.append(f'  #   {desc_line}')
    ```

  Location 2 (command_renderer_visitor.py:_append_parameter_help (lines 69-74)):
    ```python
    self.output_lines.append(f'  # Optional: {param}')
    if param in context.parameter_descriptions:
        desc = context.parameter_descriptions[param]
        for desc_line in desc.split('\n'):
            self.output_lines.append(f'  #   {desc_line}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_example_command (lines 77-83)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example:')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_p...
    ```

  Location 2 (command_renderer_visitor.py:_append_example_command (lines 77-83)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example (bash/sh):')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_example_command (lines 77-83)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example:')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_p...
    ```

  Location 2 (command_renderer_visitor.py:_append_example_command (lines 77-84)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example (bash/sh):')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_example_command (lines 77-83)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example:')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_p...
    ```

  Location 2 (command_renderer_visitor.py:_append_example_command (lines 77-85)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example (bash/sh):')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_example_command (lines 78-84)):
    ```python
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example:')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_params)}"
    self.output_lines.append(f'  # {example_cmd}')
    ```

  Location 2 (command_renderer_visitor.py:_append_example_command (lines 78-84)):
    ```python
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example (bash/sh):')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_params)}"
    self.output_lines.append(f'  # {example_cmd}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_example_command (lines 78-84)):
    ```python
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example:')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_params)}"
    self.output_lines.append(f'  # {example_cmd}')
    ```

  Location 2 (command_renderer_visitor.py:_append_example_command (lines 78-85)):
    ```python
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example (bash/sh):')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_params)}"
    self.output_lines.append(f'  # {example_cmd}')
    se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_example_command (lines 77-84)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example:')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_p...
    ```

  Location 2 (command_renderer_visitor.py:_append_example_command (lines 77-83)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example (bash/sh):')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_example_command (lines 77-84)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example:')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_p...
    ```

  Location 2 (command_renderer_visitor.py:_append_example_command (lines 77-84)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example (bash/sh):')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_append_example_command (lines 77-84)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example:')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join(example_p...
    ```

  Location 2 (command_renderer_visitor.py:_append_example_command (lines 77-85)):
    ```python
    example_params = self._build_example_params(context.parameters[:2])
    if not example_params:
        return
    self.output_lines.append('  #')
    self.output_lines.append('  # Full example (bash/sh):')
    example_cmd = f"{self.python_command} --behavior {self.behavior_name} --action {context.action_name} {' '.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:88): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_build_example_params (lines 88-97)):
    ```python
    param_name = param.split()[0]
    if '<dict>' in param:
        example_params.append(f"""{param_name} '{{"key": "value"}}'""")
    elif '<list>' in param:
        example_params.append(f'{param_name} "value1" "value2"')
    elif '<flag>' in param:
        example_params.append(param_name)
    else:
        example_params.append(f...
    ```

  Location 2 (command_renderer_visitor.py:_build_example_params (lines 94-103)):
    ```python
    param_name = param.split()[0]
    if '<dict>' in param:
        example_params.append(f"""{param_name} '{{"key": "value"}}'""")
    elif '<list>' in param:
        example_params.append(f'{param_name} "value1" "value2"')
    elif '<flag>' in param:
        example_params.append(param_name)
    else:
        example_params.append(f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_command_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_command_renderer_visitor.py:88): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_command_renderer_visitor.py:_build_example_params (lines 88-97)):
    ```python
    param_name = param.split()[0]
    if '<dict>' in param:
        example_params.append(f"""{param_name} '{{"key": "value"}}'""")
    elif '<list>' in param:
        example_params.append(f'{param_name} "value1" "value2"')
    elif '<flag>' in param:
        example_params.append(param_name)
    else:
        example_params.append(f...
    ```

  Location 2 (command_renderer_visitor.py:_build_example_params_powershell (lines 108-117)):
    ```python
    param_name = param.split()[0]
    if '<dict>' in param:
        example_params.append(f'{param_name}="{{`"key`": `"value`"}}"')
    elif '<list>' in param:
        example_params.append(f'{param_name}="value1" "value2"')
    elif '<flag>' in param:
        example_params.append(param_name)
    else:
        example_params.append(f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_header (lines 50-54)):
    ```python
    name = bot_name if bot_name is not None else self.bot_name
    self._add_line(f'## Available Cursor Commands for {name}:')
    self._add_line('')
    self._add_line('---')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_header (lines 50-54)):
    ```python
    name = bot_name if bot_name is not None else self.bot_name
    self._add_line(f'## Available Cursor Commands for {name}:')
    self._add_line('')
    self._add_line('---')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-61)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-61)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-61)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-62)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-62)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-62)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-62)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-80)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-63)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-63)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-63)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-63)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-64)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-64)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-64)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-64)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-64)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-65)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-65)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-65)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-65)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-66)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-66)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-66)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-66)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-67)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-67)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-67)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-67)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-67)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:64): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 64-71)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 64-71)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 65-72)):
    ```python
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context.additional_options.items():
            self._add_line(f'    ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 65-72)):
    ```python
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context.additional_options.items():
            self._add_line(f'    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:66): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 66-73)):
    ```python
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context.additional_options.items():
            self._add_line(f'           {option}  {d...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 66-73)):
    ```python
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context.additional_options.items():
            self._add_line(f'           {option}  {d...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-62)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-61)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-62)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-62)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-63)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-63)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-63)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-63)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-63)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-64)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-64)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-64)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-64)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-64)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-64)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-64)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-65)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-65)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-66)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-67)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-66)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-67)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-67)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-67)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-67)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-67)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-71)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-71)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-71)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-71)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:64): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 64-72)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 64-72)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:64): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 64-72)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 64-73)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 65-73)):
    ```python
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context.additional_options.items():
            self._add_line(f'    ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 65-73)):
    ```python
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context.additional_options.items():
            self._add_line(f'    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 65-73)):
    ```python
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context.additional_options.items():
            self._add_line(f'    ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 64-73)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-63)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-63)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-63)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-64)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-63)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-65)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-63)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-64)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-64)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-65)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-66)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-67)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-71)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-71)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-73)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:64): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 64-73)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 64-72)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:64): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 64-73)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 65-73)):
    ```python
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context.additional_options.items():
            self._add_line(f'    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:64): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 64-73)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 64-73)):
    ```python
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')
        for option, description in context....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-64)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-63)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-64)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-64)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-64)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-65)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-64)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-66)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-71)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-73)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-72)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-73)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-73)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 63-73)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-73)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:63): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 63-73)):
    ```python
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('           Additional options:')...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-65)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-63)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-65)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-64)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-65)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-65)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-65)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-66)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-64)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-65)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-65)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-66)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-67)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-71)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-71)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:62): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-66)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-64)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-66)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-65)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:57): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 57-66)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 57-66)):
    ```python
    cmd_name = f'{context.bot_name}-{context.behavior_name}'
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-66)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-67)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-66)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-67)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-67)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}'...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-71)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-71)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-71)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-71)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:59): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 59-71)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-71)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 59-71)):
    ```python
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:60): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-71)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-72)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-72)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 62-73)):
    ```python
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._add_line('          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 60-72)):
    ```python
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:61): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 61-73)):
    ```python
    self._add_line('')
    self._add_line('```')
    action_list = '|'.join(context.actions)
    self._add_line(f'/{cmd_name} <{action_list}> <context>')
    self._add_line('')
    self._add_line(f'action:   {action_list}')
    self._add_line('context:  Optional context or file path')
    if context.additional_options:
        self._a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-80)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_behavior (lines 58-62)):
    ```python
    self._add_line(f'## {cmd_name}')
    self._add_line('')
    self._add_line(f'{context.behavior_description}')
    self._add_line('')
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-80)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-80)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-81)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-81)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-81)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-81)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 78-84)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 78-84)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:79): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 79-85)):
    ```python
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 79-85)):
    ```python
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 80-86)):
    ```python
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 80-86)):
    ```python
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-81)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-81)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-81)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-81)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-81)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-84)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-84)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-84)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-84)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-84)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-84)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 78-85)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 78-85)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 78-85)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 78-85)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 78-86)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:79): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 79-86)):
    ```python
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 79-86)):
    ```python
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:79): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 79-86)):
    ```python
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 78-86)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-84)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-81)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-84)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-84)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-84)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-84)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-84)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-85)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-84)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 78-85)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-85)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-86)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 78-86)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 78-85)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 78-86)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 79-86)):
    ```python
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 78-86)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 78-86)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 78-86)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-86)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-85)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-84)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-85)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-85)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-85)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-85)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-86)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-86)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self....
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-85)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-86)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self....
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 78-86)):
    ```python
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self._add_line('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-86)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self....
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-86)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 77-86)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self....
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-86)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-86)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-85)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-86)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 77-86)):
    ```python
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_parameters(context)
    self._add_line('```')
    self....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:76): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:visit_action (lines 76-86)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```

  Location 2 (help_renderer_visitor.py:visit_action (lines 76-86)):
    ```python
    self._add_line(f'### {context.action_name}')
    self._add_line('')
    self._add_line(f'{context.action_description}')
    self._add_line('')
    self._add_line('```')
    self._add_line(f'/{context.bot_name}-<behavior> {context.action_name} [parameters]')
    if context.parameters:
        self._add_line('')
        self._add_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cursor_help_renderer_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cursor_help_renderer_visitor.py:89): Duplicate code detected across files - extract to shared function.

  Location 1 (cursor_help_renderer_visitor.py:_add_parameters (lines 89-94)):
    ```python
    param_desc = context.parameter_descriptions.get(param, 'Optional parameter')
    if '\n' in param_desc:
        self._add_multiline_parameter(param, param_desc)
    else:
        self._add_line(f'{param}:   {param_desc}')
    ```

  Location 2 (help_renderer_visitor.py:_add_parameters (lines 89-94)):
    ```python
    param_desc = context.parameter_descriptions.get(param, 'Optional parameter')
    if '\n' in param_desc:
        self._add_multiline_parameter(param, param_desc)
    else:
        self._add_line(f'{param}:   {param_desc}')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:visit_behavior (lines 26-31)):
    ```python
    behavior_name = context.behavior_name
    trigger_patterns = self.trigger_words_map.get(behavior_name, [])
    description = self._build_behavior_tool_description(behavior_name, trigger_patterns)
    tool_code = self._generate_behavior_tool_code(behavior_name, description)
    self.tool_registrations.append(tool_co...
    ```

  Location 2 (mcp_code_visitor.py:visit_behavior (lines 53-58)):
    ```python
    behavior_name = context.behavior_name
    trigger_patterns = self.trigger_words_map.get(behavior_name, [])
    description = self._build_behavior_tool_description(behavior_name, trigger_patterns)
    tool_code = self._generate_behavior_tool_code(behavior_name, description)
    self.tool_registrations.append(tool_co...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-56)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```

  Location 2 (mcp_code_visitor.py:_load_trigger_words (lines 77-84)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-56)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```

  Location 2 (mcp_code_visitor.py:_load_trigger_words (lines 77-85)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-56)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 114-121)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-56)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 113-121)):
    ```python
    self._ensure_bot_initialized()
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-56)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 114-122)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 50-57)):
    ```python
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_code_visitor.py:_load_trigger_words (lines 78-85)):
    ```python
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 50-57)):
    ```python
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 115-122)):
    ```python
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-57)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_code_visitor.py:_load_trigger_words (lines 77-84)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-57)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_code_visitor.py:_load_trigger_words (lines 77-85)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-57)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_code_visitor.py:_load_trigger_words (lines 75-85)):
    ```python
    if self.bot is None:
        return []
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        retur...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-57)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 114-121)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-57)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 114-122)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 49-57)):
    ```python
    behavior_obj = bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 113-122)):
    ```python
    self._ensure_bot_initialized()
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-84)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-112)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-86)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-114)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-87)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-115)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-89)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-117)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-89)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-117)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-89)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-118)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:84): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-90)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 112-118)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:86): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 86-91)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 114-119)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:86): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 86-91)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 114-120)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    return server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:87): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 87-92)):
    ```python
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    return server_file
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 115-120)):
    ```python
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    return server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-86)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-114)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-86)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-115)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-87)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-115)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-87)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-115)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-87)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-117)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-89)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-117)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-89)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-117)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-89)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-117)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-89)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-118)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-90)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-117)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-90)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-118)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-90)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-118)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-90)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-119)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:84): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-91)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 112-119)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:84): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-91)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-119)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:84): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-91)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 112-120)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:86): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 86-92)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    return server_file
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 114-119)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:86): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 86-92)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    return server_file
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 114-120)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    return server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:86): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 86-92)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    return server_file
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 112-120)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-87)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-114)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-87)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-115)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-87)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-115)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-87)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-117)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-89)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-115)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-89)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-117)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-89)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-117)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-89)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-117)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-89)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-118)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-89)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-118)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-89)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-119)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-90)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-117)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-90)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-118)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-90)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-118)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-90)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-118)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-90)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-119)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-91)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-118)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-91)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 112-119)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-91)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-119)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-91)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-119)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-91)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-120)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-91)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-119)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-91)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-120)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:84): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-92)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 112-119)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:84): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-92)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 114-120)):
    ```python
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = server_file
    return server_file
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:84): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-92)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 112-120)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:84): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 84-92)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-120)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-89)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-115)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-89)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-117)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-89)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-117)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-89)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-118)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-89)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-118)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-89)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-119)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-90)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-117)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-90)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-118)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-90)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-117)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-90)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-118)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-90)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-118)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-90)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-119)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-90)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-120)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-91)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-118)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-91)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-119)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-91)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-119)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-91)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-120)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-91)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-119)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-91)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-120)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-91)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-120)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-92)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-119)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-92)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 112-120)):
    ```python
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_text(server_code, encoding='utf-8')
    self.server_file_path = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-92)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-119)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-92)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-120)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-92)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-120)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:83): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 83-92)):
    ```python
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write_tex...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-120)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-90)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-117)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-90)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-117)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-90)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-118)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-90)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-118)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-90)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-119)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-91)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-117)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-91)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-119)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-91)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-118)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-91)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-119)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-91)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-119)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-91)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-120)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-91)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-119)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-91)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-120)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-92)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-119)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-92)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-119)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-92)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-120)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-92)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-119)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-92)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-120)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 82-92)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code,...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-120)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-91)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-117)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-91)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-118)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-91)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-119)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-91)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-119)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 80-91)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(sel...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-120)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-92)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-118)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-92)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-119)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-92)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 111-120)):
    ```python
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_code, behavior_tools_code)
    server_file.write...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-92)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-119)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-92)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 110-120)):
    ```python
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_server_code(base_tools_c...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-92)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 108-119)):
    ```python
    bot_dir = self.workspace_root / self.bot_location
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:81): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_create_mcp_server_file (lines 81-92)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._build_se...
    ```

  Location 2 (mcp_code_visitor.py:_create_mcp_server_file (lines 109-120)):
    ```python
    src_dir = bot_dir / 'src'
    src_dir.mkdir(parents=True, exist_ok=True)
    server_file = src_dir / f'{self.bot.bot_name}_mcp_server.py'
    self.server_file_path = server_file
    base_tools_code = self._generate_base_tools_code()
    behavior_tools_code = '\n\n'.join(self.tool_registrations)
    server_code = self._buil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-179)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-207)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-179)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-208)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-179)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-208)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-179)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-179)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-179)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:176): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-180)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-207)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:176): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-180)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-208)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:176): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-180)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-208)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:176): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-180)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:176): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-180)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:176): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-180)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:177): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 177-189)):
    ```python
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_code()
    return '\n\n'.join([bot_tool_code, get_working_dir_cod...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 205-217)):
    ```python
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_code()
    return '\n\n'.join([bot_tool_code, get_working_dir_cod...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:177): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 177-189)):
    ```python
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_code()
    return '\n\n'.join([bot_tool_code, get_working_dir_cod...
    ```

  Location 2 (render_spec.py:execute_synchronizer (lines 93-97)):
    ```python
    synchronizer_instance = self._import_synchronizer_class(self.synchronizer.synchronizer_class_path)()
    input_path = self._resolve_input_path()
    output_path = self._resolve_output_path(input_path)
    kwargs = self._build_synchronizer_kwargs()
    return synchronizer_instance.render(str(input_path), str(output_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-180)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-207)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-180)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-208)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-180)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-208)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-180)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-36)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    rules_content = self._g...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:176): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-189)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-217)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:176): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 176-189)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-217)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-189)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-217)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/mcp_code_visitor.py:175): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 175-189)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```

  Location 2 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-217)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\ext\bot_matcher.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/ext/bot_matcher.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_matcher.py:_load_patterns_from_file (lines 44-49)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    data = json.loads(content)
    return data.get('patterns', [])
    ```

  Location 2 (server_deployer.py:_load_trigger_words (lines 110-115)):
    ```python
    behavior_data = json.loads(behavior_file.read_text(encoding='utf-8'))
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\ext\bot_matcher.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/ext/bot_matcher.py:44): Duplicate code detected across files - extract to shared function.

  Location 1 (bot_matcher.py:_load_patterns_from_file (lines 44-49)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    data = json.loads(content)
    return data.get('patterns', [])
    ```

  Location 2 (file_link_builder.py:get_file_uri (lines 52-59)):
    ```python
    resolved_path = self._resolve_file_path(location)
    file_str = self._normalize_path_string(str(resolved_path))
    return self._build_vscode_uri(file_str, line_number)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\generator\orchestrator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/orchestrator.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (orchestrator.py:_visit_behavior (lines 38-49)):
    ```python
    behavior_name = behavior.name
    behavior_description = self.data_collector.get_behavior_description(behavior_name)
    actions = self.data_collector.get_behavior_actions(behavior)
    additional_options = self._get_additional_options(behavior_name)
    context = BehaviorHelpContext(bot_name=self.bot_name, behavio...
    ```

  Location 2 (build_action.py:inject_rules (lines 150-156)):
    ```python
    rules_obj = validate_action.rules
    rules_text = rules_obj.formatted_rules_digest()
    rules_data = validate_action.inject_behavior_specific_and_bot_rules()
    all_rules = rules_data.get('validation_rules', [])
    existing_instructions = instructions.get('base_instructions', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\generator\orchestrator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/generator/orchestrator.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (orchestrator.py:_visit_behavior (lines 38-50)):
    ```python
    behavior_name = behavior.name
    behavior_description = self.data_collector.get_behavior_description(behavior_name)
    actions = self.data_collector.get_behavior_actions(behavior)
    additional_options = self._get_additional_options(behavior_name)
    context = BehaviorHelpContext(bot_name=self.bot_name, behavio...
    ```

  Location 2 (command_file_visitor.py:_visit_rules_action (lines 174-186)):
    ```python
    data_collector = visitor.data_collector
    action_description = data_collector.get_action_description('rules')
    parameters = data_collector.get_action_parameters('rules')
    parameter_descriptions = data_collector.get_parameter_descriptions('rules', parameters)
    context = ActionHelpContext(bot_name=self.bot...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 77-84)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 114-121)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 77-84)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 113-121)):
    ```python
    self._ensure_bot_initialized()
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 77-84)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 114-122)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:78): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 78-85)):
    ```python
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 115-122)):
    ```python
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 77-85)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 114-121)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 77-85)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 114-122)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:77): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_load_trigger_words (lines 77-85)):
    ```python
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return trigger_words
    return []
    ```

  Location 2 (mcp_server_generator.py:_load_trigger_words_from_behavior_folder (lines 113-122)):
    ```python
    self._ensure_bot_initialized()
    behavior_obj = self.bot.behaviors.find_by_name(behavior)
    if behavior_obj is None:
        return []
    trigger_words = behavior_obj.trigger_words
    if isinstance(trigger_words, dict):
        return trigger_words.get('patterns', [])
    if isinstance(trigger_words, list):
        return tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:203): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-207)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:203): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-207)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:203): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-207)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:204): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-208)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:204): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-208)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:204): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 204-208)):
    ```python
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_...
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:205): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 205-217)):
    ```python
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    restart_server_code = self._generate_restart_server_code()
    return '\n\n'.join([bot_tool_code, get_working_dir_cod...
    ```

  Location 2 (render_spec.py:execute_synchronizer (lines 93-97)):
    ```python
    synchronizer_instance = self._import_synchronizer_class(self.synchronizer.synchronizer_class_path)()
    input_path = self._resolve_input_path()
    output_path = self._resolve_output_path(input_path)
    kwargs = self._build_synchronizer_kwargs()
    return synchronizer_instance.render(str(input_path), str(output_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_code_visitor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_code_visitor.py:203): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_code_visitor.py:_generate_base_tools_code (lines 203-208)):
    ```python
    bot_tool_code = self._generate_bot_tool_code()
    get_working_dir_code = self._generate_get_working_dir_code()
    set_working_dir_code = self._generate_set_working_dir_code()
    close_action_code = self._generate_close_action_code()
    confirm_out_of_order_code = self._generate_confirm_out_of_order_code()
    resta...
    ```

  Location 2 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-36)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    rules_content = self._g...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_config_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_config_generator.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_config_generator.py:generate_workspace_rules_file (lines 31-35)):
    ```python
    rules_file = self._get_rules_file_path()
    behaviors = discover_behaviors_fn()
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\mcp_config_generator.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/mcp_config_generator.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (mcp_config_generator.py:generate_workspace_rules_file (lines 33-37)):
    ```python
    bot_config = read_json_file(self.config_path)
    behavior_info = self._load_behavior_info(behaviors, load_trigger_info_fn)
    behavior_sections_str = self._build_behavior_sections(behaviors, behavior_info)
    rules_content = self._generate_rules_content(bot_config, behaviors, behavior_info, behavior_sections...
    ```

  Location 2 (rules_action.py:do_execute (lines 12-17)):
    ```python
    instructions = self.instructions.copy()
    rules = Rules(behavior=self.behavior, bot_paths=self.behavior.bot_paths)
    rules_digest = rules.formatted_rules_digest()
    rule_names = self._get_rule_names(rules)
    self._add_rules_list_to_display(instructions, rule_names)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\mcp\server_deployer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/mcp/server_deployer.py:110): Duplicate code detected across files - extract to shared function.

  Location 1 (server_deployer.py:_load_trigger_words (lines 110-115)):
    ```python
    behavior_data = json.loads(behavior_file.read_text(encoding='utf-8'))
    trigger_words = behavior_data.get('trigger_words', {})
    return trigger_words.get('patterns', [])
    ```

  Location 2 (file_link_builder.py:get_file_uri (lines 52-59)):
    ```python
    resolved_path = self._resolve_file_path(location)
    file_str = self._normalize_path_string(str(resolved_path))
    return self._build_vscode_uri(file_str, line_number)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 20-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, content, file_path, rule_obj)
            if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\abstraction_levels_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/abstraction_levels_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (abstraction_levels_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_abstraction_levels(function.node, content, file_path, rule_obj)...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 10-18)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-18)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-19)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-31)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 11-20)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-20)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-21)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-33)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 13-22)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 13-22)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-23)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-35)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-45)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 16-26)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-25)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-25)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-27)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-37)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 31-39)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-65)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-41)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 19-28)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-41)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 19-30)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-41)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 42-51)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-41)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 50-59)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-41)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-63)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-41)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-67)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-41)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 35-43)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 35-43)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 19-27)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 35-43)):
    ```python
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 61-69)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 10-20)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-20)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-21)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-33)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-43)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-23)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-35)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-45)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 13-26)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-25)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-27)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-37)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 16-26)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-39)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-49)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 31-41)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 53-63)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 31-41)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-67)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 31-41)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-69)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-43)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 19-28)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-43)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 19-30)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-43)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 42-51)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-43)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 50-59)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-43)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-63)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-43)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-67)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 32-43)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violati...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-23)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-35)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-45)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-25)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-27)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-37)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 31-43)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 53-63)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 31-43)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-67)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 31-43)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-69)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:31): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 31-43)):
    ```python
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_capability_nouns(name, node, node_type, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (communication_verb_scanner.py:scan_story_node (lines 10-26)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-25)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-27)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-37)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-49)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-39)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-49)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-41)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-41)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:29): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 29-43)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_ca...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 16-30)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-26)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-49)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-39)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-49)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-41)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-28)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-41)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-41)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-51)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 26-43)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        violations.append(vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-28)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 23-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if vi...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-51)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\active_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/active_language_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (active_language_scanner.py:scan_story_node (lines 24-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_actor_in_name(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_voice(name, node, node_type, rule_obj)
    if violation:
        vio...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 38-46)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_outline_scanner.py:scan_story_node (lines 25-33)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 38-46)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_specific_given_scanner.py:scan_story_node (lines 24-32)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ac_consolidation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ac_consolidation_scanner.py:38): Duplicate code detected across files - extract to shared function.

  Location 1 (ac_consolidation_scanner.py:_check_duplicate_ac (lines 38-46)):
    ```python
    location = f'{node.map_location()}.acceptance_criteria'
    violation = Violation(rule=rule_obj, violation_message=f'Duplicate acceptance criteria found at indices {indices} - consolidate duplicate AC', location=location, severity='warning').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 36-44)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 20-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, content, file_path, rule_obj)
            if violation:...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 20-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, content, file_path, rule_obj)
            if violation:...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 20-35)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = self._check_function_size(function.node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 20-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, content, file_path, rule_obj)
            if violation:...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 21-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_path, content, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 19-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(function.node, conte...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:scan_file (lines 17-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_aaa_structure(func...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\arrange_act_assert_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/arrange_act_assert_scanner.py:160): Duplicate code detected across files - extract to shared function.

  Location 1 (arrange_act_assert_scanner.py:_validate_aaa_structure (lines 160-165)):
    ```python
    test_lines = file_path.read_text(encoding='utf-8').split('\n')
    start_line = test_node.lineno - 1
    end_line = test_node.end_lineno if hasattr(test_node, 'end_lineno') else start_line + 50
    test_body_lines = test_lines[start_line:end_line]
    has_given_comment = any(('# Given' in line or '# Arrange' in lin...
    ```

  Location 2 (given_when_then_helpers_scanner.py:_check_test_method (lines 116-122)):
    ```python
    test_lines = content.split('\n')
    start_line = test_node.lineno - 1
    end_line = test_node.end_lineno if hasattr(test_node, 'end_lineno') else len(test_lines)
    test_body_lines = test_lines[start_line:end_line]
    inline_blocks = self._find_inline_code_blocks(test_node, test_body_lines, helper_functions, tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\background_common_setup_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/background_common_setup_scanner.py:96): Duplicate code detected across files - extract to shared function.

  Location 1 (background_common_setup_scanner.py:_get_scenario_steps (lines 96-102)):
    ```python
    if 'steps' in scenario:
        return scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            return [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (given_precondition_scanner.py:_get_scenario_steps (lines 32-38)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\background_common_setup_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/background_common_setup_scanner.py:96): Duplicate code detected across files - extract to shared function.

  Location 1 (background_common_setup_scanner.py:_get_scenario_steps (lines 96-102)):
    ```python
    if 'steps' in scenario:
        return scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            return [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (scenario_specific_given_scanner.py:_get_scenario_steps (lines 38-44)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(self._check_misleading_todos(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 21-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.extend(self._check_assertions(tree, content, file_path, rule_obj...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(self._check_mislead...
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.extend(self._check...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\bad_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/bad_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (bad_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_commented_code(lines, file_path, rule_obj))
    violations.extend(self._check_html_in_comments(lines, file_path, rule_obj))
    violations.extend(sel...
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    violations.ex...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 20-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 20-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 30-38)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 20-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    classes_under_test = self._extract_classes_under_test(tree, content, lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_language = self._extract_domain_language(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    classes_under_test = self._extract_classes_under_test(tree, content, lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:45): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:_extract_domain_language (lines 45-62)):
    ```python
    domain_terms = set()
    common_domain_terms = {'json', 'data', 'param', 'params', 'parameter', 'parameters', 'var', 'vars', 'variable', 'variables', 'method', 'methods', 'class', 'classes', 'call', 'calls', 'config', 'configuration', 'configurations', 'agent', 'bot', 'workflow', 'story', 'epic', 'scena...
    ```

  Location 2 (code_scanner.py:_extract_domain_terms (lines 45-63)):
    ```python
    domain_terms = set()
    common_domain_terms = {'json', 'data', 'param', 'params', 'parameter', 'parameters', 'var', 'vars', 'variable', 'variables', 'method', 'methods', 'class', 'classes', 'call', 'calls', 'config', 'configuration', 'configurations', 'agent', 'bot', 'workflow', 'story', 'epic', 'scena...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:279): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:_extract_code_snippet (lines 279-288)):
    ```python
    start_line_0 = start_line - 1
    if end_line is not None:
        end_line_0 = end_line
    else:
        end_line_0 = start_line_0 + 1
    ```

  Location 2 (code_scanner.py:_extract_code_snippet (lines 254-263)):
    ```python
    start_line_0 = start_line - 1
    if end_line is not None:
        end_line_0 = end_line
    else:
        end_line_0 = start_line_0 + 1
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:290): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:_extract_code_snippet (lines 290-297)):
    ```python
    snippet_start = max(0, start_line_0 - context_before)
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (tr...
    ```

  Location 2 (code_scanner.py:_extract_code_snippet (lines 265-272)):
    ```python
    snippet_start = max(0, start_line_0 - context_before)
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:290): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:_extract_code_snippet (lines 290-297)):
    ```python
    snippet_start = max(0, start_line_0 - context_before)
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (tr...
    ```

  Location 2 (code_scanner.py:_extract_code_snippet (lines 265-274)):
    ```python
    snippet_start = max(0, start_line_0 - context_before)
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:291): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:_extract_code_snippet (lines 291-299)):
    ```python
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (truncated)'
    return code_snippet
    ```

  Location 2 (code_scanner.py:_extract_code_snippet (lines 266-274)):
    ```python
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (truncated)'
    return code_snippet
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:290): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:_extract_code_snippet (lines 290-299)):
    ```python
    snippet_start = max(0, start_line_0 - context_before)
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (tr...
    ```

  Location 2 (code_scanner.py:_extract_code_snippet (lines 265-272)):
    ```python
    snippet_start = max(0, start_line_0 - context_before)
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\business_readable_test_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/business_readable_test_names_scanner.py:290): Duplicate code detected across files - extract to shared function.

  Location 1 (business_readable_test_names_scanner.py:_extract_code_snippet (lines 290-299)):
    ```python
    snippet_start = max(0, start_line_0 - context_before)
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (tr...
    ```

  Location 2 (code_scanner.py:_extract_code_snippet (lines 265-274)):
    ```python
    snippet_start = max(0, start_line_0 - context_before)
    snippet_end = min(len(lines), end_line_0 + 1)
    code_snippet = '\n'.join(lines[snippet_start:snippet_end])
    code_lines = code_snippet.split('\n')
    if len(code_lines) > max_lines:
        code_snippet = '\n'.join(code_lines[:max_lines]) + '\n    # ... (tr...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 21-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_path, content, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 28-39)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-37)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 27-39)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:
            violati...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\calculation_timing_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/calculation_timing_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (calculation_timing_code_scanner.py:scan_file (lines 25-39)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_timing_exposure(function.node, file_path, rule_obj)
        if violation:...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-25)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-34)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 20-27)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 21-32)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 20-27)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 24-34)):
    ```python
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 20-27)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 21-34)):
    ```python
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations.append(violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:scan_file (lines 18-27)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```

  Location 2 (test_file_naming_scanner.py:scan_file (lines 18-34)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    sub_epic_names = self._extract_sub_epic_names(knowledge_graph)
    file_name = file_path.stem
    violation = self._check_file_name_matches_sub_epic(file_name, sub_epic_names, file_path, rule_obj, knowledge_graph)
    if violation:
        violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:134): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_expected_scenario_name (lines 134-140)):
    ```python
    full_method_name = f'test_{method_name}' if not method_name.startswith('test_') else method_name
    method_name_norm = self._normalize_name(method_name)
    story_name_from_class = class_name[4:] if class_name.startswith('Test') else class_name
    story_name_normalized = self._normalize_name(story_name_from_c...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rule_line (lines 75-79)):
    ```python
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self._get_status_indicator(rule_info)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    anchor_link = self._rule_name_to_anchor(rule_name)
    rule_display_name...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-328)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-57)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-331)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:324): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 324-332)):
    ```python
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 51-57)):
    ```python
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-331)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-331)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-331)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-331)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-57)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-331)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-57)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-332)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-332)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-332)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-57)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:321): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 321-332)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-57)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-332)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-56)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-332)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 49-57)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:319): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_to_snake_case (lines 319-332)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```

  Location 2 (test_file_naming_scanner.py:_to_snake_case (lines 47-57)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    return name.lower()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:361): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 361-372)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 77-89)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if suggestions_normalized:
        sugges...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:361): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 361-372)):
    ```python
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 73-89)):
    ```python
    if matches:
        return None
    sub_epics_spanned = self._get_sub_epics_spanned_by_test_methods(file_path, knowledge_graph)
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if sugges...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:364): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_check_file_name_matches_sub_epic (lines 364-380)):
    ```python
    if len(sub_epics_spanned) > 1:
        return None
    suggestions = self._find_closest_sub_epic_names(name_normalized, sub_epic_names)
    suggestion_text = ''
    if suggestions:
        suggestion_list = ', '.join([f'test_{s}.py' for s in suggestions[:5]])
        suggestion_text = f' Suggested names: {suggestion_list}'...
    ```

  Location 2 (test_file_naming_scanner.py:_check_file_name_matches_sub_epic (lines 80-97)):
    ```python
    if len(sub_epics_spanned) > 1:
        return None
    suggestions_normalized = self._find_closest_sub_epic_names(name_normalized, normalized_sub_epic_names)
    suggestion_text = ''
    if suggestions_normalized:
        suggestion_list = ', '.join([f'test_{s}.py' for s in suggestions_normalized[:5]])
        suggestion_t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:392): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_get_sub_epics_spanned_by_test_methods (lines 392-401)):
    ```python
    class_name = node.name
    for item in node.body:
        if isinstance(item, ast.FunctionDef):
            if item.name.startswith('test_'):
                sub_epic = self._find_sub_epic_for_method(item.name, class_name, knowledge_graph)
                if sub_epic:
                    sub_epics.add(self._to_snake_case(s...
    ```

  Location 2 (test_file_naming_scanner.py:_get_sub_epics_spanned_by_test_methods (lines 109-118)):
    ```python
    class_name = node.name
    for item in node.body:
        if isinstance(item, ast.FunctionDef):
            if item.name.startswith('test_'):
                sub_epic = self._find_sub_epic_for_method(item.name, class_name, knowledge_graph)
                if sub_epic:
                    sub_epics.add(self._to_snake_case(s...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:433): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_sub_epic_for_method (lines 433-443)):
    ```python
    scenario_name = scenario.get('name', '')
    if not scenario_name:
        continue
    scenario_name_norm = self._normalize_name(scenario_name)
    if method_name_norm in scenario_name_norm or scenario_name_norm.startswith(method_name_norm) or method_name_norm.startswith(scenario_name_norm):
        return sub_epic_na...
    ```

  Location 2 (test_file_naming_scanner.py:_find_sub_epic_for_method (lines 149-159)):
    ```python
    scenario_name = scenario.get('name', '')
    if not scenario_name:
        continue
    scenario_name_norm = self._to_snake_case(scenario_name)
    if method_name_norm in scenario_name_norm or scenario_name_norm.startswith(method_name_norm) or method_name_norm.startswith(scenario_name_norm):
        return sub_epic_nam...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_based_organization_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_based_organization_scanner.py:453): Duplicate code detected across files - extract to shared function.

  Location 1 (class_based_organization_scanner.py:_find_sub_epic_for_method (lines 453-463)):
    ```python
    ac_text = ac.get('text', '') if isinstance(ac, dict) else str(ac)
    if not ac_text:
        continue
    ac_norm = self._normalize_name(ac_text)
    if method_name_norm in ac_norm or ac_norm.startswith(method_name_norm) or method_name_norm.startswith(ac_norm):
        return sub_epic_name
    ```

  Location 2 (test_file_naming_scanner.py:_find_sub_epic_for_method (lines 169-179)):
    ```python
    ac_text = ac.get('text', '') if isinstance(ac, dict) else str(ac)
    if not ac_text:
        continue
    ac_norm = self._to_snake_case(ac_text)
    if method_name_norm in ac_norm or ac_norm.startswith(method_name_norm) or method_name_norm.startswith(ac_norm):
        return sub_epic_name
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 43-54)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 21-32)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 20-32)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            violations.append(vio...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\class_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/class_size_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (class_size_scanner.py:scan_file (lines 18-32)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_size(cls.node, file_path, rule_obj, content)
        if violation:
            viola...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 43-52)):
    ```python
    if self._is_test_file(file_path):
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 44-54)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 46-54)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 48-56)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 46-54)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 44-56)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 47-56)):
    ```python
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 49-58)):
    ```python
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:51): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 51-62)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violation:
            violations.append(violation)
    return...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 43-54)):
    ```python
    if self._is_test_file(file_path):
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 44-56)):
    ```python
    if not file_path.exists():
        return violations
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 46-56)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 48-58)):
    ```python
    domain_terms = set()
    if self.knowledge_graph:
        domain_terms = self._extract_domain_terms(self.knowledge_graph)
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:50): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:scan_file (lines 50-62)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_parameters(function.node, file_path, rule_obj, domain_terms, content)
        if violatio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\clear_parameters_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/clear_parameters_scanner.py:65): Duplicate code detected across files - extract to shared function.

  Location 1 (clear_parameters_scanner.py:_is_test_file (lines 65-78)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_'):
        return True
    if file_name == 'conftest.py':
        return True
    return False
    ```

  Location 2 (scanner.py:_is_test_file (lines 74-83)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:133): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_extract_domain_terms (lines 133-139)):
    ```python
    if isinstance(ac, dict):
        ac_text = ac.get('criterion', '')
        if ac_text:
            domain_terms.update(self._extract_words_from_text(ac_text))
    elif isinstance(ac, str):
        domain_terms.update(self._extract_words_from_text(ac))
    ```

  Location 2 (specification_match_scanner.py:_extract_domain_terms (lines 297-303)):
    ```python
    if isinstance(ac, dict):
        ac_text = ac.get('criterion', '')
        if ac_text:
            domain_terms.update(self._extract_words_from_text(ac_text))
    elif isinstance(ac, str):
        domain_terms.update(self._extract_words_from_text(ac))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:193): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_parse_code_file (lines 193-198)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:_parse_test_file (lines 105-110)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:193): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_parse_code_file (lines 193-198)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (test_scanner.py:_parse_test_file (lines 51-56)):
    ```python
    content = test_file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(test_file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:207): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_read_and_parse_file (lines 207-214)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    lines = content.split('\n')
    tree = ast.parse(content, filename=str(file_path))
    return (content, lines, tree)
    ```

  Location 2 (test_scanner.py:_read_and_parse_file (lines 65-72)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    lines = content.split('\n')
    tree = ast.parse(content, filename=str(file_path))
    return (content, lines, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/code_scanner.py:228): Duplicate code detected across files - extract to shared function.

  Location 1 (code_scanner.py:_get_all_code_files_parsed (lines 228-232)):
    ```python
    parsed = self._parse_code_file(file_path)
    if parsed:
        content, tree = parsed
        parsed_files.append((file_path, content, tree))
    ```

  Location 2 (test_scanner.py:_get_all_test_files_parsed (lines 80-84)):
    ```python
    parsed = self._parse_test_file(test_file_path)
    if parsed:
        content, tree = parsed
        parsed_files.append((test_file_path, content, tree))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-18)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-18)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-18)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-19)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-18)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-20)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-20)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-20)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-21)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-20)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-22)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 13-22)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-22)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-23)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-22)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-45)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-25)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-27)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 18-26)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 18-26)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 19-27)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 18-26)):
    ```python
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 61-69)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-20)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-20)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-20)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-21)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-20)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-43)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-23)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-45)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-25)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-27)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-26)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-26)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-25)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-26)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-27)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-26)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-27)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 16-26)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-23)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-45)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-25)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-27)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-26)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-26)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-25)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-26)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-27)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-26)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-27)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 13-26)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation:
        violations....
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-25)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-27)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-25)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-27)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-27)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_obj)
    if violation...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-26)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (generic_capability_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-26)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-25)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-26)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-27)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\communication_verb_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/communication_verb_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (communication_verb_scanner.py:scan_story_node (lines 10-26)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_communication_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_enablement_verbs(name, node, node_type, rule_o...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\complete_refactoring_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/complete_refactoring_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (complete_refactoring_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_legacy_support(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-20)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 14-22)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_indentation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_indentation_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_indentation_scanner.py:scan_file (lines 12-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_indentation(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_naming_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_naming_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    function_names = []
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 52-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 16-25)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 15-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violations
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\consistent_vocabulary_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/consistent_vocabulary_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (consistent_vocabulary_scanner.py:scan_file (lines 13-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_terms = self._extract_domain_terms(knowledge_graph)
    violations.extend(self._check_vocabulary_consistency(content, domain_terms, file_path, rule_obj))
    return violati...
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\cover_all_paths_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/cover_all_paths_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (cover_all_paths_scanner.py:scan_file (lines 14-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 18-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (dependency_chaining_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\delegation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/delegation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (delegation_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_delegation(cls.node, content, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 21-34)):
    ```python
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 18-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 20-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 19-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(class_violations)
    ...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 21-36)):
    ```python
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\dependency_chaining_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/dependency_chaining_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (dependency_chaining_code_scanner.py:scan_file (lines 17-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_dependency_chaining(cls.node, file_path, rule_obj)
        violations.extend(cl...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 20-35)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = self._check_function_size(function.node, file_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 18-30)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node, file_path, rule_obj)
            if violation:
     ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 21-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_path, content, rule_obj)
            if violation:
     ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 17-30)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_name(function.node...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 20-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(function.node, file_pa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\descriptive_function_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/descriptive_function_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (descriptive_function_names_scanner.py:scan_file (lines 15-30)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if not function.node.name.startswith('test_'):
            violation = self._check_descriptive_na...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-52)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-54)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:43): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 43-54)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```

  Location 2 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_grouping_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_grouping_code_scanner.py:42): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_grouping_code_scanner.py:scan_file (lines 42-54)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_class_name(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_scanner.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_scanner.py:_scan_domain_concepts (lines 70-80)):
    ```python
    domain_concept_node = DomainConceptNode(concept_data, epic_idx, sub_epic_path, concept_idx)
    concept_violations = self.scan_domain_concept(domain_concept_node, rule_obj)
    violations.extend(concept_violations)
    ```

  Location 2 (enumerate_stories_scanner.py:scan_story_node (lines 19-27)):
    ```python
    location = node.map_location('description')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~X stories" notation - all stories must be explicitly enumerated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_scanner.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_scanner.py:_scan_domain_concepts (lines 70-80)):
    ```python
    domain_concept_node = DomainConceptNode(concept_data, epic_idx, sub_epic_path, concept_idx)
    concept_violations = self.scan_domain_concept(domain_concept_node, rule_obj)
    violations.extend(concept_violations)
    ```

  Location 2 (scenarios_on_story_docs_scanner.py:scan_story_node (lines 128-136)):
    ```python
    location = node.map_location()
    violation = Violation(rule=rule_obj, violation_message=f'Story "{node.name}" has no scenarios or scenario_outlines in story-graph.json - scenarios should be in JSON (scenarios or scenario_outlines fields)', location=location, severity='error').to_dict()
    violations.appe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\domain_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/domain_scanner.py:70): Duplicate code detected across files - extract to shared function.

  Location 1 (domain_scanner.py:_scan_domain_concepts (lines 70-80)):
    ```python
    domain_concept_node = DomainConceptNode(concept_data, epic_idx, sub_epic_path, concept_idx)
    concept_violations = self.scan_domain_concept(domain_concept_node, rule_obj)
    violations.extend(concept_violations)
    ```

  Location 2 (story_enumeration_scanner.py:scan_story_node (lines 20-28)):
    ```python
    location = node.map_location('estimated_stories')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~{estimated_stories}" notation - all stories must be explicitly enumerated, not estimated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            violations.append(...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            violations.append(v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\encapsulation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/encapsulation_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (encapsulation_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        if violation:
            vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\enumerate_stories_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/enumerate_stories_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (enumerate_stories_scanner.py:scan_story_node (lines 19-27)):
    ```python
    location = node.map_location('description')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~X stories" notation - all stories must be explicitly enumerated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenarios_on_story_docs_scanner.py:scan_story_node (lines 128-136)):
    ```python
    location = node.map_location()
    violation = Violation(rule=rule_obj, violation_message=f'Story "{node.name}" has no scenarios or scenario_outlines in story-graph.json - scenarios should be in JSON (scenarios or scenario_outlines fields)', location=location, severity='error').to_dict()
    violations.appe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\enumerate_stories_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/enumerate_stories_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (enumerate_stories_scanner.py:scan_story_node (lines 19-27)):
    ```python
    location = node.map_location('description')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~X stories" notation - all stories must be explicitly enumerated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (story_enumeration_scanner.py:scan_story_node (lines 20-28)):
    ```python
    location = node.map_location('estimated_stories')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~{estimated_stories}" notation - all stories must be explicitly enumerated, not estimated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mixed_error_handling(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\error_handling_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/error_handling_isolation_scanner.py:33): Duplicate code detected across files - extract to shared function.

  Location 1 (error_handling_isolation_scanner.py:_check_mixed_error_handling (lines 33-42)):
    ```python
    line_number = function.node.lineno if hasattr(function.node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Function "{function.node.name}" has {try_blocks_count} try-except blocks - extract error handling to separate functions', location=str(file_path), line_number=lin...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 30-38)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 17-25)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    classes_under_test = self._extract_classes_under_test(tree, content, lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:scan_file (lines 15-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_concepts = self._extract_domain_concepts(knowledge_graph)
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    classes_under_test = self._extract_classes_under_test(tree, content, lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exact_variable_names_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exact_variable_names_scanner.py:54): Duplicate code detected across files - extract to shared function.

  Location 1 (exact_variable_names_scanner.py:_check_variable_names (lines 54-63)):
    ```python
    line_number = target.lineno if hasattr(target, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Variable "{target.id}" uses generic name - use exact domain concept name from scenario/AC', location=str(file_path), line_number=line_number, severity='warning').to_dict()
    viol...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_classification_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_classification_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_classification_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_naming(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_exception_misuse(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\exception_handling_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/exception_handling_scanner.py:34): Duplicate code detected across files - extract to shared function.

  Location 1 (exception_handling_scanner.py:_check_exception_misuse (lines 34-44)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Empty except block at line {line_number} - exceptions should be logged or rethrown, never swallowed', location=str(file_path), line_number=line_number, severity='error').to_d...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 20-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.is_test_function:
            continue
        if function.name.startswith('_') and function.name != '__i...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:scan_file (lines 20-37)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.is_test_function:
            continue
        if function.name.startswith('_') and function.name != '__i...
    ```

  Location 2 (function_size_scanner.py:scan_file (lines 19-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
        violation = ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\excessive_guards_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/excessive_guards_scanner.py:107): Duplicate code detected across files - extract to shared function.

  Location 1 (excessive_guards_scanner.py:_get_violation_message (lines 107-111)):
    ```python
    violation_messages = rule_obj.rule_content.get('violation_messages', {})
    if message_key in violation_messages:
        template = violation_messages[message_key]
        return template.format(line=line_number, **format_args)
    ```

  Location 2 (type_safety_scanner.py:_get_violation_message (lines 213-217)):
    ```python
    violation_messages = rule_obj.rule_content.get('violation_messages', {})
    if message_key in violation_messages:
        template = violation_messages[message_key]
        return template.format(line=line_number, **format_args)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-21)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 15-23)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\explicit_dependencies_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/explicit_dependencies_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (explicit_dependencies_scanner.py:scan_file (lines 13-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_hidden_dependencies(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\fixture_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/fixture_placement_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (fixture_placement_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fixture_imports(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\function_size_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/function_size_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (function_size_scanner.py:scan_file (lines 17-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('_') and function.node.name != '__init__':
            continue
    ...
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        if function.node.name.startswith('test_'):
            violation = self._check_one_concept(functi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-18)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-19)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-18)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-20)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-21)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-20)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-22)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-23)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-22)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-45)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-25)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-25)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 17-27)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 16-24)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 18-26)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 53-61)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-28)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 42-51)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-28)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 46-55)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-28)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 50-59)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-28)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-63)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-28)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-67)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-28)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 19-27)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 22-30)):
    ```python
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 61-69)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-20)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-21)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-20)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-43)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-23)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-22)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-45)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-25)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 14-27)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-24)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 16-26)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-49)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 18-28)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 53-63)):
    ```python
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 18-28)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-67)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-30)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 42-51)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_noun_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-30)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 50-59)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_verb_pattern(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-30)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 54-63)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-30)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-67)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 19-30)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 58-69)):
    ```python
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-23)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-22)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-45)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-25)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 12-27)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-24)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-26)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-49)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 16-28)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-51)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 16-28)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._chec...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 18-30)):
    ```python
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_generic_technical_verbs(name, node, ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 57-69)):
    ```python
    violation = self._check_actor_prefix(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-25)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (specificity_scanner.py:scan_story_node (lines 11-27)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-24)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-49)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-26)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-49)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-28)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-28)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-26)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-49)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-26)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-49)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-28)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-28)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-51)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:13): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 13-30)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
        violations.appen...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-51)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:10): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 10-28)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    i...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-51)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\generic_capability_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/generic_capability_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (generic_capability_scanner.py:scan_story_node (lines 11-30)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_capability_verbs(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_passive_states(name, node, node_type, rule_obj)
    if violation:
       ...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-51)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_precondition_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_precondition_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (given_precondition_scanner.py:scan_story_node (lines 19-26)):
    ```python
    scenario_steps = self._get_scenario_steps(scenario)
    for step_idx, step in enumerate(scenario_steps):
        if step.startswith('Given') or step.startswith('And'):
            violation = self._check_given_is_functionality(step, node, scenario_idx, step_idx, rule_obj)
            if violation:
                viola...
    ```

  Location 2 (given_state_not_actions_scanner.py:scan_story_node (lines 19-26)):
    ```python
    scenario_steps = self._get_scenario_steps(scenario)
    for step_idx, step in enumerate(scenario_steps):
        if step.startswith('Given') or step.startswith('And'):
            violation = self._check_given_is_action(step, node, scenario_idx, step_idx, rule_obj)
            if violation:
                violations.a...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_precondition_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_precondition_scanner.py:32): Duplicate code detected across files - extract to shared function.

  Location 1 (given_precondition_scanner.py:_get_scenario_steps (lines 32-38)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```

  Location 2 (scenario_specific_given_scanner.py:_get_scenario_steps (lines 38-44)):
    ```python
    if 'steps' in scenario:
        steps = scenario['steps']
    elif 'scenario' in scenario:
        scenario_text = scenario['scenario']
        if isinstance(scenario_text, str):
            steps = [s.strip() for s in scenario_text.split('\n') if s.strip()]
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-36)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:scan_file (lines 28-38)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    helper_functions = self._get_helper_functions(tree, content)
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    classes_under_test = self._extract_classes_under_test(tree, content, lines)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\given_when_then_helpers_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/given_when_then_helpers_scanner.py:105): Duplicate code detected across files - extract to shared function.

  Location 1 (given_when_then_helpers_scanner.py:_parse_test_file (lines 105-110)):
    ```python
    content = file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(file_path))
    return (content, tree)
    ```

  Location 2 (test_scanner.py:_parse_test_file (lines 51-56)):
    ```python
    content = test_file_path.read_text(encoding='utf-8')
    tree = ast.parse(content, filename=str(test_file_path))
    return (content, tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\import_placement_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/import_placement_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (import_placement_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    import_section_end = self._find_import_section_end(lines)
    violations.extend(self._check_import_placement(lines, import_section_end, file_path, rule_obj))
    ```

  Location 2 (intention_revealing_names_scanner.py:scan_file (lines 52-60)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    docstring_ranges = self._get_docstring_ranges(tree)
    violations.extend(self._check_variable_names(tree, file_path, rule_obj, content, domain_terms, docstring_ranges))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\invest_principles_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/invest_principles_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (invest_principles_scanner.py:scan_story_node (lines 22-29)):
    ```python
    story_data = node.data if hasattr(node, 'data') else {}
    scenarios = story_data.get('scenarios', [])
    scenario_outlines = story_data.get('scenario_outlines', [])
    acceptance_criteria = story_data.get('acceptance_criteria', [])
    has_scenarios = scenarios and len(scenarios) > 0 or (scenario_outlines and l...
    ```

  Location 2 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-29)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 20-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-29)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 28-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-29)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 20-29)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\meaningful_context_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/meaningful_context_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (meaningful_context_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_magic_numbers(lines, file_path, rule_obj))
    violations.extend(self._check_numbered_variables(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\minimize_mutable_state_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/minimize_mutable_state_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (minimize_mutable_state_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mutable_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\mock_boundaries_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/mock_boundaries_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (mock_boundaries_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_mock_usage(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 25-35)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if violation:
          ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\natural_english_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/natural_english_code_scanner.py:23): Duplicate code detected across files - extract to shared function.

  Location 1 (natural_english_code_scanner.py:scan_file (lines 23-35)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_natural_english(function.node, file_path, rule_obj, content)
        if v...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_fallbacks_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_fallbacks_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (no_fallbacks_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_fallback_patterns(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 20-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 28-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 20-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 20-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 19-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\no_guard_clauses_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/no_guard_clauses_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (no_guard_clauses_scanner.py:scan_file (lines 17-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_guard_clause_patterns(lines, file_path, rule_obj))
    violations.extend(self._check_ast_guard_clauses(file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\observable_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/observable_behavior_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (observable_behavior_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_internal_testing(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\one_concept_per_test_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/one_concept_per_test_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (one_concept_per_test_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\open_closed_principle_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/open_closed_principle_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (open_closed_principle_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_type_switches(tree, content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\primitive_vs_object_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/primitive_vs_object_scanner.py:36): Duplicate code detected across files - extract to shared function.

  Location 1 (primitive_vs_object_scanner.py:scan_file (lines 36-44)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 15-23)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 18-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violations)
    return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 25-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\property_encapsulation_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/property_encapsulation_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (property_encapsulation_code_scanner.py:scan_file (lines 17-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_encapsulation(cls.node, content, file_path, rule_obj)
        violations.extend(class_violation...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 24-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        class_violations = self._check_class(cls.node, file_path, rule_obj, lines, content)
        violations.extend(class_violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\real_implementations_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/real_implementations_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (real_implementations_scanner.py:scan_file (lines 25-33)):
    ```python
    method_violations = self._check_test_methods_call_production_code(content, lines, file_path, rule_obj, knowledge_graph)
    violations.extend(method_violations)
    fake_violations = self._check_fake_implementations(lines, file_path, rule_obj)
    violations.extend(fake_violations)
    return violations
    ```

  Location 2 (type_safety_scanner.py:_check_function_type_safety (lines 80-86)):
    ```python
    return_violations = self._check_return_type(func_node, file_path, rule_obj, content)
    violations.extend(return_violations)
    get_violations = self._check_parameters_get_pattern(func_node, file_path, rule_obj, content)
    violations.extend(get_violations)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner.py:74): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner.py:_is_test_file (lines 74-83)):
    ```python
    path_str = str(file_path).lower()
    file_name = file_path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```

  Location 2 (file.py:is_test_file (lines 81-90)):
    ```python
    path_str = str(self._path).lower()
    file_name = self._path.name.lower()
    if '/test' in path_str or '/tests' in path_str or '\\test' in path_str or ('\\tests' in path_str):
        return True
    if file_name.startswith('test_') or file_name == 'conftest.py':
        return True
    return False
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner_loader.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner_loader.py:39): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_loader.py:_load_scanner_class (lines 39-49)):
    ```python
    try:
        module = importlib.import_module(path)
        if hasattr(module, class_name):
            scanner_class = getattr(module, class_name)
            if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
                if issubclass(scanner_class, Scanner):
                    return (scanner_cl...
    ```

  Location 2 (scanner_registry.py:loads_scanner_class_with_error (lines 49-59)):
    ```python
    try:
        module = importlib.import_module(path)
        if hasattr(module, class_name):
            scanner_class = getattr(module, class_name)
            if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
                if issubclass(scanner_class, Scanner):
                    return (scanner_cl...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scanner_loader.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scanner_loader.py:40): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_loader.py:_load_scanner_class (lines 40-49)):
    ```python
    module = importlib.import_module(path)
    if hasattr(module, class_name):
        scanner_class = getattr(module, class_name)
        if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
            if issubclass(scanner_class, Scanner):
                return (scanner_class, None)
    ```

  Location 2 (scanner_registry.py:loads_scanner_class_with_error (lines 50-59)):
    ```python
    module = importlib.import_module(path)
    if hasattr(module, class_name):
        scanner_class = getattr(module, class_name)
        if isinstance(scanner_class, type) and hasattr(scanner_class, 'scan'):
            if issubclass(scanner_class, Scanner):
                return (scanner_class, None)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenarios_on_story_docs_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenarios_on_story_docs_scanner.py:128): Duplicate code detected across files - extract to shared function.

  Location 1 (scenarios_on_story_docs_scanner.py:scan_story_node (lines 128-136)):
    ```python
    location = node.map_location()
    violation = Violation(rule=rule_obj, violation_message=f'Story "{node.name}" has no scenarios or scenario_outlines in story-graph.json - scenarios should be in JSON (scenarios or scenario_outlines fields)', location=location, severity='error').to_dict()
    violations.appe...
    ```

  Location 2 (story_enumeration_scanner.py:scan_story_node (lines 20-28)):
    ```python
    location = node.map_location('estimated_stories')
    violation = Violation(rule=rule_obj, violation_message=f'Epic "{node.name}" uses "~{estimated_stories}" notation - all stories must be explicitly enumerated, not estimated', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_outline_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_outline_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_outline_scanner.py:scan_story_node (lines 25-33)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (scenario_specific_given_scanner.py:scan_story_node (lines 24-32)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_outline_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_outline_scanner.py:25): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_outline_scanner.py:scan_story_node (lines 25-33)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message='Scenario Outline used but no Examples table found - Scenario Outlines require Examples table', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 36-44)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\scenario_specific_given_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/scenario_specific_given_scanner.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (scenario_specific_given_scanner.py:scan_story_node (lines 24-32)):
    ```python
    location = f'{node.map_location()}.scenarios[{scenario_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'Scenario does not start with Given step - scenario-specific setup should start with Given, not When', location=location, severity='error').to_dict()
    violations.append(violation)
    ```

  Location 2 (vertical_slice_scanner.py:scan (lines 36-44)):
    ```python
    location = f'increments[{increment_idx}]'
    violation = Violation(rule=rule_obj, violation_message=f'''Increment "{increment.get('name', f'Increment {increment_idx + 1}')}" spans only 1 epic - increments should be vertical slices spanning multiple epics''', location=location, severity='error').to_dict...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-29)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 20-31)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violation...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-29)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 19-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if violation:
           ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\separate_concerns_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/separate_concerns_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (separate_concerns_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_mixed_concerns(function.node, content, file_path, rule_obj)
        if vi...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if violation:
            ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\simplify_control_flow_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/simplify_control_flow_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (simplify_control_flow_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_nesting_depth(function.node, file_path, rule_obj, content)
        if vio...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 21-31)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 22-33)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations.append(violation)
    return violations
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 17-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if violation:
            violations.append(violation)
    r...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 21-33)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
            violations....
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 16-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule_obj)
        if vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (technical_abstraction_code_scanner.py:scan_file (lines 17-31)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    for cls in classes.get_many_classes:
        violation = self._check_technical_abstraction(cls.node, file_path, rule_obj)
        if violation:
            vio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\single_responsibility_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/single_responsibility_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (single_responsibility_scanner.py:scan_file (lines 19-33)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_function_sr(function.node, file_path, rule_obj)
        if violation:
       ...
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    for function in functions.get_many_functions:
        violation = self._check_variable_declaration_distance(function.node, content, file_path, rule...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 20-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specification_match_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specification_match_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (specification_match_scanner.py:scan_file (lines 18-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_method_names(tree, file_path, rule_obj))
    violations.extend(self._check_variable_names(tree, content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 11-19)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-41)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 12-21)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-43)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 14-23)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-45)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 17-25)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 17-25)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 19-27)):
    ```python
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 61-69)):
    ```python
    violation = self._check_noun_only(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_third_person_singular(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 11-21)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-43)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 12-23)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-45)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 14-25)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 14-25)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 17-27)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    return violations
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 39-47)):
    ```python
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 11-23)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-45)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 12-25)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 14-27)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violations.append(viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 36-47)):
    ```python
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        violations.append(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 11-25)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:12): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 12-27)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if violation:
        violat...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 34-47)):
    ```python
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if violation:
        v...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\specificity_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/specificity_scanner.py:11): Duplicate code detected across files - extract to shared function.

  Location 1 (specificity_scanner.py:scan_story_node (lines 11-27)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_too_generic(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_too_specific(name, node, node_type, rule_obj)
    if viola...
    ```

  Location 2 (verb_noun_scanner.py:scan_story_node (lines 33-47)):
    ```python
    violations = []
    name = node.name
    if not name:
        return violations
    node_type = self._get_node_type(node)
    violation = self._check_verb_noun_order(name, node, node_type, rule_obj)
    if violation:
        violations.append(violation)
    violation = self._check_gerund_ending(name, node, node_type, rule_obj)
    if ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_map.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_map.py:255): Duplicate code detected across files - extract to shared function.

  Location 1 (story_map.py:from_bot (lines 255-272)):
    ```python
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise TypeError(f...
    ```

  Location 2 (nodes.py:from_bot (lines 350-363)):
    ```python
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise TypeError(f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\story_map.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/story_map.py:253): Duplicate code detected across files - extract to shared function.

  Location 1 (story_map.py:from_bot (lines 253-272)):
    ```python
    import json
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise...
    ```

  Location 2 (nodes.py:from_bot (lines 350-363)):
    ```python
    if hasattr(bot, 'bot_paths') and hasattr(bot.bot_paths, 'bot_directory'):
        bot_directory = Path(bot.bot_paths.bot_directory)
    elif hasattr(bot, 'bot_directory'):
        bot_directory = Path(bot.bot_directory)
    elif isinstance(bot, (str, Path)):
        bot_directory = Path(bot)
    else:
        raise TypeError(f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```

  Location 2 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_swallowed_exceptions(tree, file_path, rule_obj, content))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```

  Location 2 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\swallowed_exceptions_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/swallowed_exceptions_scanner.py:46): Duplicate code detected across files - extract to shared function.

  Location 1 (swallowed_exceptions_scanner.py:_check_swallowed_exceptions (lines 46-57)):
    ```python
    line_number = handler.lineno if hasattr(handler, 'lineno') else None
    violation = self._create_violation_with_snippet(rule_obj=rule_obj, violation_message=f'Except block only contains pass at line {line_number} - exceptions must be logged or rethrown, never swallowed', file_path=file_path, line_numbe...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\technical_abstraction_code_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/technical_abstraction_code_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (technical_abstraction_code_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    classes = Classes(tree)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-25)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 19-27)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 16-24)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_boundary_behavior_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_boundary_behavior_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (test_boundary_behavior_scanner.py:scan_file (lines 17-27)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_boundary_tests(content, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_file_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_file_naming_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_file_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_file_naming_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_file_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_file_naming_scanner.py:47): Duplicate code detected across files - extract to shared function.

  Location 1 (test_file_naming_scanner.py:_to_snake_case (lines 47-55)):
    ```python
    name = name.replace('&amp;', 'and')
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_file_naming_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_file_naming_scanner.py:49): Duplicate code detected across files - extract to shared function.

  Location 1 (test_file_naming_scanner.py:_to_snake_case (lines 49-56)):
    ```python
    name = name.replace('&', 'and')
    name = re.sub('\\band\\s+', 'and', name)
    name = re.sub('([a-z])([A-Z])', '\\1_\\2', name)
    name = name.replace(' ', '_')
    name = re.sub('[^a-zA-Z0-9_]', '', name)
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 18-29)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 15-26)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 20-31)):
    ```python
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 17-28)):
    ```python
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 27-34)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 28-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:28): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 28-36)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 18-31)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if not self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:scan_file (lines 15-28)):
    ```python
    violations = []
    if not file_path.exists():
        return violations
    if self._is_test_file(file_path):
        return violations
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 27-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:27): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:scan_file (lines 27-36)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_test_independence(tree, content, file_path, rule_obj))
    violations.extend(self._check_test_names_quality(tree, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\test_quality_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/test_quality_scanner.py:58): Duplicate code detected across files - extract to shared function.

  Location 1 (test_quality_scanner.py:_check_test_independence (lines 58-67)):
    ```python
    line_number = node.lineno if hasattr(node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Line {line_number} uses global state - tests should be independent, not share state', location=str(file_path), line_number=line_number, severity='error').to_dict()
    violations.appen...
    ```

  Location 2 (unnecessary_parameter_passing_scanner.py:_check_method_parameters (lines 101-111)):
    ```python
    line_number = method_node.lineno if hasattr(method_node, 'lineno') else None
    violation = Violation(rule=rule_obj, violation_message=f'Internal method "{method_node.name}" receives parameter "{arg.arg}" that matches instance attribute. Consider accessing via self.{arg.arg} instead.', location=str(fil...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\third_party_isolation_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/third_party_isolation_scanner.py:14): Duplicate code detected across files - extract to shared function.

  Location 1 (third_party_isolation_scanner.py:scan_file (lines 14-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_third_party_usage(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\ubiquitous_language_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/ubiquitous_language_scanner.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (ubiquitous_language_scanner.py:scan_file (lines 17-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    domain_entities = self._extract_domain_entities_from_story_graph(knowledge_graph)
    ```

  Location 2 (vertical_density_scanner.py:scan_file (lines 14-22)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    functions = Functions(tree)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-24)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 19-28)):
    ```python
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-26)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 18-28)):
    ```python
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violations
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-26)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```

  Location 2 (useless_comments_scanner.py:scan_file (lines 16-28)):
    ```python
    violations = []
    parsed = self._read_and_parse_file(file_path)
    if not parsed:
        return violations
    content, lines, tree = parsed
    violations.extend(self._check_useless_docstrings(content, file_path, rule_obj))
    violations.extend(self._check_useless_comments(lines, file_path, rule_obj))
    return violatio...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-133)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-120)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:127): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 127-142)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 114-129)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:127): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 127-142)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 114-131)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:129): Duplicate code detected across files - extract to shared function.

Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 129-144)):
```python
lines_before = before_docstring.strip()
if not lines_before or lines_before.count('\n') == 0:
    return False
lines = recent_context.split('\n')
for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    line = lines[i].strip()
    if line.startswith('def ') or line.startswith('Test class [):
     ...
```

Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 116-131)):
```python
lines_before = before_docstring.strip()
if not lines_before or lines_before.count(](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:129)\n') == 0:
    return False
lines = recent_context.split('\n')
for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    line = lines[i].strip()
    if line.startswith('def ') or line.startswith('class '):
     ...
```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-142)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-129)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-142)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-131)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:127): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 127-144)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 114-129)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:127): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 127-144)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 114-131)):
    ```python
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(len(lines) - 1, max(0, len(lines) - 5), -1):
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-144)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-129)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\scanners\useless_comments_scanner.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/scanners/useless_comments_scanner.py:126): Duplicate code detected across files - extract to shared function.

  Location 1 (useless_comments_scanner.py:_is_useless_docstring (lines 126-144)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```

  Location 2 (useless_comments_scanner.py:_is_useless_docstring (lines 113-131)):
    ```python
    before_docstring = content[:docstring_start]
    recent_context = before_docstring[-200:] if len(before_docstring) > 200 else before_docstring
    lines_before = before_docstring.strip()
    if not lines_before or lines_before.count('\n') == 0:
        return False
    lines = recent_context.split('\n')
    for i in range(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\clarify\requirements_clarifications.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/clarify/requirements_clarifications.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (requirements_clarifications.py:save (lines 17-21)):
    ```python
    original_questions = self.required_context.key_questions.questions
    original_evidence_requirements = self.required_context.evidence.evidence_list
    new_data = {'key_questions': {'questions': original_questions, 'answers': self.key_questions_answered}, 'evidence': {'required': original_evidence_requirem...
    ```

  Location 2 (strategy_decision.py:save (lines 20-24)):
    ```python
    original_assumptions = self.strategy.assumptions.assumptions
    original_recommended_activities = self.strategy.recommended_activities.recommended_activities
    new_data = {'strategy_criteria': {'criteria': original_strategy_criteria, 'decisions_made': self.decisions_made}, 'assumptions': {'typical_assump...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```

  Location 2 (scanner_status_formatter.py:format_executed_rule_line (lines 104-108)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (rule.py:scan (lines 117-121)):
    ```python
    files_to_scan = changed_files if changed_files else files
    test_files = files_to_scan.get('test', [])
    code_files = files_to_scan.get('src', [])
    all_test_files = files.get('test', [])
    all_code_files = files.get('src', [])
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 227-231)):
    ```python
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\rules\rule_filter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/rules/rule_filter.py:80): Duplicate code detected across files - extract to shared function.

  Location 1 (rule_filter.py:_is_file_in_folder (lines 80-87)):
    ```python
    folder_path = Path(folder_pattern)
    if not (folder_path.is_absolute() or file_path.is_absolute()):
        return False
    return self._check_path_relative(file_path, folder_path)
    ```

  Location 2 (file_discovery.py:_is_file_in_folder (lines 60-66)):
    ```python
    folder_path = Path(folder_pattern)
    if not (folder_path.is_absolute() or file_path.is_absolute()):
        return False
    return self._check_path_relative(file_path, folder_path)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\file_link_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/file_link_builder.py:82): Duplicate code detected across files - extract to shared function.

  Location 1 (file_link_builder.py:get_relative_path (lines 82-90)):
    ```python
    if file_path.is_absolute() and self.workspace_directory:
        return str(file_path.relative_to(self.workspace_directory))
    if self.workspace_directory and (not file_path.is_absolute()):
        return self.get_relative_path_for_relative_file(file_path)
    return file_path.name
    ```

  Location 2 (validation_report_formatter.py:get_relative_path (lines 158-166)):
    ```python
    if file_path.is_absolute() and self.workspace_directory:
        return str(file_path.relative_to(self.workspace_directory))
    if self.workspace_directory and (not file_path.is_absolute()):
        return self._get_relative_path_for_relative_file(file_path)
    return file_path.name
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-19)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-21)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-20)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-22)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-21)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-23)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-22)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 272-276)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-22)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-24)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 268-272)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 269-273)):
    ```python
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanne...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 271-275)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 272-276)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-23)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-24)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 273-277)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-24)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 20-24)):
    ```python
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execution Failures', 'E...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-20)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-21)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-23)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-22)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-24)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 268-273)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 270-275)):
    ```python
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(sel...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 271-276)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-23)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-24)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 272-277)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-24)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 19-24)):
    ```python
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(categorized['execution_failed'], 'Scanner Execut...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-21)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-22)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-24)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-22)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-22)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-23)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-24)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-23)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-25)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-23)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-26)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-24)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_report_writer.py:_build_report_lines (lines 271-277)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-24)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 18-24)):
    ```python
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self.format_failed_rules_section(c...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-26)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-22)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-23)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-24)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-23)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-23)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 17-24)):
    ```python
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rules_section(categorized['load_failed'], 'Scanner Load Failures',...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 19-26)):
    ```python
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines....
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-23)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-24)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-25)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:16): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 16-24)):
    ```python
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['executed']))
    lines.extend(self.format_failed_rule...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 18-26)):
    ```python
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_fa...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-25)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:15): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_scanner_status (lines 15-24)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self.categorize_scanner_rules(validation_rules)
    stats = self.build_scanner_stats(validation_rules, categorized)
    lines.extend(self.build_status_summary(stats))
    lines.append('')
    lines.extend(self.format_executed_rules_section(categorized['execu...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 17-26)):
    ```python
    lines = ['## Scanner Execution Status', '']
    categorized = self._categorize_rules(validation_rules)
    stats = self._build_stats(validation_rules, categorized)
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(se...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-97)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-70)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-97)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-71)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:93): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 93-98)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-71)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:93): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 93-98)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-71)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-98)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-70)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-98)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 66-71)):
    ```python
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:91): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rules_section (lines 91-98)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self.format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rules (lines 64-71)):
    ```python
    if not executed_rules:
        return []
    lines = ['### 🟩 Successfully Executed Scanners', '']
    executed_rules.sort(key=lambda x: (-x['violations'], x['rule']))
    for rule_info in executed_rules:
        lines.extend(self._format_executed_rule_line(rule_info))
    lines.append('')
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 103-107)):
    ```python
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 226-230)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(in...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:104): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 104-108)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 227-231)):
    ```python
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower(...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:104): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 104-108)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:105): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 105-109)):
    ```python
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, violations)
    status_suffix = f'({exec_status})' if exec_status != 'SUCCESS' else f'({s...
    ```

  Location 2 (validation_report_formatter.py:format_violation_line (lines 14-18)):
    ```python
    location = violation.get('location', 'unknown')
    message = violation.get('violation_message', 'No message')
    severity = violation.get('severity', 'error')
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:105): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 105-109)):
    ```python
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, violations)
    status_suffix = f'({exec_status})' if exec_status != 'SUCCESS' else f'({s...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 229-233)):
    ```python
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    violations_link = f' - [View Details](#{anchor_id}-violations)' if info.get('violations', 0) > 0 else ...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:101): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 101-106)):
    ```python
    violations = rule_info['violations']
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = r...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_executed_rule_line (lines 74-78)):
    ```python
    violations = rule_info['violations']
    rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
    status_indicator, status_text = self._get_status_indicator(rule_info)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    anchor_link = self._rule_name_t...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:103): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 103-108)):
    ```python
    status_indicator, status_text = self.get_rule_status_indicator(rule_info, violations)
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 226-231)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    rule_name = Path(rule_file).stem if rule_file else 'unknown'
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(in...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:104): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_executed_rule_line (lines 104-109)):
    ```python
    violations_text = f'{violations} violation(s)' if violations > 0 else '0 violations'
    exec_status = rule_info.get('execution_status', 'SUCCESS')
    anchor_link = self.rule_name_to_anchor(rule_name)
    rule_display_name = rule_name.replace('_', ' ').title()
    details_link = self.build_details_link(rule_name, ...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-233)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    violations...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:128): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:format_failed_rules_section (lines 128-139)):
    ```python
    if not failed_rules:
        return []
    lines = [f'### 🟥 {title}', '']
    for rule_info in failed_rules:
        rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
        anchor_link = self.rule_name_to_anchor(rule_name)
        rule_display_name = rule_name.replace('_', ' ').title()
        line...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_failed_rules (lines 95-106)):
    ```python
    if not failed_rules:
        return []
    lines = [f'### 🟥 {title}', '']
    for rule_info in failed_rules:
        rule_name = Path(rule_info['rule']).stem if rule_info['rule'] else 'unknown'
        anchor_link = self._rule_name_to_anchor(rule_name)
        rule_display_name = rule_name.replace('_', ' ').title()
        lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:173): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:_get_violation_status (lines 173-180)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```

  Location 2 (validation_scanner_status_builder.py:_get_overall_status (lines 143-155)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:173): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:_get_violation_status (lines 173-180)):
    ```python
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ERRORS_FOR_GOOD_STATUS:
        return ('🟨', 'GOOD - Minor Issues...
    ```

  Location 2 (validation_scanner_status_builder.py:_get_overall_status (lines 138-155)):
    ```python
    if not stats.has_violations:
        return ('🟩', 'ALL CLEAN')
    violation_density = stats.total_violations
    if violation_density < 150 and stats.rules_with_errors == 0:
        return ('🟩', 'HEALTHY')
    if violation_density < MAX_VIOLATION_DENSITY_FOR_GOOD_STATUS and stats.rules_with_errors <= MAX_RULES_WITH_ER...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\scanner_status_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/scanner_status_formatter.py:220): Duplicate code detected across files - extract to shared function.

  Location 1 (scanner_status_formatter.py:build_totals_summary (lines 220-227)):
    ```python
    lines = [f'**Total Rules:** {stats.total_rules}', f'- **Rules with Scanners:** {stats.total_with_scanners}', f'  - 🟩 **Executed Successfully:** {stats.executed_count}']
    if stats.load_failed_count > 0:
        lines.append(f'  - 🟥 **Load Failed:** {stats.load_failed_count}')
    if stats.execution_failed_cou...
    ```

  Location 2 (validation_scanner_status_builder.py:_build_totals_summary (lines 180-187)):
    ```python
    lines = [f'**Total Rules:** {stats.total_rules}', f'- **Rules with Scanners:** {stats.total_with_scanners}', f'  - 🟩 **Executed Successfully:** {stats.executed_count}']
    if stats.load_failed_count > 0:
        lines.append(f'  - 🟥 **Load Failed:** {stats.load_failed_count}')
    if stats.execution_failed_cou...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_executor.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_executor.py:79): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_executor.py:_collect_scanner_status (lines 79-83)):
    ```python
    scanner_status = rule_dict.get('scanner_status', {})
    status = scanner_status.get('status', 'UNKNOWN')
    rule_file = rule_dict.get('rule_file', 'unknown')
    self._process_scanner_status(status, counts, scanner_status_lines, rule_file, scanner_status)
    ```

  Location 2 (validation_scanner_status_builder.py:_categorize_rules (lines 30-34)):
    ```python
    rule_file = rule_dict.get('rule_file', 'unknown')
    scanner_status = rule_dict.get('scanner_status', {})
    status = scanner_status.get('status', 'UNKNOWN')
    self._categorize_rule_by_status(status, rule_dict, rule_file, scanner_status, result)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-23)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-310)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-25)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-312)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-26)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-313)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-27)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-314)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-315)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-316)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-28)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-317)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-316)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-316)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-29)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-317)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:26): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 26-30)):
    ```python
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_line}']
    lines.extend(self._format_multiline_message_parts(parts[1:]))
    return lines
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 313-317)):
    ```python
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_line}']
    lines.extend(self.violation_formatter.format_multiline_message_parts(parts[1:]))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-25)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-312)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-26)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-313)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-27)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-314)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-28)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-315)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-28)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-316)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-315)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-316)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-316)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-29)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-317)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-316)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-317)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:24): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 24-30)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-317)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-26)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-26)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-27)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-314)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-27)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-315)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-316)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-28)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-317)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-315)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-316)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-316)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-29)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-317)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-315)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-316)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 311-317)):
    ```python
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts else formatted_message
    lines = [f'- {severity_icon} **{severity.upper()}** - {location_link}: {first_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:22): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 22-30)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 309-317)):
    ```python
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = formatted_message.split('\n')
    first_line = parts[0] if parts...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-26)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-313)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-27)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-315)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-314)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-28)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-317)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-315)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-316)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-29)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-317)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-315)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-316)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 308-317)):
    ```python
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:21): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 21-30)):
    ```python
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {formatted_message}']
    parts = for...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-317)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-27)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-315)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-28)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-316)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-316)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-29)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-317)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-315)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-316)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 307-317)):
    ```python
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_i...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:20): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 20-30)):
    ```python
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in formatted_message:
        return [f'- {severity_icon} **{severit...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-317)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-28)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-315)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:17): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 17-28)):
    ```python
    line_number = violation.get('line_number')
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    tes...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-316)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-314)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-315)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:18): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 18-29)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, loca...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 305-316)):
    ```python
    severity_icon = '<span style="color: red;">[X]</span>' if severity == 'error' else '<span style="color: orange;">[!]</span>' if severity == 'warning' else '<span style="color: blue;">[i]</span>'
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-315)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-316)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:19): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:format_violation_line (lines 19-30)):
    ```python
    location_link = create_file_link_fn(location, line_number)
    test_info = extract_test_info_fn(message, location, line_number)
    formatted_message = format_violation_message_fn(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_link}: {test_info}']
    if '\n' not in fo...
    ```

  Location 2 (validation_report_writer.py:_format_violation_line (lines 306-317)):
    ```python
    location_link = self.file_link_builder.create_file_link(location, line_number)
    test_info = self._extract_test_info(message, location, line_number)
    formatted_message = self.formatter.format_violation_message(message)
    if test_info:
        return [f'- {severity_icon} **{severity.upper()}** - {location_lin...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_formatter.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_formatter.py:35): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_formatter.py:_format_multiline_message_parts (lines 35-39)):
    ```python
    formatted = self._format_message_part(part, in_code_block)
    if part.strip().startswith('```'):
        in_code_block = not in_code_block
    result.append(formatted)
    ```

  Location 2 (violation_formatter.py:format_multiline_message_parts (lines 76-80)):
    ```python
    formatted = self._format_message_part(part, in_code_block)
    if part.strip().startswith('```'):
        in_code_block = not in_code_block
    result.append(formatted)
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```

  Location 2 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:117): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_check_for_errors (lines 117-122)):
    ```python
    scanner_results = rule_result.get('scanner_results', {})
    file_by_file_violations = scanner_results.get('file_by_file', {}).get('violations', [])
    cross_file_violations = scanner_results.get('cross_file', {}).get('violations', [])
    file_by_file_errors = any((v.severity == 'error' if hasattr(v, 'severit...
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:268): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 268-272)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:269): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 269-273)):
    ```python
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanne...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:271): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 271-275)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:272): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 272-276)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-25)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:273): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 273-277)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 22-26)):
    ```python
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECUTION FAILED'))
    li...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:273): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 273-277)):
    ```python
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    lines.extend(self.builder.build_report_location(report_path))
    return lines
    ```

  Location 2 (validation_scanner_status_builder.py:build_status_summary (lines 124-128)):
    ```python
    lines.append(f'### {overall_status} Overall Status: {overall_text}')
    lines.append('')
    lines.extend(self._build_summary_table(stats))
    lines.extend(self._build_totals_summary(stats))
    return lines
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:268): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 268-273)):
    ```python
    lines.extend(self.builder.build_header())
    lines.extend(self.builder.build_metadata())
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:270): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 270-275)):
    ```python
    lines.extend(self.builder.build_summary(validation_rules, files))
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(sel...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:271): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 271-276)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-25)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:272): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 272-277)):
    ```python
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))
    lines.extend(self.violation_formatter.build_violations(validation_rules))
    lines.extend(self.builder.build_instructions(instructions))
    ...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 21-26)):
    ```python
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_failed'], 'Scanner Execution Failures', 'EXECU...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_report_writer.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_report_writer.py:271): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_report_writer.py:_build_report_lines (lines 271-277)):
    ```python
    lines.extend(self.builder.build_content_validated(files, self.file_link_builder.get_relative_path, self._build_scanned_files_section))
    lines.extend(self.scanner_status_formatter.build_scanner_status(validation_rules))
    lines.extend(self.scanner_status_builder.build_validation_rules(validation_rules))...
    ```

  Location 2 (validation_scanner_status_builder.py:build_scanner_status (lines 20-26)):
    ```python
    lines.extend(build_status_summary_fn(stats))
    lines.append('')
    lines.extend(self._format_executed_rules(categorized['executed']))
    lines.extend(self._format_failed_rules(categorized['load_failed'], 'Scanner Load Failures', 'LOAD FAILED'))
    lines.extend(self._format_failed_rules(categorized['execution_f...
    ```
- <span style="color: red;">[X]</span> **ERROR** - [`src\actions\validate\validation_scanner_status_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/actions/validate/validation_scanner_status_builder.py:228): Duplicate code detected across files - extract to shared function.

  Location 1 (validation_scanner_status_builder.py:_format_rule_entry (lines 228-232)):
    ```python
    description = rule_dict.get('rule_content', rule_dict).get('description', 'No description')
    info = lookup.get(rule_file, {})
    status_indicator, status_text = self._get_rule_status_display(info)
    anchor_id = rule_name.replace('_', '-').lower()
    rule_title = rule_name.replace('_', ' ').title()
    ```

  Location 2 (command_file_visitor.py:_generate_base_commands (lines 99-115)):
    ```python
    self.commands[f'{self.bot_name}'] = self._write_command_file(self.commands_dir / f'{self.bot_name}.md', self.python_command)
    self.commands[f'{self.bot_name}-continue'] = self._write_command_file(self.commands_dir / f'{self.bot_name}-continue.md', f'{self.python_command} --close')
    help_content = self...
    ```

#### <span id="use-resource-oriented-design-violations">Use Resource Oriented Design: 1 violation(s)</span>

- <span style="color: red;">[X]</span> **ERROR** - [`src\cli\cli_context_builder.py`](vscode://file/C:/dev/augmented-teams/agile_bot/bots/base_bot/src/cli/cli_context_builder.py:22): Class "CliContextBuilder" uses manager/doer/loader pattern but is not owned by a domain object. Use resource-oriented design instead (e.g., make it a property of a domain object like "CliContext").

## Validation Instructions

The following validation steps were performed:

1. **MANDATORY: Before validating any content, you MUST load and review the project's context files:**
2. 1. Load `{project_area}/docs/stories/clarification.json` - Contains key questions and evidence (generated file)
3. 2. Load `{project_area}/docs/stories/planning.json` - Contains assumptions and decisions (generated file)
4. 3. Load `{project_area}/docs/context/input.txt` (or similar) - Original input/source material if needed for validation (original context)
5. 
6. **CRITICAL: File locations:**
7. - **Generated files:** `{project_area}/docs/stories/clarification.json`, `{project_area}/docs/stories/planning.json`
8. - **Original input:** `{project_area}/docs/context/input.txt` and other original context files
9. 
10. These files contain critical requirements, decisions, and context that MUST be checked against during validation.
*... and 259 more instructions*

## Report Location

This report was automatically generated and saved to:
`C:\dev\augmented-teams\agile_bot\bots\base_bot\docs\stories\reports\code-validation-report-2025-12-24_01-47-58.md`

