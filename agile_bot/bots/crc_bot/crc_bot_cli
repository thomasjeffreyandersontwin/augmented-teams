#!/bin/bash

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

    export WORKING_AREA="${WORKING_AREA:-$(cd "$SCRIPT_DIR/../../.." && pwd)}"

    python3 "$SCRIPT_DIR/src/crc_bot_cli.py" "$@"
    