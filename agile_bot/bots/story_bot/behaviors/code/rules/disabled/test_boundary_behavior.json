{
  "description": "Ensure boundaries work as expected. Create adapters for third-party code, test boundary behavior explicitly, and update tests when upgrading dependencies.",
  "examples": [
    {
      "do": {
        "description": "Test and adapt boundary behavior",
        "content": [
          "Create adapters for third-party code",
          "Test boundary behavior explicitly",
          "Update tests when upgrading dependencies",
          "Write learning tests to understand third-party APIs",
          "",
          "Correct Examples (tested boundaries):",
          "// Learning test for third-party library",
          "describe('SendGrid API behavior', () => {",
          "  test('sends email with HTML body', async () => {",
          "    const client = new SendGridAPI(testApiKey);",
          "    const result = await client.sendEmail({",
          "      to: 'test@example.com',",
          "      subject: 'Test',",
          "      html: '<p>Test</p>'",
          "    });",
          "    expect(result.status).toBe('sent');",
          "  });",
          "});",
          "",
          "// Adapter test",
          "describe('SendGridEmailService', () => {",
          "  test('translates our interface to SendGrid API', async () => {",
          "    const mockClient = createMockSendGridClient();",
          "    const service = new SendGridEmailService(mockClient);",
          "    ",
          "    await service.send('test@example.com', 'Subject', 'Body');",
          "    ",
          "    expect(mockClient.sendEmail).toHaveBeenCalledWith({",
          "      to: 'test@example.com',",
          "      subject: 'Subject',",
          "      html: 'Body'",
          "    });",
          "  });",
          "});",
          "",
          "Boundary Testing Requirements:",
          "- Learning tests explore third-party behavior",
          "- Adapter tests verify correct translation",
          "- Tests updated when upgrading libraries",
          "- Boundary failures caught early"
        ]
      },
      "dont": {
        "description": "Don't trust third-party code implicitly or skip boundary testing",
        "content": [
          "Don't trust third-party code implicitly",
          "Don't skip testing boundary integrations",
          "Don't let boundary changes break your code",
          "",
          "Wrong Examples (untested boundaries):",
          "// No tests for third-party integration",
          "class EmailService {",
          "  send(to, subject, body) {",
          "    // Direct call to third-party, no tests",
          "    return sendGridAPI.send(to, subject, body);",
          "  }",
          "}",
          "",
          "// Upgrade library version → code breaks in production",
          "// No learning tests to catch breaking changes",
          "",
          "Untested Boundary Indicators (ADD TESTS):",
          "- No learning tests for third-party libraries",
          "- No adapter tests verifying integration",
          "- Library upgrades cause production failures",
          "- Unclear how third-party APIs actually behave",
          "- No tests documenting expected boundary behavior",
          "",
          "Testing Strategy:",
          "- Write learning tests to explore third-party APIs",
          "- Test adapters verify correct translation",
          "- Run learning tests when upgrading dependencies",
          "- Treat learning tests as documentation",
          "- Fail fast if third-party behavior changes unexpectedly"
        ]
      }
    }
  ],
  "scanner": "agile_bot.bots.base_bot.src.actions.validate.scanners.test_boundary_behavior_scanner.TestBoundaryBehaviorScanner"
}
