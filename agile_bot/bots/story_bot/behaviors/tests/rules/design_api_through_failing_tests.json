{
  "priority": 11,
  "description": "Write tests against the REAL expected API BEFORE implementing code. Tests MUST fail initially. Set up real test data and call real API. Failure reveals complete API design. Example: project = Project(path=path); project.initialize() (doesn't exist yet -> fails -> drives implementation)",
  "scanners": [
    "agile_bot.bots.base_bot.src.actions.validate.scanners.failing_test_api_scanner.FailingTestApiScanner"
  ],
  "do": {
    "description": "Write test against real expected API that fails initially. Example: project = Project(path); project.initialize(); assert project.is_ready (fails until implemented)",
    "guidance": [
      {
        "description": "Call the real expected API even if it doesn't exist",
        "example": ["from mymodule import Project  # Will fail: ImportError", "project = Project(path=path)", "project.initialize()  # Will fail: AttributeError"]
      },
      {
        "description": "Set up real test data (files, directories)",
        "example": ["config_path = tmp_path / 'config.json'", "config_path.write_text(json.dumps({'name': 'bot'}))", "project = Project(config_path=config_path)  # Real file, real path"]
      },
      {
        "description": "Assert real expected behavior",
        "example": ["assert project.agent.name == 'story_bot'", "assert project.config_path.exists()"]
      }
    ]
  },
  "dont": {
    "description": "Don't use placeholders, dummy values, or skip the failing step. Example: project = 'TODO' (wrong); assuming test passes first (wrong)",
    "guidance": [
      {
        "description": "Don't use placeholder values",
        "example": ["project = 'placeholder'  # WRONG - call real constructor", "config = {}  # WRONG if real config has required fields"]
      },
      {
        "description": "Don't skip the RED phase",
        "example": ["# WRONG: Implementing code before test fails", "# Test should fail with ImportError/AttributeError first"]
      },
      {
        "description": "Don't write tests that pass immediately",
        "example": ["def test_something(): assert True  # WRONG - proves nothing"]
      }
    ]
  }
}
