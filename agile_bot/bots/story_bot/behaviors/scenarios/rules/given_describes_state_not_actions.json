{
  "priority": 1,
  "description": "Given statements describe STATE/PRECONDITIONS, not actions or functionality. Given = what exists before test. When = first action. Then = expected behavior. Example: Given user is logged in (state), not Given user logs in (action).",
  "scanners": [
    "agile_bot.bots.base_bot.src.actions.validate.scanners.given_state_not_actions_scanner.GivenStateNotActionsScanner"
  ],
  "do": {
    "description": "Given describes state/preconditions only. Example: 'Given user is logged in' (state), 'Given character sheet exists' (precondition)",
    "guidance": [
      {
        "description": "Use state language - what EXISTS, not what HAPPENS",
        "example": ["Given user is logged in", "Given character sheet exists", "Given workflow state is persisted", "Given bot has behavior configured as 'shape'"]
      },
      {
        "description": "First action is always When, never Given",
        "example": ["Given bot is configured (STATE)", "When Tool invokes method (ACTION)", "Then Action loads instructions (OUTCOME)"]
      },
      {
        "description": "Preconditions describe what's true BEFORE the action",
        "example": ["Given activity log is initialized (precondition)", "When action completes (trigger)", "Then activity log captures data (functionality being tested)"]
      }
    ]
  },
  "dont": {
    "description": "Don't describe actions or functionality in Given. Example: 'Given user logs in' (action - wrong), 'Given system validates input' (functionality - wrong)",
    "guidance": [
      {
        "description": "Don't use action verbs in Given - clicking, sending, calling are When actions",
        "example": ["Given user clicks button (WRONG)", "Given system sends message (WRONG)", "Given API is called (WRONG)", "Given tool executes (WRONG)"]
      },
      {
        "description": "Don't describe functionality being tested in Given - that's what Then is for",
        "example": ["Given activity log tracks: timestamp, action_state (WRONG - this is functionality to test in Then)"]
      },
      {
        "description": "Don't use past tense actions in Given",
        "example": ["Given Tool has invoked method (WRONG - 'invoked' is action)", "Given user has clicked (WRONG - 'clicked' is action)"]
      }
    ]
  }
}
