{
  "priority": 4,
  "description": "Scenarios must cover happy path, edge cases, and error cases based on acceptance criteria. Example: Valid input → success; Boundary value → validates; Invalid input → error message.",
  "scanners": [
    "agile_bot.bots.base_bot.src.actions.validate.scanners.scenarios_cover_all_cases_scanner.ScenariosCoverAllCasesScanner"
  ],
  "do": {
    "description": "Cover all case types: happy path, edge cases, error cases. Example: User enters valid data → success; User enters boundary → validates; User enters invalid → error",
    "guidance": [
      {
        "description": "Include happy path scenario",
        "example": ["Scenario: User enters valid data - When user submits valid form Then system saves successfully"]
      },
      {
        "description": "Include edge case scenarios",
        "example": ["Scenario: User enters boundary value - When user enters maximum allowed value Then system validates correctly"]
      },
      {
        "description": "Include error case scenarios",
        "example": ["Scenario: User enters invalid data - When user submits empty form Then system shows validation error"]
      }
    ]
  },
  "dont": {
    "description": "Don't skip case types. Example: Only happy path scenarios (missing edge and error cases)",
    "guidance": [
      {
        "description": "Don't cover only happy path",
        "example": ["Only 'User submits valid data' scenario - MISSING edge cases and error cases"]
      },
      {
        "description": "Don't assume error handling is implicit",
        "example": ["No explicit error scenarios - must show what happens when things fail"]
      }
    ]
  }
}
