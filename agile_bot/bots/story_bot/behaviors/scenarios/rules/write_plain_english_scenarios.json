{
  "priority": 6,
  "description": "Write scenarios in plain English. NO variables, NO placeholders, NO Scenario Outlines, NO Examples tables at initial scenario writing stage. Example: 'Given user has typed request message start shaping' not 'Given user has typed <request_message>'.",
  "scanners": [
    "agile_bot.bots.base_bot.src.actions.validate.scanners.plain_english_scenarios_scanner.PlainEnglishScenariosScanner"
  ],
  "do": {
    "description": "Plain English scenarios without variables. Example: 'Given user has attached documents' not 'Given user has attached <documents>'",
    "guidance": [
      {
        "description": "Use concrete values, not placeholders",
        "example": ["Given user has typed request message 'start shaping'", "When AI Chat processes request", "Then AI Chat identifies story shaping keywords"]
      },
      {
        "description": "Write readable, human-understandable steps",
        "example": ["Given user has attached documents to chat window", "And user has typed request message 'start shaping'"]
      }
    ]
  },
  "dont": {
    "description": "Don't use variables or placeholders initially. Example: '<request_message>' variable (wrong at this stage), Scenario Outline (wrong at this stage)",
    "guidance": [
      {
        "description": "Don't use angle-bracket variables",
        "example": ["Given user has typed request message '<request_message>' (WRONG - has variable)"]
      },
      {
        "description": "Don't use Scenario Outline at initial stage",
        "example": ["Scenario Outline: AI Chat detects keywords (WRONG - Outline at this stage)", "Examples: | request_message | (WRONG)"]
      }
    ]
  }
}
