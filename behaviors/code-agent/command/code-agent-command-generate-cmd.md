### Command: `/code-agent-command-generate`

**Purpose:** Generate files for a new command. Delegates to main command with explicit generate action.

**Usage:**
* `/code-agent-command-generate [feature-name] [command-name] [command-purpose] [target-entity]` — Generate command files (AI determines parameters if not provided)

**Steps:**
1. **Code** Execute the generate action in `/code-agent-command`

