### Command: `/code-agent-sync-generate`

**Purpose:** Generate files for new commands, rules, and configuration files. Delegates to main command with explicit generate action.

**Usage:**
* `/code-agent-sync-generate` — Generate command files

**Steps:**
1. **Code** Execute the generate action in `/code-agent-sync`
