### Command: `/code-agent-sync-validate`

**Purpose:** Validate files for commands, rules, and configuration files. Delegates to main command with explicit validate action.

**Usage:**
* `/code-agent-sync-validate` — Validate command files

**Steps:**
1. **Code** Execute the validate action in `/code-agent-sync`
