### Command: `/code-agent-feature-generate`

**Purpose:** Generate feature files for a new Code Agent feature. Delegates to main feature command with explicit generate action.

**Usage:**
* `/code-agent-feature-generate [feature-name] [location]` — Generate feature files (AI determines name/location if not provided)


**Steps:**
1. **Code** Execute the generate action in `/code-agent-feature`

